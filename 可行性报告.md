# 侧边栏三项功能可行性报告

> 基于 `main` 分支（v0.11.6）代码库现状整理，仅作评估，未实施。
> 生成日期：2026-08-28

---

## 一、同窗口拖放文件到侧边栏（位置/设备）进行复制或移动（对话框）

### 1.1 需求

把文件（夹）从文件列表拖到左侧侧边栏的条目上（常用位置 Places、设备分区、MTP/PTP 设备），弹出「移动 / 复制 / 取消」对话框后执行操作。

### 1.2 现状（可复用资产）

| 资产 | 位置 | 作用 |
| --- | --- | --- |
| `handleDropOnTarget` | `ExplorerTab.tsx:429` | 完整落点管线：回收站语义 → 拖拽动作对话框 → 冲突对话框 → 批量任务 + 进度 toast。仅需 `targetDirFiles`（目标目录列表） |
| `handleDropOnBreadcrumb` | `ExplorerTab.tsx:561` | **先例**：对面包屑目录（非当前目录）先 `FileSystemService.listDir(targetPath)` 补列表，再调 `handleDropOnTarget`；`trash://` 走 `trashFiles` |
| TabBar 文档级捕获监听 | `TabBar.tsx:119-178` | 原生 `startDrag` 后 HTML5 会话终止，元素级 onDrop 不可靠 → `document` 捕获阶段 dragover/drop + `elementFromPoint` + `closest()` 命中路由；只接受内部拖拽（`DragContext.getDragState` 存活） |
| `useDragActionDialog` | App 层 | 移动/复制/取消 M3 对话框，`requestDragAction` 已注入 ExplorerTab |
| 挂载能力 | `handleDeviceMount` / `handleGvfsMount` | 未挂载设备可先挂载（返回 mountpoint）再落点 |

### 1.3 方案

**推荐：路由复用「标签页落点」模式（Option A），不动操作管线核心。**

1. **Sidebar 侧**（`Sidebar.tsx`）：
   - 给每个可落点条目加数据属性：`data-drop-target`（真实路径或 `trash://`）；Dashboard 按钮不加（不可落点）。
   - 复制 TabBar 的文档级捕获监听模式：dragover 时 `elementFromPoint` → `closest('[data-drop-target]')` → `preventDefault` + 高亮（`.drag-over` 类，参考 `.breadcrumb-chip.drag-over`）；drop 时经 `shouldSuppressDrop()` 过滤幻影回弹。
   - 命中条目后区分三类目标：
     - Places：直接用其 `path`（`Trash` → `trash://`）；
     - 块设备分区：已挂载 → `mountpoint`；未挂载 → 先 `onDeviceMount` 再取 `mountpoint`；
     - GVfs 卷：已挂载 → `mountpoint`；未挂载 → 先 `onGvfsMount`（已有地址漂移/竞态处理）。
   - 从 `DragContext` 取 `{files, sourcePath}`，Shift 强制复制（与 TabBar 一致），把 `{targetPath, files, operation, sourcePath}` 通过新 prop `onDropFiles` 交给 App。
2. **App 侧**（`App.tsx`）：
   - 新增 `pendingSidebarDrop` 状态（结构与 `pendingTabDrop` 相同 + `targetPath`），set 后交给当前活动标签页的 ExplorerTab 消费（复用 `pendingDrop` 管道，或扩展现有类型加可选 `targetPath`）。
3. **ExplorerTab 侧**：
   - `pendingDrop` 消费 effect 中：若带 `targetPath`，先 `listDir(targetPath)` 补目标列表（面包屑落点同款逻辑），再调 `handleDropOnTargetRef`。`trash://` 走 `trashFiles`。其余（对话框、冲突、任务、toast、回收站还原语义）零改动复用。

**Option B（备选）**：把 `handleDropOnTarget` 提炼成 App 级共享 hook/util，Sidebar 直接调用。改动面大（触及 ExplorerTab 核心路径），回归风险高，不推荐。

### 1.4 边界与风险

- 同源目录：复用 `drop.same_dir` toast；拖回自身已有过滤（`f.path !== targetPath`）。
- 拖到 `trash://` = 移入回收站（与面包屑行为一致，无对话框）。
- 只接受**同窗口内部拖拽**——与 TabBar 决定一致：跨窗口/外部拖放不把侧边栏作为目标（避免高亮卡死与路径交付不可靠）。
- 文档级监听器新增一组，与 TabBar 的监听并存：命中选择器互斥（`.tab-item` vs 侧边栏条目），无冲突；dragover 每次多一次 closest 查询，开销可忽略。
- 挂载失败/超时：复用 `useDeviceActions` 既有 toast，直接中止落点。
- EXDEV 跨设备移动：后端任务管线已自动回退复制+删除，无需处理。

### 1.5 结论

**可行，工作量中等（约 250–350 行 + i18n + CSS）**。所有硬逻辑（对话框、冲突、任务、回收站语义）均已存在且有先例（标签页/面包屑落点），改动集中在"路由"层，对现有功能几乎无侵入。唯一新增逻辑是"未挂载设备先挂载再落点"，且挂载函数现成。

---

## 二、侧边栏设备过多溢出时增加滚动条

### 2.1 需求

设备区条目过多时按钮溢出屏幕，需要滚动。

### 2.2 现状（根因已定位）

- `.sidebar`（`Sidebar.css:1-13`）**已声明** `overflow-y: auto` 与细滚动条样式（`scrollbar-width: thin` + WebKit 样式），但没有生效。
- 根因是 flexbox 的 `min-height: auto` 陷阱：`.app-shell`（`index.css:99-104`）为 `display: flex; height: 100vh`，`<aside class="sidebar">` 是其横向 flex 容器中的子项，交叉轴被 stretch。子项默认 `min-height: auto`（内容最小高度），当设备条目超出视口时，子项高度被内容撑大超过视口，`overflow-y: auto` 永远不会触发——内容被 `body { overflow: hidden }` 裁掉，表现为"按钮溢出屏幕、无滚动条"。
- 现有设备树本身无独立滚动容器（`.sidebar-section` / `.sidebar-list` 均无 overflow 设置）。

### 2.3 方案

1. **必做（一行修复）**：`.sidebar` 增加 `min-height: 0`，使 flex 子项可收缩到容器内，`overflow-y: auto` 立即生效，整栏出现已有样式的滚动条。
2. **可选增强 A（推荐）**：Places 区固定、设备区独立滚动——设备 section 设 `flex: 1; min-height: 0; overflow-y: auto`，整个 aside 保持不滚。优点：常用位置始终可见；缺点：出现双滚动场景时体验需斟酌（设备区很少超过一屏时无感）。
3. **可选增强 B**：设备组折叠/展开（`<details>` 或状态切换），滚动与收纳双管齐下。
4. **边缘自动滚动（顺手项，建议纳入）**：拖放过程中光标贴侧边栏上下边缘时自动滚动，便于拖到长列表底部设备（TabBar 有同类需求但当前代码无 drag 自动滚动；橡皮筋框选已有边缘自动滚动先例可参考）。

### 2.4 风险

- 纯 CSS 改动，无逻辑风险。增强 A 需注意 `.sidebar-device-group` 的 margin 与滚动容器内边距的视觉协调。
- 若实测发现滚动条已在某些环境下出现，也只需按增强项微调即可。

### 2.5 结论

**可行，工作量极小**（修复 1 行 CSS；增强 A 约 10–15 行 CSS）。根因明确，无回归面。

---

## 三、侧边栏固定目录（Pin）功能

### 3.1 需求

用户可以把自己选的目录固定在侧边栏，点击即可导航。

### 3.2 现状（可复用资产）

| 资产 | 位置 | 作用 |
| --- | --- | --- |
| 仪表盘固定项 | `Dashboard.tsx:62-123` | `useLocalStorage<PinnedItem[]>('dashboard.pinned', [])`：持久化 + 多窗口 storage 事件同步、首次启动播种默认项、文件/目录两类选择对话框、悬停移除按钮，全部现成可抄 |
| `useLocalStorage` | `hooks/useLocalStorage.ts` | 通用持久化 hook（跨窗口同步已实现） |
| IPC | `pickFile` / `pickDirectory` / `stat` | 已存在（Dashboard 在用），无需后端改动 |
| 侧边栏结构 | `Sidebar.tsx` | Places / Devices 两 section + `onNavigate` 注入，加第三个 section 即可 |

### 3.3 方案

1. **状态**：`useLocalStorage<SidebarPinnedItem[]>('sidebar.pinned', [])`——**独立键**，不与 `dashboard.pinned` 混用（两处语义不同，耦合会互相污染）。
2. **UI（`Sidebar.tsx`）**：在 Places 与 Devices 之间新增「已固定」section（参考 Places 的 `.sidebar-item` 渲染），条目点击 → `onNavigate(path)`，悬停显示移除按钮（Dashboard pinned-item 同款）。
3. **添加入口（三选一，可组合）**：
   - 区块底部「+ 固定目录」按钮 → `pickDirectory` + `stat`（Dashboard 同款，工作量最小）；
   - 对已有条目（Places / 已挂载设备）右键菜单加「固定」——需接入 App 层 `useContextMenu` 体系，工作量中等；
   - 拖目录到该 section 固定（可与报告一共享拖放路由，但作为远期项）。
4. **类型与 i18n**：`PinnedItem` 类型目前定义在 Dashboard 内，需提升为共享类型（或新建 `SidebarPinnedItem`）；新增文案键覆盖全部 12 种语言。
5. **可选扩展**：固定项拖拽排序（Dashboard 固定项本身也无排序，见 `进度.md` 未实现清单）、支持固定文件（点击用系统默认打开，需复用 ExplorerTab 的 `openFile` 链路，建议一期只做目录）。

### 3.4 风险

- 低。不触碰文件操作与拖放管线；storage 键独立，不会影响 Dashboard。
- 需注意 Places 与固定项重复显示时的体验（允许重复或去重，建议允许，用户自选）。
- 右键菜单入口若做，需小心 App 层 context menu 状态已有 file/device/gvfs 三类，新增 sidebar 类时保持序列化弹窗约束。

### 3.5 结论

**可行，工作量低（约 100–200 行 + i18n）**。核心样板（持久化、跨窗口同步、选择对话框、移除交互）已由仪表盘固定项验证，无后端改动。

---

## 总结

| 功能 | 可行性 | 工作量 | 主要依据 |
| --- | --- | --- | --- |
| 拖放到侧边栏复制/移动 | 高 | 中（250–350 行） | 复用标签页/面包屑落点管线，仅新增路由层 |
| 设备区滚动条 | 高 | 极小（1 行 + 可选 CSS） | 根因 `min-height: auto` 已定位 |
| 固定目录 | 高 | 低（100–200 行） | 复用仪表盘固定项全套样板 |

三项均不破坏现有逻辑；实现时遵守 AGENTS.md 约束（JSDoc、字段说明、i18n、先验证不破坏既有功能）。

---

## 四、实施决策记录（2026-08-28，已实施）

### 4.1 侧边栏拖放（报告一，推荐方案 A 落地）

- **目标标识**：条目加 `data-sidebar-target`，三种前缀——`place:<path>`（含 `trash://`）、`device:<devicePath>`、`gvfs:<key>`（key = `mountpoint ?? deviceId ?? name`，与 React key 一致）。仪表盘按钮不加（不可落点）。
- **事件路由**：`Sidebar.tsx` 内文档级捕获监听（dragover/drop/dragend）+ `elementFromPoint` + `closest('[data-sidebar-target]')`，与 TabBar 同款模式；仅接受同窗口内部拖拽（`DragContext` 存活）；drop 前经 `shouldSuppressDrop()` 过滤幻影回弹。
- **落点解析** `resolveDropTarget`：place 直接用路径；设备/卷已挂载用挂载点，未挂载先调 `onDeviceMount`/`onGvfsMount`（复用既有稳健挂载与 toast）；同目录拦截（`drop.same_dir`）。
- **执行管道**：Sidebar → App（`pendingSidebarDrop`）→ 活动标签页 ExplorerTab（`pendingDrop` 新增可选 `targetPath`）→ 先 `listDir(targetPath)` 补目标列表 → 复用 `handleDropOnTarget`（动作对话框/冲突/任务/回收站语义零改动）。读取目标失败新增 toast `drop.target_unreadable`。
- **Wayland 兜底**：`nativeDragTracker.resolveTargetKey` 新增 `sidebar:` 目标解析，合成 drop 也能路由到侧边栏。
- 涉及文件：`Sidebar.tsx`、`SidebarPartitionItem.tsx`（`dropTarget`/`dragOver` props）、`App.tsx`、`ExplorerTab.tsx`、`nativeDragTracker.ts`、`Sidebar.css`。

### 4.2 滚动条（报告二，必做 + 边缘自动滚动落地）

- **必做**：`.sidebar` 增加 `min-height: 0`（修复 flex `min-height: auto` 陷阱），原有 `overflow-y: auto` + 细滚动条样式立即生效。
- **边缘自动滚动**：`Sidebar.tsx` 内 rAF 循环——dragover 时按光标 Y 距侧边栏上下边缘的距离线性调速（边缘区 64px、最大 16px/帧），进入边缘区启动、离开停止；drop/dragend/非内部拖拽时停止；自调度经 `tickAutoScrollRef` 间接引用（避免 useCallback 自引用）。滚动范围为整个侧边栏容器。
- 纯 CSS/前端改动，未做「Places 固定 + 设备区独立滚动」增强项（保留为可选后续）。

### 4.3 固定目录（报告三落地）

- **竖向顺序**：位置（Places）→ 已固定目录（空时整个 section 隐藏）→ 固定按钮（常显）→ 设备。
- 状态：`useLocalStorage<SidebarPinnedItem[]>('sidebar.pinned', [])`，独立存储键，与仪表盘 `dashboard.pinned` 互不影响，跨窗口 storage 事件自动同步。
- 条目：div+role=button（内含悬停显现的移除 IconButton，避免嵌套 button），点击 `onNavigate`，同样可作拖放目标（`place:` 前缀）；名称走 MarqueeText。
- 固定按钮：`pickDirectory` → `stat` 校验目录 → 按路径去重（重复提示 `sidebar.already_pinned`）→ 追加。
- 一期仅目录（`isDir` 字段保留以备将来支持文件）；未做拖拽排序与右键菜单入口。
- 新增 i18n 键（12 语言）：`sidebar.pinned`、`sidebar.add_pin`、`sidebar.unpin`、`sidebar.already_pinned`、`drop.target_unreadable`。

### 4.4 验证

- `npx tsc -b`、`npm run lint`、`npm run build` 全部通过（chunk 体积警告为既有现象，与本次改动无关）。

---

## 五、四项修复/改版可行性评估（2026-08-28，仅评估，未实施）

### 5.1 固定按钮与 Places 间距不一致

- **根因**：`.sidebar-section` 无底边距，固定按钮 section 直接紧贴 Places 列表；Places 条目之间是 `.sidebar-list` 的 4px gap，而最后一个条目到固定按钮之间为 0。
- **方案**：给固定按钮所在 section 加 `margin-top: 4px`（或统一 `.sidebar-section` 间距规范）。
- **结论**：**可行，纯 CSS 一行级改动，无风险。**

### 5.2 取消固定（×）按钮位置随名称漂移

- **根因**：`.sidebar-item` 是 flex 行，`.sidebar-label` 无 `flex: 1; min-width: 0`，宽度随名称增长，把 × 按钮往右推（名称超长时甚至溢出屏幕）。
- **方案**：给固定条目内的 label 加 `flex: 1; min-width: 0`（已有 ellipsis 截断生效），× 按钮自然钉在最右。**必须作用域限定**（如 `.sidebar-pinned-item .sidebar-label`），不能直接改 `.sidebar-label` 全局——它在 `.sidebar-partition-info`（column flex）里，`flex: 1` 会变成垂直拉伸，可能破坏分区条目布局；设备头里已有 `flex:1` 占位 div，改了也会重复占用。
- **结论**：**可行，约 3 行 CSS + 1 个类名，低风险**（作用域限定后不影响其他条目）。

### 5.3 固定了 Places 目录后，进入该目录 Places 按钮同时高亮

- **根因**：Places 条目高亮条件是 `currentPath === place.path`（active 类）与 `currentPath.startsWith(place.path)`（filled 图标）；固定项同样按此高亮。固定项路径与 Place 相同时两者同时高亮。
- **方案**：渲染 Places 时，若 `pinnedDirs.some(p => p.path === place.path)`，则抑制该 Place 的 active/filled（固定项优先展示高亮）。只抑制**精确相等**；固定目录是 Place 的**子目录**时（如固定 `~/Documents/Work`），进入子目录 Documents 图标仍高亮祖先——语义合理，保持不变。
- **结论**：**可行，Sidebar 内约 5 行 JSX 改动，无风险。** 注意该判断需要读 `pinnedDirs`，与 5.4 的状态上提天然兼容。

### 5.4 固定交互改版（按钮进入固定模式 + 拖拽固定 + 目录右键菜单固定；保留文件管理器选择）

#### 5.4a 固定按钮「按下高亮 → 拖动文件夹到按钮上固定」

- **现有资产**：侧边栏已有文档级拖放路由（`data-sidebar-target` + `elementFromPoint`），固定按钮只需加 `data-sidebar-target="pin:"` 并在 `resolveDropTarget` 加分支：校验 `dragState.files` 恰为 1 个且 `isDirectory` → 固定（去重 + toast）；悬停高亮直接复用 `dragOverTarget` 机制。
- **armed 状态**：`pinArmed` state，点击按钮切换；视觉用类（如 outline 常亮）。建议语义：armed 仅为视觉提示，**拖动单个文件夹悬停时无论 armed 与否都可固定**（武装态只是让入口更明显）；Esc / 固定成功 / 点击其他地方时解除 armed。
- **校验**：非单个目录 → 新 toast 键 `sidebar.pin_single_folder`；回收站条目（`trashOriginalPath` 存在）拒绝固定。
- **结论**：**可行，中等工作量（约 60–100 行）**，全部复用既有拖放管线。

#### 5.4b 文件夹右键菜单加「固定」

- **位置**：`App.tsx` 文件上下文菜单构建器（`menuItems`，~405 行起）——`item.isDirectory` 时追加「固定到侧边栏」；已固定时改显示「从侧边栏取消固定」。批量选中时建议仅单目录显示该菜单项（一期不做批量固定）。
- **关键前置**：`pinnedDirs` 目前存于 Sidebar 内（`useLocalStorage`）。**同窗口两个 useLocalStorage 同键实例不会互相同步**（storage 事件只在跨窗口触发），所以必须**把 `sidebar.pinned` 状态上提到 App**，Sidebar 改为受控组件（value + setter props）。这是本项唯一结构性改动，波及 App/Sidebar，低风险。
- **i18n**：`context_menu.pin/unpin` 键 12 语言已存在但**无组件使用**（死键，且文案是「固定到仪表盘」）。建议新增 `context_menu.pin_sidebar` / `context_menu.unpin_sidebar`（12 语言 ×2），不误用仪表盘文案。
- **结论**：**可行，工作量中等（状态上提 + 菜单项 + i18n，约 80–120 行）。**

#### 5.4c 固定按钮右键菜单：「使用默认文件管理器选择」（保留现有方案）

- **现有资产**：Dashboard 已有「本地 state + ContextMenu 组件」先例（`pinMenuPos` → 两项菜单）；Sidebar 可照搬：按钮 `onContextMenu` → 本地菜单 state → ContextMenu 单项「使用默认文件管理器选择」→ 调现有 `handleAddPinned`（pickDirectory 流程原样保留）。
- **结论**：**可行，工作量极小（约 30–40 行 + i18n 1 键 ×12）。**

#### 5.4 汇总

| 子项 | 可行性 | 工作量 | 风险 |
| --- | --- | --- | --- |
| 5.4a 按钮模式 + 拖拽固定 | 高 | 中 | 复用拖放管线，无新机制 |
| 5.4b 目录右键固定 | 高 | 中 | 唯一结构性改动：pinned 状态上提 App |
| 5.4c 按钮右键文件管理器选择 | 高 | 小 | 照搬 Dashboard 菜单先例 |

- **实施顺序建议**：先做 5.1–5.3（微改动）→ 5.4c（独立）→ 状态上提（为 5.4b 铺路）→ 5.4a → 5.4b。
- **新增 i18n 键预估（×12 语言）**：`context_menu.pin_sidebar`、`context_menu.unpin_sidebar`、`sidebar.pin_via_file_manager`、`sidebar.pin_single_folder`（4 键）。
- **对现有功能的破坏面**：5.4b 的状态上提会动 Sidebar props（App 传 value/setter），其余为增量；5.3 的高亮抑制只影响「固定项与 Place 相同」的场景。整体无后端改动。

### 5.5 实施记录（2026-08-28，已按评估实施）

- **5.1**：`.sidebar-pin-section { margin-top: 4px }`，与条目间 4px gap 一致。
- **5.2**：固定条目标签加 `.sidebar-pin-label`（`flex: 1; min-width: 0`），作用域限定，× 按钮钉在最右；设备头/分区条目不受影响。
- **5.3**：Places 渲染时 `pinnedDirs.some(p => p.path === place.path)` 则抑制 active/filled，高亮让位固定条目（仅精确相等）。
- **5.4c**：固定按钮右键弹 ContextMenu（本地 state + 组件，Dashboard 同款先例），单项「使用文件管理器选择」→ 原 `pickDirectory + stat + 去重` 流程（经 `onPinPath`）。
- **5.4 状态上提**：`sidebar.pinned` 由 App 持有（`useLocalStorage`），`pinSidebarDir`（去重 + toast）/`unpinSidebarDir` 下发 Sidebar 与文件右键菜单；Sidebar 改为受控 props。
- **5.4a**：固定按钮 `data-sidebar-target="pin:"` 接入既有拖放路由；点击切换 armed 高亮（`.pin-armed`），Esc / 点击按钮外 / 固定成功解除；drop 校验「恰好 1 个目录且非回收站条目」，否则 toast `sidebar.pin_single_folder`；无需 armed 也可直接拖入固定。
- **5.4b**：`App.tsx` 文件菜单对目录追加「固定到侧边栏 / 从侧边栏取消固定」（`push_pin` 图标）。
- **仪表盘右键固定（5.4b 同构，追加）**：文件与文件夹右键菜单追加「固定到仪表盘 / 从仪表盘取消固定」（复用既有 `context_menu.pin/unpin` 键，无新增 i18n）；`dashboard.pinned` 状态同样上提 App（`PinnedItem` 类型自 Dashboard 导出），经 ExplorerTab 透传 Dashboard 受控化；Dashboard 原「添加固定项」文件管理器选择流程不变；首次启动默认项播种逻辑随状态上提至 App，语义一致；块设备（`inode/blockdevice`）不出现在固定菜单。
- **i18n**：新增 4 键 × 12 语言（`context_menu.pin_sidebar`、`context_menu.unpin_sidebar`、`sidebar.pin_via_file_manager`、`sidebar.pin_single_folder`）。
- **验证**：`npx tsc -b`、`npm run lint`、`npm run build` 全部通过。交互行为（armed 高亮、拖入固定、菜单）建议实机验证。

---

## 六、仪表盘存储卡片扩展可行性评估（/home + 外接设备，2026-08-28，仅评估，未实施）

### 6.1 现状

- 后端 `system:get-storage-usage`（`electron/handlers/system.ts:957`）：`df -kP /` 解析单条输出，返回 `{total, used, free}`。
- 前端唯一消费者为 Dashboard（`window.electron.getStorageUsage()`，挂载时拉取一次，渲染单张「System Storage」卡片）。
- 设备数据源已齐备：`system:get-all-devices`（lsblk 树，含 `mountpoint/label/model/fstype`，`isExternalDevice`/`getDeviceTitle` 工具现成）、`system:get-gvfs-volumes`（MTP/PTP，含挂载点与显示名）；事件推送 `system:devices-changed`（UDisks2）与 `system:gvfs-changed` 已广播所有窗口，Sidebar 已有「订阅 + 5s 轮询兜底」的成熟模式可抄。
- `getStorageUsage` 无其他消费者，改造无破坏面。

### 6.2 方案

**后端（约 40–60 行）**

1. 新增批量 IPC `system:get-storage-usages(paths: string[])`：一次 `df -kP <p1> <p2> …` 解析多行，返回 `{ path, filesystem, total, used, free }[]`；`df` 对 `/home`、`/run/media/...`、gvfs FUSE 挂载点（`/run/user/<uid>/gvfs/...`）均适用。原 `system:get-storage-usage` 保留不动（兼容）。
2. `/home` 是否独立分区的判断：比较同一批 `df` 输出中 `/` 与 `/home` 的 `filesystem`（设备列）——不同则展示独立卡片；相同（含 bind mount）则合并，避免重复数字。

**前端（Dashboard 约 100–150 行 + CSS + i18n）**

3. `storage` 单状态改为 `usages: StorageCard[]`（`{ label, subtitle, total, used, free }`）：
   - 系统卡：`df /`，固定显示。
   - 家目录卡：`df <home>`，仅 filesystem 与 `/` 不同时显示（见 2）。
   - 外接卡：`getAllDevices` 过滤 `isExternalDevice && mounted && mountpoint` + `getGvfsVolumes` 过滤 `mounted`，每个挂载点一张卡（一块磁盘多分区 = 多卡），label 用 `getDeviceTitle`/卷显示名，subtitle 为挂载点。
4. 订阅与自动隐藏：Dashboard 内自行 `onDeviceChange` + `onGvfsChange`（ipcRenderer 支持多监听者，与 Sidebar 订阅并存无冲突）；事件到达后重新枚举挂载点 → 拉批量 usage → 卡片增删自动生效（拔出即消失）。无 watcher 时沿用 Sidebar 的 5s 轮询兜底。切回仪表盘标签时 Dashboard 重新挂载会自然重拉（现为条件渲染）。
5. UI：复用 `.dashboard-card storage-card` 结构逐卡渲染；`dashboard-grid` 为 `auto-fit minmax(300px,1fr)`，卡片增多自动换行，无需改布局。`formatBytes`/`getUsagePercent` 现成。
6. i18n：新增 `dashboard.home_storage`（家目录卡标题，×12 语言）；外接卡标题用系统设备名，无需翻译键。

### 6.3 边界与风险

- **/home 与 / 同分区**：按 2 合并为一张系统卡（推荐）；若用户坚持「始终显示 /home」，仅需去掉合并判断——两种均可，实现成本相同。
- **FUSE 挂载点**：df 可读 gvfs 挂载点；未挂载卷（mountpoint=null）自动跳过。
- **挂载事件时序**：UDisks2 事件到达时挂载点已可用，df 直接可查；个别竞态可用一次重试或延迟刷新兜底。
- **重复挂载/快照**：按挂载点枚举天然去重；同一 filesystem 多次挂载（罕见）不特殊处理。
- **性能**：df 一次批量查询毫秒级，事件驱动 + 切回刷新，无常开轮询。
- **破坏面**：`getStorageUsage` 仅 Dashboard 使用；改造成「列表渲染」局限在 Dashboard 与新增 IPC，Sidebar/设备管理/其他逻辑零改动。

### 6.4 结论

**三项子功能全部可行，整体工作量小（约 200–250 行 + 1 个 i18n 键 ×12）**：
- /home 卡：高可行，后端一行 `df` + filesystem 对比；
- 外接卡：高可行，设备枚举/事件/UI 组件全部现成，仅新增「按挂载点批量 df」；
- 拔出自动隐藏：高可行，直接复用既有设备事件推送与 Sidebar 订阅模式。

无后端结构风险、无其他功能破坏面；唯一设计决策点是「/home 与 / 同分区时是否仍显示 /home 卡」（见 6.3 第一项）。

### 6.5 实施记录（2026-08-28，已实施）

- 决策：**/home 卡无条件显示**（无论是否独立分区，与系统卡数值可能相同）。
- 后端：新增 `system:get-storage-usages(paths[])`，主进程 `fs.promises.statfs` 逐路径查询（无 shell 解析、单条失败跳过）；原 `getStorageUsage` 保留。
- 前端 Dashboard：`storage` 单卡改为 `storageCards[]`——系统卡 + 家目录卡 + 每个已挂载外接设备（块设备分区递归收集 + gvfs 卷）一张卡；副标题显示挂载点（省略号截断）；卡片为空时显示原加载占位。
- 热插拔：Dashboard 自行订阅 `onDeviceChange` / `onGvfsChange`（拔出自动消失），无 watcher 时 5s 轮询兜底；切回仪表盘重挂载自然重拉。
- i18n：新增 `dashboard.home_storage` × 12 语言（命名与各语言侧栏「主页」按钮 `sidebar.home` 一致：主页/Home/ホーム/홈/Главная 等）。
- 验证：`npx tsc -b`、`npm run lint`、`npm run build` 全部通过；设备插拔行为建议实机验证。

---

## 七、存储区重构可行性评估（单一区域 + 子区域，2026-08-28，仅评估，未实施）

### 7.1 需求解读

- 存储占用合并为**一个区域**（一张卡），不再每设备一张卡。
- 卡片内设**子区域**（动态数量 = 系统 + 主页 + N 个已挂载外接设备）；全屏时三个子区域横向排布，窗口变窄自动换行。
- 子区域显示目录/设备的存储占用；**点击子区域跳转**到对应目录或设备挂载点。
- **/home 非独立分区时，主页子区域隐藏占用信息**（保留子区域本身作为导航入口，见 7.4 决策点 1）。
- 整体符合 M3 设计。

### 7.2 现状（可复用资产）

- 后端 `system:get-storage-usages` 已按路径批量 statfs（本次会话已实现，无 shell 解析）；只需每条结果**附带 `dev`**（`fs.stat(p, {bigint:true}).dev` 转字符串）用于判断「/home 是否独立分区」——本机已验证：`stat('/').dev` 与 `stat(home).dev` 不同值即独立分区（本机分别为 37/43，恰好独立）。
- 前端 Dashboard 已有 `storageCards[]`（label/subtitle/total/used/free）、设备订阅（拔出自动消失）、`formatBytes`/`getUsagePercent`、`onNavigate`（`loadPath` 可跳转任意路径/挂载点/虚拟路径）。
- 卡片样式体系（`dashboard-card`/`usage-bar`/`usage-text`）与 M3 token 全部现成。

### 7.3 方案

**后端（约 3 行）**：`get-storage-usages` 每条结果加 `dev: string`（同一 `stat` 调用即可，无额外开销）。

**前端（Dashboard 约 120–180 行 + CSS 约 40 行）**：

1. `StorageCard` 增 `path` 与 `isHome`、`separateFs`（dev 与根比较）；根/主页/设备目标照旧组装。
2. 渲染：单一 `dashboard-card storage-card`（标题换通用「存储」），内部子区域为 M3 tonal tile：
   - 布局：`grid-template-columns: repeat(auto-fit, minmax(~200px, 1fr))`，全屏宽时 3 个横排（系统+主页+1 设备），多设备自动换行；
   - 为保证横向三区，**存储卡片跨全宽**（`grid-column: 1 / -1`），其余卡片布局不变；
   - 子区域内容：图标（系统 `hard_drive`/主页 `home`/设备沿用 `getDeviceTitle`+`getDiskIcon`）+ 标签 + 用量条 + used/total +（设备）挂载点副标题；
   - 子区域为可点击 tile：`onClick → onNavigate(path)`（系统 → `/`，主页 → home，设备 → 挂载点），M3 状态层（hover 背景）与现有 `.recent-item` 交互一致；
   - 主页非独立分区（`!separateFs`）：隐藏用量条与数字，仅保留图标+标签+点击跳转。
3. 空态/加载占位保留；设备拔出子区域自动消失（现有订阅不动）。

**i18n**：新增 `dashboard.storage`（区域标题，×12 语言）；子区域标签复用 `dashboard.system_storage` / `dashboard.home_storage` / 设备名，无新增。

### 7.4 决策点（需确认）

1. **主页非独立分区时子区域形态**：按字面「隐藏占用信息」→ 子区域保留（图标+标签，可点击跳主页，无用量条）；备选：整个子区域隐藏。**推荐前者**。
2. **存储卡片跨全宽**（`grid-column: 1 / -1`）：不跨全宽则窄屏时三个子区域会挤成竖排，达不到「横向三区」效果。**推荐跨全宽**。
3. **子区域最小宽度**：建议 200px（auto-fit 自然列数；全屏宽时恰为一行三区，更多设备换行）。

### 7.5 风险与结论

- **无破坏面**：仅 Dashboard 渲染重构 + 后端加一个字段；`getStorageUsages` 无其他消费者；设备订阅/热插拔逻辑不动。
- 点击跳转复用 `onNavigate → loadPath`，设备挂载点与虚拟路径均支持；被跳转路径不存在时后端权限回退已兜底。
- 结论：**可行，工作量小（约 200 行 + 1 个 i18n 键 ×12）**；唯一交互设计差异点是 7.4 第 1 项，确认后即可实施。

### 7.6 实施记录（2026-08-28，已按用户最终决策实施）

- 决策：主页非独立分区时**只隐藏占用信息**（保留子区域作导航入口）；顺序固定 系统(/) → 主页 → 设备（识别到追加尾部）；子区域为**列表形式**（M3 列表项），宽度恒定略小于卡片内宽；存储卡片为普通网格单元格（与固定项/最近文件同级），不跨全宽。
- 后端：`get-storage-usages` 每条结果附带 `dev`（`stat({bigint:true}).dev` 字符串），用于判断 /home 是否独立分区。
- 前端：单一「存储」卡（标题新键 `dashboard.storage` ×12）内含 `storage-list`；子区域 = 图标 + 标签 + 用量条 + used/total（设备含挂载点副标题），hover/焦点 M3 状态层，点击 `onNavigate` 跳转（/、home、挂载点），键盘可操作；主页同文件系统时隐藏用量条与数字。
- **改版（追加）**：取消 st_dev 同分区检测；新增设置项 `settings.showHomeStorageUsage`（「显示主页存储占用」，默认关闭），经 `useLocalStorage` 持久化 + 跨窗口同步，App → ExplorerTab → Dashboard 透传；开启时主页子区域显示占用，关闭时仅保留导航条目；系统与设备子区域不受该设置影响。
- 验证：`npx tsc -b`、`npm run lint`、`npm run build` 全部通过。

### 7.7 实施记录（2026-08-30，设备判定扩展 + 轮询兜底，已实施）

- 需求缺口：v0.11.8 实现的设备子区域只收集「外接标志」设备（`hotplug/rm/tran`），手动挂载的内部分区（Windows 分区等）永远被排除；且手动 `mount` 命令不触发 UDisks2 接口增删事件，有 watcher 时仪表盘感知不到。
- 收集规则改为「全部已挂载块设备分区 − 系统挂载点排除」：排除 `SYSTEM_MOUNTPOINTS`（`/`、`/home`、`/boot`、`/efi`、`/usr`、`/var`、`/tmp`、`/opt`、`/srv`、`/etc`，含子路径前缀）与 loop 设备；不再依赖外接标志。
- 设备图标按 `getDeviceIcon` 区分；标签单行化（`label · fstype · size`），弃用多行 `getDeviceTitle`。
- 刷新：仪表盘存储区常开 5s 轮询（`refreshing` 并发去重），与 UDisks2 / GVfs 事件订阅并存；手动挂载最长 5 秒内出现。
- 仪表盘背景右键菜单：仅「刷新」一项（复用 `context_menu.refresh`，12 语言已存在，无新增 i18n）——`onContextMenu` 记录坐标 → `ContextMenu` 组件；`refresh` 经 `refreshRef` 暴露给菜单 action（不重建订阅）；与固定按钮菜单互斥；仪表盘页此前无背景右键菜单（ExplorerTab 背景菜单只挂文件列表分支），无冲突。
- 验证：`npx tsc -b`、`npm run lint`、`npm run build` 全部通过；设备插拔/手动挂载行为建议实机验证。

---

## 八、设备弹出误报修复（分区已卸载仍提示需先卸载，2026-08-30，已实施）

### 8.1 根因

`system:eject-device` 弹出前预检挂载表，读的是 `getMountMap()`——带 30 秒 TTL 缓存（`electron/shared.ts`，供文件列表挂载富化复用）。卸载全部分区后立即点弹出，缓存可能仍是卸载前的旧表 → 随机误判 `PARTITIONS_MOUNTED`；是否复现取决于「最后一次缓存填充」与「卸载」的时序，故不可复现。附带缺陷：挂载/卸载成功后缓存不失效，文件列表挂载徽标最长 30 秒陈旧；`startsWith` 前缀匹配理论上误配 `/dev/sdab` 与 `/dev/sda`。

### 8.2 方案与实施

- `getMountMap(force = false)`：force 为 true 跳过缓存直读 `/proc/mounts`；新增 `invalidateMountMapCache()` 清空缓存。
- eject 预检改用 `getMountMap(true)`；分区归属匹配收紧为「后缀 `p?`+数字」正则（`sda1`/`nvme0n1p1`/`mmcblk0p1` 均覆盖，磁盘整体挂载不算分区）。
- `mount-device` / `unmount-device` / `eject-device` 成功后调用 `invalidateMountMapCache()`。
- 兜底：`udisksctl power-off` 失败且 stderr 命中 `mount|busy` 时同样归类 `PARTITIONS_MOUNTED`（覆盖预检与断电之间的竞态）。
- 破坏面：前端零改动；其余 `getMountMap` 调用者（fs.ts 挂载富化、get-mount-map IPC）行为不变（默认仍走缓存）。
- 验证：`npx tsc -b`、`npm run lint`、`npm run build` 全部通过；双分区 U 盘卸载后立即弹出的场景建议实机复测。

---

## 九、文件预览功能（设置开关 + 右侧挤压式预览区，2026-09-01，仅评估，未实施）

### 9.1 需求

- 设置**一级页面**（设置主对话框）加开关，开启后生效。
- 开启时：用户**单选**一个可预览文件 → 文件浏览区右侧「挤压」出一个预览区；拖动预览区与文件区的边界可调整比例。
- 预览区与文件区一起受内置终端挤压（终端打开时两者同时变矮）。
- 预览区顶端与文件区平齐，不触碰地址栏及上方组件。
- 重新选中不可预览文件 / 取消选择 → 预览区隐藏。
- 预览区出现时解析文件并显示：图片、文本、视频三类。

### 9.2 现状（可复用资产）

| 资产 | 位置 | 作用 |
| --- | --- | --- |
| 布局骨架 | `src/index.css:131-155` + `App.tsx:974-1070` | `.app-shell`（横 flex：Sidebar + `.main-content`）→ `.main-content`（竖 flex：TabBar → `.content-area`(flex:1) → TerminalPanel）。**终端在 content-area 下方，天然挤压整个内容区**——预览区只要放在 content-area 内（ExplorerTab 内），就自动满足「与文件区一起被终端挤压」 |
| ExplorerTab 文件区 | `ExplorerTab.tsx:1242-1244` | 文件列表容器 `<div style="flex:1; overflow:hidden; position:relative" data-drop-target="filelist">`，内含 FileList；顶部是 Omnibar 顶栏（`flexShrink:0`），底部是 StatusBar。预览区只需把这个容器改成横 flex（文件列表 + 分隔条 + 预览面板），顶端即与文件区平齐、不碰地址栏 |
| react-window 自适应 | `FileList.tsx:459` AutoSizer | 容器宽度变化时 AutoSizer（ResizeObserver）自动重测并重渲染列表——**挤压式改宽不触发任何数据重取**，无重排风险（旧「必须 absolute 覆盖」约束只针对旧 overlay 方案，本方案不适用） |
| 单选状态 | `ExplorerTab.tsx:665` `selectedFiles: Set<string>` | `size === 1` 即单选；在 `files` 列表中按 path 反查条目可得 mime/是否目录/大小；搜索模式选中项同样是真实路径，可直接预览 |
| MIME | `IFile.mime`（`src/types/files.ts`） | 图片 `image/*`、文本 `text/*` 等直接可判；视频 `video/*` |
| 文本读取 | `fs:read-file` IPC（`electron/handlers/fs.ts:1008`） | utf-8 读全文返回；**无大小上限**（需加 stat 前置 + 上限，防大文件卡渲染进程） |
| 图片/视频服务协议 | `media://`（`electron/main.ts:292-302`） | **不能直接复用**：先走 `getThumbnail(path, 256)`——图片必然返回 ≤256px 缩略图（非原图）；视频虽因 getThumbnail 只认 `image/*` 而回落原文件，但无 Range 处理（见 9.4 风险 1）。需新协议或加 `?original` 参数 |
| 拖动调整比例先例 | `TerminalPanel`（拖动标题栏调高度，Pointer Capture） | 预览区分隔条拖动（横向）可完全照搬该 Pointer Capture 模式 |
| 设置开关先例 | `SettingsDialog.tsx` 行为区（`marqueeEnabled`/`showHomeStorageUsage` 行，`Switch` 组件） | 新开关 `settings.file_preview` 照抄：App `useLocalStorage` 持久化 + 跨窗口同步 + 透传 ExplorerTab |
| 主题/缩放适配 | 全部 CSS 变量 + uiScale 整页缩放 | 预览区用 M3 变量 + 现有字体栈即可，无额外适配 |

### 9.3 方案

**后端（约 80–110 行）**

1. **新增 `preview://` 协议**（`electron/main.ts`，`protocol.handle`，privileges 与 `media` 相同）：
   - 路径校验：绝对路径 + `stat` 确认为普通文件（防目录/设备文件）；
   - **Range 支持**：解析 `Range: bytes=a-b` 单段头 → `fs.createReadStream`/buffer 切片 → 返回 `206 Partial Content` + `Content-Range`/`Accept-Ranges: bytes`；无 Range 时返回 200 全量。图片/文本直接 `<img>`/fetch 走 200 路径；视频 seek 依赖 206；
   - 大文件流式返回（`createReadStream` → `Readable.toWeb` 作 Response body，需在实现首日验证 Electron `protocol.handle` 对 web stream 的支持；若受限则退化为「≤ N MB 缓冲 + 超限不预览」）。
2. **文本读取加护栏**：新增 `fs:read-preview-text`（或给 `fs:read-file` 加可选 maxBytes）：先 `stat` 判大小，超过 512 KiB 返回结构化错误码 `TOO_LARGE`，前端显示「文件过大，无法预览」；其余返回 utf-8 文本。避免渲染进程被数百 MB 日志/JSON 卡死。

**前端（约 300–380 行 + CSS + i18n）**

3. **设置开关**：`SettingsDialog` 行为区新增「文件预览」行（Switch，`settings.file_preview`，默认**开或关待定**——建议默认开）；App 侧 `useLocalStorage<boolean>` 持久化 + 跨窗口 storage 同步，经 ExplorerTab 透传。
4. **ExplorerTab 文件区重构**：把 `data-drop-target="filelist"` 容器改为横 flex：
   `[文件列表容器(flex:1, min-width:0)] + [分隔条(6px, cursor: col-resize)] + [FilePreviewPanel(宽度百分比)]`。
   - 面板宽度 `settings.previewWidth`（百分比，默认 35%，拖动范围建议 20%–60%，Pointer Capture 拖动分隔条，模式照抄 TerminalPanel；是否持久化建议**持久化**，与 uiScale 同款 storage 同步）。
   - 拖动时给 FileList 加 `pointer-events: none` 防 react-window 悬停/框选干扰（TerminalPanel 同款处理可参考）。
5. **显示条件**（单一判定函数）：开关开 && `selectedFiles.size === 1` && 条目非目录 && mime 可预览（`image/*`、`video/*`、`text/*` + 少量代码类扩展名/`application/json` 白名单）；否则面板卸载隐藏（含多选、取消选择、切标签、导航走、进回收站/仪表盘视图）。**不可预览文件不显示占位**（按需求直接隐藏）。
6. **FilePreviewPanel 组件**（按 mime 分支渲染）：
   - 图片：`<img src="preview://<path>">`（object-fit: contain，居中，深色底）；
   - 视频：`<video src="preview://<path>" controls preload="metadata">`；**容器限制**：Chromium 只可靠播 MP4(H.264/AAC)、WebM(VP8/VP9/AV1)、OGG——`video/x-matroska`（.mkv）等不保证可播，遇到非白名单容器显示「格式不支持」提示；
   - 文本/代码：`fs:read-preview-text` → `<pre>`（等宽、自动换行、滚动）；`TOO_LARGE`/读取失败显示对应提示；
   - 顶部小标题栏：文件名 + 大小（复用 `formatBytes`）；解析失败/加载中状态位。
7. **回收站/搜索行为（建议允许，需确认）**：回收站条目 `path` 即 Trash/files 内真实文件，可直接预览（只读）；搜索选中项是真实路径，天然可用。
8. **i18n**（×12 语言，约 4–5 键）：`settings.file_preview`、`preview.too_large`、`preview.load_failed`、`preview.unsupported_format`、（可选）`preview.empty`。

### 9.4 边界与风险

1. **Range/流式是唯一真正的技术风险点（中低）**：`protocol.handle` + `net.fetch(file://)` 对 Range 头的处理需要实测——不保证透传 206。预案：手动解析 Range + 切片 buffer（≤50 MB 视频完全可行）；大视频流式需验证 `Readable.toWeb` 作为 Response body，验证失败则「超上限提示不预览」。**首日做一个 20 行原型验证即可闭环此风险。**
2. **media:// 与预览的协议分离**：绝不能复用 `media://` 预览图片（会拿到 256px 缩略图）。新 `preview://` 协议与现有 `media://` 并存，零改动现有缩略图链路。
3. **布局侵入面可控**：只改 ExplorerTab 文件列表容器一处结构（横 flex），StatusBar/Omnibar/搜索条不动；`data-drop-target="filelist"` 语义保留在文件列表子容器上，预览面板是兄弟节点——拖文件落到预览区不会误触文件区落点。
4. **react-window 无风险**：AutoSizer 自动响应宽度变化；唯一注意是拖动过程中避免高频全量重排（节流 rAF 更新宽度即可）。
5. **多选/目录/仪表盘/回收站空态**：判定函数一次性覆盖（目录、多选、不可预览 mime、虚拟视图一律隐藏），无分支漏网风险。
6. **性能**：预览出现即解析（惰性），切换选中项时重建；对图片/视频只是浏览器惰性解码，无后台任务。

### 9.5 结论

**可行，工作量中等（约 400–500 行 + CSS + 4–5 个 i18n 键 ×12）。**

- 布局目标（挤压式、与终端同挤、顶端平齐）由现有 flex 骨架**天然满足**，零结构性风险；
- 单选状态、MIME、设置开关、Pointer Capture 拖动、AutoSizer 自适应全部现成可抄；
- 唯一需原型验证的是 Electron `protocol.handle` 的 Range/流式行为（9.4 风险 1，有明确预案）；
- 破坏面：仅 ExplorerTab 文件区一处结构改动 + 新增协议，不触碰拖放/操作/终端管线。

**待确认决策点**：① 默认开关状态（建议开）；② 预览宽度是否持久化（建议持久化）；③ 回收站/搜索模式是否允许预览（建议允许）；④ 文本大小上限（建议 512 KiB）；⑤ 多选时隐藏预览（按需求字面理解即隐藏，建议确认）。

### 9.6 实施记录（2026-09-01，已按用户最终决策实施）

- **用户决策**：默认**关**；宽度持久化；回收站允许预览；文本上限 512 KiB；**多选时显示「多个文件无法预览」占位**（非隐藏）。
- **后端**：
  - `electron/main.ts` 新增 `preview://` 协议（privileges 同 `media://`）：绝对路径 + 普通文件校验；`parseRangeHeader` 解析单段 Range（`a-b`/`a-`/`-b`），206 + `Content-Range`/`Accept-Ranges`，无效 416；`createReadStream` + `Readable.toWeb` 流式（大视频不整体缓冲）；`detectMime` 填 Content-Type。
  - `electron/handlers/fs.ts` 新增 `fs:read-preview-text`（`PREVIEW_TEXT_MAX_BYTES = 512 KiB`）：`INVALID_PATH`/`NOT_FILE`/`TOO_LARGE`（附 size）/`READ_FAILED` 结构化错误码；`fs:read-file` 不动。preload 暴露 `readPreviewText`，`electron.d.ts` 补类型。
- **前端**：
  - 新组件 `FilePreviewPanel`（+css）：图片 `<img>`、视频 `<video controls preload="metadata">`（mime 白名单 mp4/webm/ogg/quicktime，扩展名兜底判图片/文本）、文本 `<pre>`（512 KiB 上限 + TOO_LARGE/失败占位）；「多选/不支持格式/加载失败」居中占位；标题栏文件名 + 大小；file.path 作 key 整体重建 + 「渲染期间重置状态」模式清媒体错误/文本态。
  - `ExplorerTab`：文件列表容器改为横 flex 行（文件区 `flex:1, min-width:0` + 分隔条 + 面板）；`data-drop-target="filelist"` 语义保留在文件区子容器；`previewState` 由开关 + 视图 + 选中集推导（隐藏/多选占位/单文件）；分隔条 Pointer Capture 拖动钳制 [20%, 60%]，拖动期间 `--dragging` 类屏蔽兄弟节点指针；拖放/回收站/搜索/框选逻辑零改动。
  - `App`：`settings.filePreview`（默认 false）与 `settings.previewWidth`（默认 35）经 `useLocalStorage` 持久化 + 跨窗口同步，透传 ExplorerTab 与设置对话框。
  - `SettingsDialog` 行为区新增「文件预览」行（Switch，icon `preview`）。
  - i18n 新增 7 键 × 12 语言（`settings.file_preview`、`preview.multiple/too_large/load_failed/unsupported_format/loading/drag_hint`）。
- **验证**：`npm run lint`、`npx tsc -b`、`npx tsc -p electron/tsconfig.json --noEmit` 全部通过；预览协议 Range 行为与流式播放建议实机验证（Electron 43，`protocol.handle` + `Readable.toWeb`）。
- **格式扩展（2026-09-01，追加实施）**：音频（audio 元素 + 扩展名兜底）、MKV（视频容器白名单）、视频/音频扩展名兜底、更多文本扩展名、.ico 图片；PDF 经 pdfjs-dist 惰性加载（动态 import + worker ?url，逐页 canvas + 滚动追加渲染）；归档内容列表经新增 `fs:list-archive`（unzip -Z1 / tar -tf / bsdtar / 7z -slt，上限 5000 截断 + total）；Markdown 经 marked + DOMPurify 消毒。新依赖 pdfjs-dist/marked/dompurify 全部代码分割，主 chunk 不受影响。
- **版本**：升至 `0.11.17`。

---

## 十、e2e 脚本固化（从零重建）可行性报告（2026-09-01，仅评估，未实施）

### 10.1 什么是本项目语境下的「e2e 脚本」

- **定义**：以 `npx electron <脚本>.cjs` 方式运行的**主进程驱动测试脚本**——Electron 自身就是测试框架：启动真实构建产物（`dist/index.html` + 真实 `preload.js` + 全部 IPC handler），用 `webContents.sendInputEvent` 模拟真实鼠标/键盘事件、`executeJavaScript` 读取/操作页面状态、`node:assert` 断言。**不引入 jest/playwright 等外部框架**，这也是本项目「无测试框架」现状下的最优选择（零新依赖）。
- **与单元测试的区别**：覆盖真实链路——React 渲染、preload 桥、IPC handler、主进程协议、CSS/布局交互，而不是孤立函数。
- **本次会话已实际验证的可行性**：调试 PDF 预览时用同一模式写了最小脚本（注册 `preview://` 协议 + 真实 dist 的 pdfjs chunk/worker + fetch 断言 + `executeJavaScript` 取结果），在本机（DISPLAY=:0）跑通并定位了 corsEnabled/URL 形态两个 bug——**该模式可直接固化成正式套件**。

### 10.2 现状

| 项 | 状态 |
| --- | --- |
| 原 12 套 e2e 脚本 | **已丢失**（/tmp/opencode 被系统清理，无法移入） |
| 测试框架 | 无（AGENTS.md 已注明，质量靠 `tsc -b` + ESLint） |
| 本地运行条件 | 本机有 DISPLAY=:0，`npx electron` 可跑（已验证）；无头 CI 需 xvfb |
| 已知坑点文档 | 进度.md 已记录：React 受控输入 prototype setter 技巧、sendInputEvent DIP 坐标、lit-react 事件映射、Dialog 串行化等待 |

### 10.3 从零重建方案

**1. 公共 harness：`scripts/e2e/harness.cjs`（约 150–250 行）**

- `setupApp({ userData })`：隔离运行环境——`app.setPath('userData', tmp)`（不污染真实配置/剪贴板 JSON）、临时 HOME 或 matugen 探测规避；**不调用 `requestSingleInstanceLock`**（测试进程互不冲突）；注册全部 handler（`registerFsHandlers`/`registerSystemHandlers`/`registerWindowHandlers`/`registerThemeHandlers`/`registerPickerHandlers`/`initJobHandlers`）与 `media://`/`preview://` 协议（复用 main.ts 同一段注册代码，防止测试与实现漂移）。
- `createTestWindow({ url?, preload? })`：窗口工厂，默认加载 `dist/index.html` + 真实 preload（file:// 直载，验证构建产物本身）。
- `click(x, y, opts)` / `key(...)`：`sendInputEvent` 封装；**zoom 感知坐标换算**（读 `webContents.getZoomFactor()`，DIP 坐标 × factor）。
- `js(win, expr)` / `waitFor(win, expr, timeout)`：`executeJavaScript` + 轮询等待（`executeJavaScript` 会吞异常，返回结构需统一 `{ ok, value, error }`）。
- React 受控输入 setter：`setReactInput(win, selector, value)`——用 `Object.getOwnPropertyDescriptor(HTMLInputElement.prototype, 'value').set` 写入后派发 `input` 事件（React onChange 依赖原生 setter 触发）。
- lit-react 事件辅助：md-* 组件交互以**宿主元素**为目标（click 挂宿主、`md-outlined-select` 用 `input` 事件等）。
- Dialog 串行化等待：`waitDialogClosed()`——对话框关闭动画 250ms（DIALOG_GAP_MS），连续操作间 `await sleep(300)`。
- `run(name, fn)`：统一打印 `✓/✗ NAME`、超时保护、`process.exitCode` 汇总。

**2. 测试套件（建议 8–12 套，每套 50–150 行，全部针对真实 dist 构建）**

| # | 测试 | 覆盖坑点/功能 |
| --- | --- | --- |
| 1 | 文件列表渲染与目录导航 | 基础：真实产物加载、列表出现、点击进入/面包屑返回 |
| 2 | React 受控输入（重命名输入框） | prototype setter 技巧 |
| 3 | 右键菜单开/关与定位 | sendInputEvent 坐标、菜单关闭竞态（v0.11.14 曾回归） |
| 4 | 设置对话框：语言切换确定时应用 | Dialog 串行化等待、lit-react 事件映射 |
| 5 | 主题颜色预览同步（preset → broadcast → 双窗口 CSS 一致） | 跨窗口 storage/预览广播 |
| 6 | 文件选择器（picker 窗口、resolvePicker 回传、直接关窗 null） | picker 链路（历史 e2e 曾覆盖） |
| 7 | 界面缩放：uiScale → zoom factor → 坐标换算 | zoom 感知坐标 |
| 8 | 剪贴板跨窗口（clipboard.json 持久化） | 多窗口共享剪贴板 |
| 9 | `media://` 缩略图 + `preview://` Range/206 | 本次 PDF 调试模式直接固化（含 416 与 localhost 形态回归） |
| 10 | 归档列表 `fs:list-archive`（zip 用例） | 结构化 IPC 断言 |
| 11 | 批量重命名对话框预览/确认 | 对话框内容断言 + 输入 setter |
| 12 | 终端 PTY（spawn/write/onData/exit，临时目录） | node-pty 在 Electron 主进程可用 |

**3. 入口与文档**

- `package.json` 加 `"e2e": "npm run build && for f in scripts/e2e/*.test.cjs; do npx electron $f || exit 1; done"`（先 build 保证测的是最新产物；单独跑可用 `npx electron scripts/e2e/xxx.test.cjs`）。
- README/AGENTS.md 记录：运行方式、需要 DISPLAY（无头用 `xvfb-run`）、坑点速查（受控输入 setter、DIP 坐标、lit 事件、Dialog 等待）。

### 10.4 风险与边界

- **显示依赖**：本机 DISPLAY=:0 可跑；CI 需 xvfb-run 一行包装，无新增依赖。
- **环境隔离**：userData 指向临时目录；主进程单例锁必须在 harness 中绕过（复制 main.ts 注册段而非 import 整个 main.ts——main.ts 顶层有锁逻辑与 createWindow 副作用）。
- **稳定性**：断言一律走 waitFor 轮询（React 渲染异步）；全局超时 30–60s；测试间串行执行，不并行开窗口。
- **不覆盖**：UDisks2 热插拔（需真实设备）、外部应用拖放（LocalSend）、Wayland 合成器行为——这些仍靠实机手测。
- **维护**：harness 与 main.ts 的 handler/协议注册段需注释互相指向，防止漂移。

### 10.5 结论

**可行，工作量中等偏大（harness 150–250 行 + 12 套约 800–1200 行 + 文档）**。模式已在本会话用最小脚本实证（真实产物 + 协议注册 + executeJavaScript 断言），风险低；主要成本在逐套编写与实机跑通调试。建议分批落地：先 harness + 套件 1/9/10（基础设施验证）→ 再交互类 2/3/4/5 → 最后 6/7/8/11/12。

### 10.6 实施记录（2026-09-01，已实施）

- **harness**（`scripts/e2e/harness.cjs`，约 500 行）：`setupApp`（临时 userData 隔离 + 全部 handler 注册——dist-electron 编译产物模块 + main.ts 顶层 handler 副本互指注释 + media/preview 协议副本 + `parseRangeHeader` 副本）、`createTestWindow`（真实 preload、启动路径解析、picker 变体）、`js/waitFor/clickEl/rightClickEl/doubleClickEl/clickAt/key/hotkey`（zoom 感知坐标换算）、`setReactInput`（prototype setter + md-* shadow 穿透）、`scrollIntoView`、`waitDialogAnim`、`makeFileTree/makeZip/minimalPdfBytes/PNG_1PX_BASE64` fixtures、`run/finish` 汇总退出码、**120s 全局看门狗**（防 Wayland 启动竞态等静默挂起）。
- **12 套用例全部实机跑通**：01 列表导航、02 行内重命名、03 右键菜单、04 设置对话框、05 主题跨窗口同步、06 选择器、07 界面缩放、08 剪贴板跨窗口、09 协议（media/preview Range/416）、10 归档列表、11 批量重命名、12 PTY。
- **实施中实证/固化的新坑点**（已写入 AGENTS.md）：双击 = 两次独立 click（手动检测）；缩放因子**会话级共享**（任一窗口 set 后其他窗口读到同值）；同窗口直接 `localStorage.setItem` 不触发 storage 事件（跨窗口同步必须另一窗口写）；`picker:resolve` 立即关窗致其自身 IPC 响应丢失（测试必须 fire-and-forget）；Dialog 250ms 串行化延迟下须断言 `md-dialog.open === true`；md-* 文本域输入须穿透 shadow root；菜单/按钮文案按中英文双匹配。
- **入口**：`package.json` 加 `"e2e": "npm run build && for f in scripts/e2e/*.test.cjs; do npx electron \"$f\" || exit 1; done"`；README 双语新增 Testing/测试 节；AGENTS.md 增补 e2e 章节。
- **验证**：全量 `for f in scripts/e2e/*.test.cjs; do npx electron $f; done` 一次跑完 12/12 ✓（本机 DISPLAY=:0）。

---

## 十一、批量重命名菜单精简与对话框顺序修正可行性评估（2026-09-01，仅评估，未实施）

### 11.1 需求

1. 多选时右键菜单只显示「批量重命名...」，去掉单项「重命名」；
2. 批量重命名对话框里的文件顺序 = 当前显示顺序模式下的视觉顺序（列表：从上到下；网格：行内从左到右、行间从上到下），而非点击顺序。

### 11.2 现状（根因已定位）

- **菜单项**：`App.tsx` 文件菜单构建器（~789 行）无条件添加「重命名」（作用于右键命中的 `item`），`selectedFiles.length >= 2` 时再追加「批量重命名...」——多选时两者并存。
- **顺序根因**：`ExplorerTab.handleFileContextMenu`（~1081 行）构造选中数组用
  `filesForFileListRef.current.filter(f => currentSelection.has(f.path))`——`filesForFileListRef` 是**原始目录列表** `files`，而 FileList 实际渲染的是 `sortedFiles`（`sortFiles(files, {sortBy, sortOrder, groupingEnabled, showHiddenFiles})`）。选中集 `Set` 的插入顺序 = 点击顺序，与显示顺序无关 → 对话框里显示的是原始列表序（且与排序/分组设置脱节）。
- `selected` 数组还流向复制/剪切/删除等批量操作——这些是集合语义，顺序无关，改动无副作用。
- 搜索模式本地菜单不含批量重命名，不受影响。

### 11.3 方案

1. **菜单**：`App.tsx` 中「重命名」项改为仅 `selectedFiles.length < 2` 时添加（多选只剩「批量重命名...」）。右键未选中文件时 ExplorerTab 会先把选中集重置为单文件（现有行为），该项仍正常出现。
2. **顺序**：`ExplorerTab` 增加 `sortedFilesForFileListRef`（与 `filesForFileListRef` 同款渲染期同步 ref），`handleFileContextMenu` 的 `selected` 改由 **sortedFiles** 过滤——与 FileList 渲染完全同序。列表视图 = 从上到下；网格视图 = 行主序（左→右、上→下）；开启分组时同样与视觉分组一致。`setBatchRenameFiles(selectedFiles)` 与对话框渲染均保序，无需后端改动。

### 11.4 风险与结论

- **改动约 10–15 行**（ref 一行 + filter 源切换 + 菜单条件），无后端改动、无 i18n 新增；`selected` 其余消费方为集合语义不受影响。
- **结论：可行，工作量极小，风险近零。**

### 11.5 实施记录（2026-09-01，已实施）

- **菜单**：`App.tsx` 文件菜单「重命名」项改为仅 `selectedFiles.length < 2` 时添加——多选右键只剩「批量重命名...」；右键未选中文件时选择集已先重置为单文件（既有行为），单项重命名不受影响。
- **顺序**：`ExplorerTab` 新增 `sortedFilesForFileListRef`（与 FileList 渲染同源的渲染期同步 ref），`handleFileContextMenu` 的 `selected` 改由 sortedFiles 过滤——批量重命名对话框的文件顺序 = 当前排序/分组模式的视觉顺序（列表从上到下、网格行主序）；其余批量操作（复制/剪切/删除）为集合语义不受影响。
- **e2e 11 强化**：多选菜单断言（只含批量重命名、无单项重命名）+ 按大小升序断言对话框顺序 b→c→a（a=100B/b=1B/c=50B，与字母序不同）；过程中发现跑马灯把 `.batch-individual-name` 文本重复 3 份——改用 marquee 关闭时的 `.batch-individual-old` 纯净 span 断言（测试内先关 marquee 设置再 reload）。

---

## 十二、通信层抽象与选择器过滤器（模式 + 文件类型筛选）可行性报告（2026-09-01，仅评估，未实施）

### 12.1 需求

1. 明确抽象通信层：内部/外部程序可调用；`picker:open` 可传入选择模式（目录/文件/全部）与**文件类型筛选（后缀，可多项）**；
2. 选择器底部 UI 加类型选择框：未声明 → 只有「所有文件」；声明则显示声明的类型（如 .docx →「Microsoft Office 2007-365 Word文档」）与「所有文件」；多项时多个可选类型 + 所有文件；
3. 选择框样式与设置里语言选择的选择器（`OutlinedSelect`）一致。

### 12.2 现状（可复用资产）

| 资产 | 位置 | 作用 |
| --- | --- | --- |
| Picker IPC 扩展点 | `electron/handlers/picker.ts` | `PickerConfig { mode }` + `VALID_MODES` 白名单 + 按窗口配置（`picker:get-config`）——加 `filters` 字段即扩展；`docs/picker-api.md` 已有接口文档与变更记录 |
| MIME 显示名体系 | `src/utils/mimeTypes.ts` `mimeDescriptionMap`（76+ 键 × 12 语言）+ `getFileTypeDescription` | **用户示例「Microsoft Office 2007-365 Word文档」应走这套 i18n 描述**（现 zh 文案为「DOCX 文档」，需扩充）；含 Office/OpenDocument/图片/音视频/归档全覆盖 |
| 扩展名 → MIME | `electron/mimeMap.ts`（约 436 行） | 前端按扩展名匹配条目：可用 `IFile.mime`（列表已带，零额外 IPC）或后缀比对 |
| 选择器样式先例 | `SettingsDialog.tsx` 语言选择 = `OutlinedSelect` + `SelectOption`（slot headline） | 底部类型选择框直接同款 |
| 可选性判定 | `FilePicker.tsx` `isSelectable` | 过滤器的落点：`mode` 判定 + filter 判定合并 |
| 外部调用先例 | dbus-next（UDisks2 监听已在用） | 外部程序接入的标准化路线（见 12.4） |

### 12.3 方案（内部 API 扩展）

1. **类型**：`PickerFilter = { id: string; label?: string; extensions: string[]; mimes?: string[] }`；`openPicker(options: { mode; filters?: PickerFilter[]; initialPath?: string; defaultFilterId?: string })`。`filters` 缺失/空 = 仅「所有文件」。
2. **主进程校验**（白名单策略，与 VALID_MODES 同款）：filters ≤ 20 项；每个 filter 的 `extensions` 为 `'.ext'` 形态（正则 `^\.[A-Za-z0-9_+-]+$`）、`mimes` 为合法 mime 形态；非法字段忽略或整体拒绝（按现有「未知字段忽略、已知字段强校验」约定）。
3. **UI**：`picker-footer` 左侧加 `OutlinedSelect`（与语言选择同款）：选项 = 「所有文件」（**常驻**）+ 每个 filter 一项；默认选中 `defaultFilterId ?? 所有文件`；`filters` 未声明时不渲染选择框。
4. **过滤语义**：当前 filter = 所有文件 → 仅受 mode 约束（现状）；指定 filter → 条目可选中还需命中 filter（`IFile.mime` 匹配 mimes、后缀匹配 extensions，或关系）。**目录永远不受文件 filter 影响**（folder/items 模式下目录始终可选中；file/files 模式目录本不可选）。目录导航不受影响（过滤只约束条目可选中性，不隐藏）。
5. **显示名生成**：filter 缺省 label 时——extensions 经 mimeMap 转 mime → `mimeDescriptionMap` 取 i18n 名（未知扩展名显示 `*.ext`）；调用方显式给 label 时原样显示（第三方自管语言）。「所有文件」新增 i18n 键 `picker.all_files` × 12。
6. **filter 变化时的选中集**：当前选中项不再命中 filter → 清除其选中（与 mode 语义一致，可确认集始终合法）。

### 12.4 外部程序的标准化路线（对用户想法的补全）

- **优先：xdg-desktop-portal FileChooser 接口**（`org.freedesktop.portal.FileChooser`）。这是 Linux 生态「外部程序请求文件对话框」的**既有标准**——GTK/Qt 应用经 portal 请求时由实现者响应；portal 协议原生支持 `filters`（`{id,label}` + `mime_types`）与多选/目录模式，与用户需求一一对应。dbus-next 已是依赖，实现约 300–500 行 + `portal.conf` 注册（`UseIn=org.freedesktop.impl.portal.FileChooser`）。做成后**任何外部程序零适配**即可用本选择器。
- 备选：自定义 D-Bus 服务（`com.hoshineko.FileManager.Picker.OpenPicker(filters) → paths`）——私有协议、简单，但不标准。
- 内部层：保持 `picker:open` IPC 为唯一事实源，portal 后端把 portal 请求翻译成同一个 PickerConfig → 复用同一窗口工厂，**一条实现两条入口**。

### 12.5 用户想法中的缺陷与标准化建议（补全）

1. **显示名不要硬编码**：用户示例的「Microsoft Office 2007-365 Word文档」应来自 mime 描述体系（mimeTypes.ts 的 mime.docx，现为「DOCX 文档」，可扩充文案），第三方 filter 可自带 label 兜底。硬编码字符串会绕过 12 语言 i18n。
2. **「所有文件」永远存在**：无声明时只有它；有声明时也必须保留（用户要能撤销过滤），建议固定在第一项。
3. **默认过滤器**：`defaultFilterId` 由调用方声明；不持久化记忆（减少跨会话状态面，GTK 的记忆行为可选做）。
4. **API 版本化与错误语义**：`picker:open` 现在对非法 mode 抛异常（rejected promise）——建议统一为结构化返回或至少文档化异常约定（与 fs 系的结构化错误码风格统一）；未知字段继续忽略以向前兼容。
5. **类型共享**：`PickerFilter` 放 `src/types/`（electron.d.ts 引用），picker.ts/preload/docs 同源，避免三处漂移。
6. **可判定性收敛**：把 `isSelectable` 收敛为纯函数 `isSelectable(file, config, activeFilter)`——mode 与 filter 一处判定，e2e 可直接覆盖。
7. **文档化通信层**：picker-api.md 升级为「内部通信 API」文档（filters、错误约定、版本化）；DnD 的 claim 仲裁协议同样层级文档化——这是「明确抽象通信层」的落地形态。
8. **多选语义**：filter 对 `files`/`items` 多选同样生效（框选只框可选条目）。

### 12.6 风险与结论

- 风险低：过滤匹配用列表已有 `IFile.mime`，零额外 IPC；底部下拉是纯前端；校验在既有白名单模式内。需注意 filter 变化时清理选中集、框选（`allowBoxFromItems`）与过滤的交互——用 e2e 覆盖。
- 工作量：**内部 API 扩展约 250–350 行**（类型 30 + 校验 40 + 描述生成 30 + 下拉/过滤 UI 120 + i18n + 文档 + e2e 用例 ~80）；**portal FileChooser 后端约 300–500 行**（远期、独立分期）。
- **结论：可行。建议分两期——一期：picker 内部 API 扩展（filters/mode/initialPath）+ 底部下拉 + i18n + e2e；二期：xdg-desktop-portal FileChooser 后端（外部程序标准入口）。**

### 12.6 实施记录（2026-09-01，一期已实施）

- **类型层**：`src/types/picker.ts`（PickerFilter / PickerConfig）三端同源；electron.d.ts 的 openPicker/getPickerConfig 改引用完整 PickerConfig。
- **主进程**（`electron/handlers/picker.ts`）：`sanitizeOptions` 白名单校验（mode 抛错、initialPath 绝对路径、filters ≤ 20 / extensions ≤ 30 `.ext` 形态统一小写 / mimes ≤ 30 合法形态）；缺省 label 的 filter 用 EXT_TO_MIME 解析首扩展名 mime → `resolvedMime`；`pendingPickers` 按 webContents.id 隔离（并发独立，原有设计，文档化 + e2e 覆盖）。
- **前端**（`FilePicker.tsx`）：config 状态升级为 PickerConfig 对象；`isSelectableFor(file, filterId, cfg)` 纯函数收敛 mode + 过滤器判定（目录不受约束）；`matchesFilter`（后缀或 MIME 或关系）；`handleFilterChange` 切换过滤并清除失效选中；底部 `OutlinedSelect`（所有文件常驻 + 各类型，`filterLabel` = 显式 label → mime 描述 → `*.ext`）；init 支持 `initialPath`（stat 校验无效回退家目录）与 `defaultFilterId`。
- **描述体系**：`mimeTypes.ts` 导出 `getMimeDisplayName`；docx/xlsx/pptx/doc/xls/ppt 文案 × 12 扩充为「Microsoft Office …」描述性名称；新增 `picker.all_files` × 12。
- **e2e**：`13-picker-filters.test.cjs`（过滤器 UI/可选性/切换清除/并发独立全链路）；harness 新增 `selectOption`（md-select `select()` 静默，须补派发 input 事件——坑点记入 AGENTS.md）。
- **文档**：docs/picker-api.md 全面更新（配置表、过滤器语义、并发语义、校验规则、变更记录）。
- **二期（已实施）**：xdg-desktop-portal FileChooser D-Bus 后端——`electron/handlers/portalFileChooser.ts` 注册 `org.freedesktop.impl.portal.desktop.hoshineko` 并实现 `org.freedesktop.impl.portal.FileChooser`（dbus-next 类式 Interface + configureMembers；OpenFile → PickerConfig 翻译：`directory/multiple` → 模式、`filters a(sa(us))` → 名称作 filter id、`current_filter (sa(us))` 按名匹配；服务端须 unwrap `dbus.Variant`；Request 接口在 portal 传入的 handle 路径导出，Close → 关窗取消；结果 Response 信号 `ua{sv}` 回传 `file://` URI）；SaveFile/SaveFiles 返回 NotSupported（v1 限制）；`--portal` 启动参数仅服务不建主窗口；packaging 提供 `.portal` 与 D-Bus `.service` 文件，docs/portal-filechooser.md 文档化安装与验证；e2e 14 以 dbus-next 客户端模拟 portal 全链路（含 Close 取消路径）。
- **UI 迭代（追加）**：类型下拉常驻（无声明仅「所有文件」一项），位于路径提示右侧、宽度自适应内容（覆盖 md-select 默认 210px 最小宽）。

---

## 十三、自定义 M3 标题栏（frameless + 窗口控制 + 标题规则）可行性报告（2026-09-01，仅评估，未实施）

### 13.1 需求

1. **彻底隐藏 Electron 原生标题栏/顶栏**（默认隐藏，屏蔽 Alt 唤出顶栏），保留 F12 开发人员工具；
2. **自制 M3 标题栏**：右侧 最小化 / 最大化（取消最大化）/ 关闭 三按钮；左侧「v」菜单按钮（展开菜单：最大化 / 最小化 / 退出）；v 按钮右侧显示窗口标题（超长截断尾部 `...`，开启滚动文本则滚动显示）；
3. 标题栏**可选**：设置项「跟随系统 / 开 / 关」（逻辑参考明暗主题开关）——跟随系统时：平铺 WM（niri/hyprland/i3/sway 等）→ 隐藏；常规 DE（xfce/gnome/kde_plasma）→ 显示；fallback = 显示。手动模式持久化；
4. **窗口标题规则**：仪表盘 →「Hoshineko Nya~」；回收站 →「回收站」；任意目录 → 目录名；设置项「标题栏显示完整路径」开启时目录显示完整路径；
5. **Electron 窗口标题实时同步**（任务栏等 DE 区域显示用）。

### 13.2 现状（可复用资产）

| 资产 | 位置 | 作用 |
| --- | --- | --- |
| frameless 窗口 | `electron/main.ts` `createWindow`（现 `autoHideMenuBar: true`） | 改 `frame: false` + `-webkit-app-region: drag`（自定义标题栏拖动）；F12 经 `before-input-event` 补回（菜单移除后 Chromium 快捷键失效）；`Menu.setApplicationMenu(null)` 屏蔽 Alt |
| 明暗开关样板 | `App.tsx` `darkMode: boolean \| null` + `ThemeColorDialog` 的 switch+复位交互 | **标题栏三态（null=跟随系统/true=开/false=关）完全照抄**：md-switch 纯展示化 + 外层容器接管 + 检测副标题 |
| DE 检测先例 | `electron/handlers/theme.ts` `detectColorScheme`（execFileAsync 探测链 + 结构化 source 返回） | 新增 `system:detect-window-manager` 同款：`XDG_CURRENT_DESKTOP` / `XDG_SESSION_DESKTOP`（niri/hyprland/sway/i3/qtile/awesome/dwm/bspwm → tiling；gnome/kde/plasma/xfce/cinnamon/mate/budgie → stacking）→ `{ kind: 'tiling' \| 'stacking', source, name }`，fallback stacking |
| 窗口控制 | `electron/handlers/window.ts` | 新增 `window:minimize` / `window:toggle-maximize` / `window:close` / `window:is-maximized`（invoke）+ `maximize`/`unmaximize` 状态事件推送（图标随最大/还原切换） |
| 标题同步 | 页面 `document.title` → BrowserWindow 标题自动同步 | **零额外工作**（选择器已用此机制设置窗口标题，实测生效） |
| 标题数据 | `App.tsx` tabs 状态（`onPathChange` 已回传每标签路径） | 标题 = 活动标签路径派生：`app://dashboard` → 品牌串、`trash://` → `t('nav.trash')`、真实路径 → basename 或完整路径 |
| 滚动文本 | `MarqueeText` 组件（文件列表在用） | 标题超长滚动显示直接复用 |
| 菜单 | `ContextMenu` 组件 | v 按钮展开菜单（最大化/最小化/退出）直接用 |

### 13.3 方案

**后端（约 80–120 行）**
- `createWindow`：`frame: false`；`Menu.setApplicationMenu(null)`（屏蔽 Alt 顶栏）；`before-input-event` 拦截 F12 → `toggleDevTools()`；保留 `resizable`（Linux frameless 边缘 resize 基本可用，niri 下另测）。
- `system:detect-window-manager`：探测链 env（XDG_CURRENT_DESKTOP → XDG_SESSION_DESKTOP）→ 名称归一（含 Hyprland/i3 大小写）→ 白名单归类 tiling/stacking；都查不到 fallback `{ kind: 'stacking', source: 'fallback' }`。
- 窗口控制 IPC：minimize / toggle-maximize / close / is-maximized；主进程 `maximize`/`unmaximize` 事件广播到窗口（preload 订阅）。
- `window:set-title`（可选）：页面标题自动同步已够，但显式 IPC 可防抖动/兜底——评估后倾向直接用 `document.title`。

**前端（约 300–400 行 + CSS + i18n）**
- `TitleBar` 组件（挂在 `.app-shell` 之上成为竖向 wrapper：title-bar + 现有横向布局；隐藏时整块不渲染，内容区自动占满）：
  - 左：v 按钮（`ContextMenu`：最大化/最小化/退出）；右：min/max/close 三个 `IconButton`（max 图标随 `isMaximized` 切 restore 图标）；
  - 中：标题（`MarqueeText` 包裹，超宽自动截断 + 尾部 `...`，`settings.marqueeEnabled` 开启时滚动）；
  - CSS `-webkit-app-region: drag` 于条体、`no-drag` 于按钮/菜单。
- 三态设置：`settings.titleBar: boolean | null`（默认 null=跟随系统，useLocalStorage 持久化 + 跨窗口同步）；`App` 持 `detectedWm`（挂载时 detect 一次）；**effectiveVisible = mode === null ? detected.kind !== 'tiling' : mode**；设置 UI 放外观区（md-switch + 「跟随系统」复位按钮，副标题显示检测来源，照抄明暗开关交互）。
- 标题派生（App 内 useMemo，活动标签路径）：dashboard → 「Hoshineko Nya~」；trash → `t('nav.trash')`；真实路径 → `settings.showFullPathTitle` ? 路径 : basename（`/` 显示 `/`）；写入 `document.title`（实时同步 Electron 标题）。
- i18n 新增约 6 键 × 12：`settings.title_bar`、`settings.show_full_path_title`、`window.minimize`、`window.maximize`、`window.quit`、（副标题复用 `theme.follow_system` 与 `theme.source_*`）。

### 13.4 风险与决策点

1. **手动「关」在常规 DE 上失去拖拽把手**（frameless 固定）：接受为用户自主选择，文档注明；跟随系统模式在常规 DE 永远显示、平铺 WM 永远隐藏，无此问题。
2. **frameless 边缘 resize**：Linux 下可用但个别 WM 表现差异——niri/hyprland 需实测；必要时补 `resize` 提示或不做边缘 resize（决策点）。
3. **F12 拦截**：`before-input-event` 处理；若将来要菜单栏（无计划）再评估。
4. **多窗口**：标题/可见性按窗口独立；设置跨窗口同步（storage 事件），检测只做一次（主进程缓存）。
5. **e2e 影响**：现有用例顶部点击（如 03 关闭菜单点 y=8）会落在标题栏上——仍属 document 内 mousedown，行为不变，但新增标题栏用例时须按 visible 状态分支（本机 niri = 跟随系统隐藏，测试用 `settings.titleBar=true` 强制显示断言）。
6. 标题栏高度建议 40px（与按钮行一致）；仪表盘/回收站标题不随「完整路径」开关变化。

### 13.5 结论

**可行，工作量中等（后端 ~100 行 + 前端 ~350 行 + i18n 6 键 × 12 + e2e 新用例 ~100 行）。** 关键机制全部有现成先例（frameless+app-region、明暗三态开关、检测链、MarqueeText、ContextMenu、document.title 同步），风险集中在 frameless 边缘 resize 的 WM 兼容性（需实机验证）。**建议决策点确认后实施：① 手动「关」时接受无拖拽把手；② 标题栏高度 40px；③ 设置放「外观」区。**

### 13.6 实施记录（2026-09-01，已实施）

- **窗口层**：`frame: false` + `Menu.setApplicationMenu(null)`（屏蔽 Alt）+ `before-input-event` 补回 F12 devtools；`maximize/unmaximize` 事件推送（`window:maximized-changed`）。
- **后端**：`system:detect-window-manager`（XDG_CURRENT_DESKTOP→XDG_SESSION_DESKTOP 探测链 + TILING_WMS/STACKING_DESKTOPS 白名单，fallback stacking）；窗口控制 IPC（minimize/toggle-maximize/close/is-maximized）。
- **前端**：`TitleBar` 组件（app-region drag + v 菜单（ContextMenu）+ 三窗口按钮 + MarqueeText 标题，40px 高）；`useTitleBar` hook（`settings.titleBar: boolean|null` 三态 + 检测，主窗口与选择器窗口共用）；`App` 标题派生（仪表盘品牌串 / nav.trash / basename / 完整路径开关）→ `document.title` 同步；布局改 `.app-root`（column：标题栏 + 原 `.app-shell`）；设置外观区两行（标题栏开关+跟随系统复位+来源副标题、完整路径开关）。
- **i18n**：7 键 × 12。
- **e2e 15**：全链路（标题规则/同步/菜单/最大化还原/最小化/跟随系统隐藏/检测结果）；实测发现的坑点（跑马灯文本复制多份须读 title 属性、重载后启动路径异步须 waitFor）记入 AGENTS 与用例注释；portal 后端 `busName` 可覆盖（e2e 独立名防与运行中实例抢名）。

---

## 十四、任务栏/DMS 窗口标题前缀（app 名称）可行性报告（2026-09-01，仅评估，未实施）

### 14.1 现象与根因

- 现象：DMS（任务栏类区域）把窗口显示为「material-3-file-manager · <窗口标题>」。
- 根因：**前缀不是窗口标题的一部分**——e2e 15 已断言 `win.getTitle()` 与 `document.title` 精确等于我们设定的标题（目录名/「Hoshineko Nya~」等）。前缀来自 **Electron 运行时应用名**（WM_CLASS / Wayland app_id），DMS 按「应用名 · 窗口标题」渲染。
- 运行时 `app.name` 默认取 package.json 的 **`name`** 字段（`material-3-file-manager`），`productName`（"HoshinekoFM"）只被 electron-builder 用于打包命名，不影响运行时名称。

### 14.2 方案

1. **`app.setName('HoshinekoFM')`**：运行时应用名 → WM_CLASS/app_id → DMS 前缀变为新名称。注意：`setName` 会连带改变 **userData 默认路径**（`~/.config/<name>`）——必须先读旧 `app.getPath('userData')` 再 `setPath('userData', 旧路径)` 保留既有数据（clipboard.json、settings 等），且须在 ready 之前、任何 userData 读取之前执行（main.ts 模块顶层）。
2. **`app.setDesktopName('HoshinekoFM.desktop')`**（Wayland）：xdg-toplevel 的 app_id 来源，AppImage 的 .desktop 文件名按 electron-builder 配置（productName 派生），必要时配套设置保证 DMS 分组正确。
3. 不修改 package.json 的 `name`（npm 包身份 + userData 路径迁移风险，全部规避）。

### 14.3 风险与结论

- 风险低：改动集中在 main.ts 启动早期（约 5 行）；窗口标题本身不变（已由 e2e 锁定）；唯一副作用是任务栏分组名称变化（预期内）。
- **需实机验证**：DMS 究竟取 WM_CLASS 还是 app_id 作为前缀——两种来源都已覆盖（setName + setDesktopName）；若 DMS 强制显示「应用名 · 标题」且不允许隐藏前缀，则只能把应用名改得好看（无法从应用侧完全去掉前缀，这是面板的展示行为）。
- **结论：可行，改动极小；建议直接实施 setName（保留 userData）+ setDesktopName，用户在 DMS 实机确认显示效果。**

### 14.3 实施记录（2026-09-01，已实施）

- `electron/main.ts` 模块顶层（ready 前、任何 userData 读取前）：`app.getPath('userData')` 取旧路径 → `app.setName('HoshinekoFM')` → `app.setPath('userData', 旧路径)`（既有数据不迁移）→ `app.setDesktopName('HoshinekoFM.desktop')`（Wayland app_id 与打包 .desktop 对齐）。
- 窗口标题本身不变（e2e 15 已锁定）；DMS 前缀由应用名 HoshinekoFM 替代 material-3-file-manager，需实机确认。
