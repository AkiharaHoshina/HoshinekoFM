# 同步规则

主窗口（GUI）与 portal 窗口（服务模式常驻进程 `--portal` / `--filemanager1` 的
选择器/保存器）之间的个性化设置同步规则。

## 背景：两种窗口形态

- **GUI 模式选择器/保存器**：与主窗口共享同一 session/localStorage——
  `useLocalStorage` 经 storage 事件双向即时同步，无需快照。
- **服务模式选择器/保存器**：常驻进程 userData 与 GUI 隔离，读不到 GUI 的
  localStorage——必须经**快照文件**（GUI userData 下的 JSON）+ 主进程注入 +
  `fs.watch` 监听广播（实时继承）。

本文档的规则针对服务模式；GUI 模式天然即时同步。

## 快照机制总览

| 快照文件（GUI userData） | 上报 IPC | 广播 | 注入字段 | 同步时机 |
| --- | --- | --- | --- | --- |
| `sidebar-pinned.json` | `app:set-pinned-dirs` | `picker:pinned-dirs-changed` | `pickerConfig.pinnedDirs` | 变化即同步（立即） |
| `picker-prefs.json` | `app:set-picker-view-prefs` | `picker:view-prefs-changed` | `pickerConfig.viewPrefs` | **立即同步组** |
| `picker-settings.json` | `app:set-picker-settings` | `picker:settings-changed` | `pickerConfig.settings` | **确认时同步组** |
| `theme-snapshot.json` | `app:set-theme-snapshot` | `picker:theme-changed` | `pickerConfig.theme` | 变化即同步（立即） |

## 立即同步组

主窗口一变，打开中的选择器/保存器**立即**跟随（快照落盘 + 广播，不重开窗口）。

- 视图模式（网格/列表）——**例外**：它在设置对话框里，但仍属立即同步组
- 图标大小
- 显示隐藏文件
- 实心图标
- 跑马灯标题滚动
- **分类与否（语义分组）**——文件区个性化设置
- **排序方式（字段 + 方向）**——文件区个性化设置

实现：App.tsx 在上述状态变化时（含首次挂载）经 `app:set-picker-view-prefs`
上报 `picker-prefs.json`；服务模式创建选择器时注入 `viewPrefs`；常驻进程
快照监听广播 `picker:view-prefs-changed`；FilePicker 优先取注入/广播值
（GUI 模式回落共享 localStorage）。选择器内对分类/排序的本地调整写自己的
localStorage，下一次主窗口变化即覆盖（主窗口为权威）。

## 确认时同步组

设置对话框里开关的个性化设置，在**主窗口设置按下确定/退出**时才同步
（退出设置等于确定，见 SettingsDialog）。

- 搜索分类与否（`searchGroupByDir`）
- 标题栏显示完整路径（`showFullPathTitle`）——开启时选择器/保存器标题为
  「选择文件夹：完整目录」/「保存文件：完整目录」等
- 语言（`locale`，'auto' = 跟随系统）——服务模式选择器 userData 隔离、
  i18n 默认跟随系统；注入/广播后 FilePicker 应用同一语言（标题、按钮、
  筛选器等全部文本；标题文本渲染期派生，语言切换实时跟随）
- （后续新增的「设置对话框开关类」个性化设置归入此组）

实现：App.tsx 在 `settingsDialogOpen` 关闭时（含初始挂载种子值）经
`app:set-picker-settings` 上报 `picker-settings.json`；服务模式创建选择器时
注入 `settings`；常驻进程快照监听广播 `picker:settings-changed`；FilePicker
优先取注入/广播值。主窗口内部这两个设置**本身也确认时生效**（开关在
对话框内只改预览，确定/退出才落 `localStorage`，见 SettingsDialog 的
pending 机制）——主窗口、GUI 模式选择器（storage 事件）、服务模式选择器
（确认时上报 + 广播）三方同拍。

**GUI 模式回落**：真实 main.ts 只在服务模式（`--portal`/`--filemanager1`）
注入快照（`createWindow` 的 `SERVICE_ONLY_MODE` 分支）；GUI 模式选择器与
主窗口共享 session，FilePicker 经 `useLocalStorage` 读同一组键
（`localSearchGroupByDir` / `localShowFullPathTitle`，默认值与主窗口一致）——
新窗口创建时挂载即取当前值（**打开时同步**），主窗口确认后经 storage
事件实时跟随；语言在 GUI 模式由 i18n 模块自身的 storage 事件同步
（`settings.locale` 同键）。注入/广播值存在时优先于本地（服务模式路径
不受影响）。

搜索分类开启时若搜索页面打开：右上角分类按钮（SortControls）强制高亮
（filled 变体）且点击无效，退出搜索后恢复可点——主窗口与选择器/保存器
一致（FilePicker 同样经 `groupingForced={searchActive && searchGroupByDir}`）。

另：portal 窗口的搜索功能与主窗口同款——搜索态显示结果计数、清除按钮
与类型/最小/最大大小筛选器（`system:search` 同一条 IPC），搜索结果按
注入的 `searchGroupByDir` 分组。

## 主题与明暗

颜色主题（`settings.theme`）与明暗（`settings.darkMode`）在确定/应用后
变化即同步（`theme-snapshot.json`）：服务模式选择器/保存器注入
`pickerConfig.theme`，实时继承经 `picker:theme-changed`；明暗跟随系统走
与主窗口同一条后端检测链显式落值（`detectColorScheme` + 系统明暗广播）。

## 固定目录

侧边栏固定目录（`sidebar-pinned.json`）变化即同步（`picker:pinned-dirs-changed`）。

## 安全

所有快照经主进程 sanitize（白名单 + 字段类型校验），非法整体丢弃；
调用方经 `picker:open` 传入的 `pinnedDirs` / `viewPrefs` / `theme` /
`settings` 字段一律被白名单忽略——注入值来源唯一，不可由第三方伪造。

## 测试

- e2e 32/33/40/41 分别覆盖固定目录、显示偏好、主题、同步规则（立即组 +
  确认时组）四条快照链路的注入与实时广播断言。
- e2e 43 覆盖 GUI 模式回落：关闭快照注入后，选择器打开时即取共享
  localStorage 的当前设置（标题栏完整路径/搜索分类），主窗口写入后经
  storage 事件实时跟随。
- e2e 44 覆盖恢复默认设置（确认/取消 + 全部默认值断言）、滚动文本
  确认时生效、pickerSettings.locale 注入与广播（选择器标题实时切换语言）。
- harness（`scripts/e2e/harness.cjs`）对 main.ts 的快照 handler 有手工副本，
  修改 main.ts 的 snapshot 逻辑时须同步更新（见 harness 内注释）。快照
  注入默认开启（服务模式语义），`h.setPickerSnapshotInjection(false)`
  模拟 GUI 模式（真实 main.ts 的 `SERVICE_ONLY_MODE` 分支）。
