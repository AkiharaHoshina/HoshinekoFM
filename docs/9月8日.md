# 9 月 8 日工作记录

> 本文件汇总 2026-09-08 会话的全部变更与关键信息（版本 0.11.45.14 → 0.11.47）。
> 提交：本会话末（见 git log）。

---

## 一、功能变更总览

### v0.11.46 — 打开方式默认规则 + 无默认处理程序跳转 + 地址栏路径语法

本会话按「A → B → C」三组顺序落地：

#### A 组：无默认处理程序打开 → 「打开方式」对话框

- **问题**：文件 MIME 未注册系统默认应用时（如 application/octet-stream），
  xdg-open 会把文件交给浏览器弹「是否保存」，而不是打开文件
- **主进程**：`fs:open` 在非直接执行路径上先 `detectMime` 取 MIME，经
  `xdg-mime query default <mime>` 查询默认处理程序（3s 超时）——确无
  默认（空输出）时返回哨兵 `OPEN_NO_HANDLER`（'NO_HANDLER'，
  electron/handlers/fs.ts 导出与 src/utils/fileOperations.ts 常量同值）；
  查询工具缺失/检测失败按「有默认」fail-open（保持旧行为）；可执行
  文件直接执行路径与 ENOEXEC 回退均不受影响（回退也走同款检测）
- **渲染层**：`openFile` 返回状态串，四处打开入口接住 `OPEN_NO_HANDLER`
  后直接弹「打开方式」对话框——App 右键菜单「打开」、ExplorerTab
  双击/回车（handleNavigate）、搜索态右键菜单、仪表盘最近访问
  （Dashboard onOpenFile 构造最小 IFile）
- **e2e 61**：PATH 假 xdg-mime（text/plain 默认受 FAKE_HANDLER_MIME
  控制）/ 假 xdg-open（写 marker）——有默认仍走 xdg-open（回归）/
  无默认双击弹对话框且不调 xdg-open / 无默认右键「打开」同款

#### B 组：手动默认打开方式规则（DefaultOpenRule）

- **规则存储**：`electron/openRules.ts`——按 **MIME 键**写入
  `<userData>/DefaultOpenRule/<mime 把 / 换成 _>.json`（原子写
  tmp+rename；读取校验字段形状，缺失/损坏视为无规则）
- **启动链路抽取**：`electron/openLaunch.ts` 抽出 `launchWithApp`
  （gio launch 优先保 DE 环境 + 无桌面文件/失败回退 Exec 字段码替换
  spawn）——`system:open-with` 与规则覆盖共用，语义同源
- **覆盖语义**：`fs:open` 里规则查找**优先于系统默认**（也优先于无
  handler 检测：有规则不弹打开方式对话框）——「在打开方式界面指定的
  程序覆盖系统默认」
- **对话框 UI**：底部固定操作区（与取消/打开同排、左对齐，不随程序
  列表滚动）新增「以此应用作为默认打开方式」勾选行（md-checkbox 纯
  展示化 + 外层 role=checkbox 容器接管交互，与三态开关同款防受控
  时序竞争模式；Tab 序 = 搜索框 → 列表 → 勾选行 → 取消 → 打开）——
  勾选并打开成功后写入规则；所选程序已是手动默认时勾选行换
  「还原默认打开方式」链接（无图标）——点击删除规则文件、**关闭
  打开方式对话框**并弹带遮罩 AlertDialog（标题「已还原」、正文
  「已还原为默认打开方式」，onRestored 回调由 App 渲染提示）
- **IPC**：system:get/set/delete-open-rule（按文件路径参数，主进程
  detectMime；preload + electron.d.ts 同步）
- **e2e 62**：system:get-apps 换假应用（exec = 假脚本写 marker）+ 假
  xdg-mime/xdg-open + 规则目录落在沙箱 userData——勾选设为默认 →
  规则落盘 + 双击走规则应用覆盖系统默认（xdg-open 不被调用）/
  还原链接删除规则 + 关闭对话框 + 提示弹窗 + 双击回落系统默认 /
  规则优先于无系统默认（不弹对话框）。**踩坑**：关闭的对话框常驻
  DOM（rect 0×0），元素查询必须限定 open 的 md-dialog；对话框 cycle
  重挂载期间 DOM click 也失效（必须 waitDialogAnim）——均入 AGENTS.md

#### C 组：地址栏路径语法（~ / ./ / ../）

- **`src/utils/addressPath.ts`**：normalizePosixPath（折叠 `.`/`..`/
  重复斜杠）、resolveRelativePath（相对当前显示路径，trash:// 视为
  根级虚拟目录、`..` 越根回落 `/`，仪表盘同理）、expandAddressPath
  （`~`/`~/…` 家目录展开 + 相对/绝对解析）、looksLikePathInput
  （`.`/`..` 纳入路径判定）
- **Omnibar 接线**：handleSubmit 改 async（先取 `fs:get-home`）——
  主窗口与选择器/保存器共用 Omnibar 同款生效；编辑图标判定同源；
  软链接按词法父级（地址栏惯例，与 shell 逻辑 PWD 不同）
- **~ 文件名修复**：波浪号开头的文件名（`~file.txt`，不含 `/`）不算
  `~` 语法，按搜索处理——此前裸 `startsWith('~')` 把它当目录导航并
  弹「目录不存在」
- **e2e 63**：真实目录 `..`/`.`/`./x`/`../x`/折叠、`~` 与 `~/.` 家
  目录、trash:// 上 `..` 回落 `/`、无斜杠输入与带 `~` 文件名走搜索
  回归。**踩坑**：Enter 后 loadPath 异步完成，立即读地址栏拿到旧值
  （须轮询）；/tmp 大目录条目在虚拟列表不一定渲染（勿用条目断言）；
  仪表盘不渲染地址栏（无 UI 入口）；不存在的目标走 loadPath 最近可
  访问父级回落

#### e2e 55 修复（环境无关化）

- 主窗口换行断言原靠「侧边栏显隐」制造窄态，依赖窗口被平铺 WM 固定
  为窄宽——本会话环境窗口 1274px（未被平铺），窄态永不触发。改为
  全部宽度驱动走顶栏容器 `style.maxWidth` 注入（宽 1200 单行 /
  窄 500 换行，与选择器/保存器段落同款）；基线 stash 对照确认与
  本会话改动无关（纯环境变化暴露的测试脆弱点）

### v0.11.47 — 打开方式配置管理 + 对话框 Tab 滚动闪烁修复

承接上一版本的手动默认打开方式规则，提供可视化管理界面（A 组），
并修复对话框键盘遍历的滚动闪烁（B 组）。

#### A 组：打开方式配置管理

- **设置入口**：设置 → 行为新增「打开方式配置管理」行（副标题常驻）
  →「进入」按钮打开二级对话框 `OpenRuleManagerDialog`（与设置同宽
  512px、固定高 480px、带遮罩；`.openrule-scroll` 列表区内部滚动）
- **底部提示位置**：`openrule.hint`（系统配置只读/用户配置优先）在
  **actions 行与「完成」按钮同行**（常驻不滚动），不在内容滚动区内
- **用户配置段（先）**：标题 = 文件类型（`getMimeDisplayName` 回落
  原始 mime）+ **常见文件后缀**（`.openrule-item__ext`，主进程
  `getExtensionsForMime` 由 EXT_TO_MIME 反向映射，最多 6 个加 …）、
  副标题 = `打开方式：exec`；悬停/聚焦显示编辑图标（opacity 切换）；
  点击进内联编辑——「编辑」+ 路径输入 + 清除配置/快速导入/取消/确认
  - **清除配置** = 草稿清空（含快速导入带入的 desktopFile/name 隐藏字段）
  - **快速导入** = OpenWithDialog 导入模式（`mime` prop 直传按 MIME 查
    推荐程序——无文件路径场景；按钮文案「确认/OK」、隐藏设为默认行、
    `onSelect(exec, desktopFile, name)` 写草稿不启动）
  - **输入为空确认 = 删除配置回归系统默认**
  - **`~` 处理**：`~`/`~/…` 展开家目录、`~file`（波浪号开头文件名）
    保持字面量——组件内 `expandTilde` 只做前缀展开（不用
    expandAddressPath：它折叠整条 `..`/重复斜杠，会破坏 exec 命令行参数）
  - 段头「清除全部用户配置」按钮（遮罩确认 → `clearAllOpenRules`）
- **系统配置段（后，只读）**：各层 mimeapps.list `[Default
  Applications]` 合并——**过滤 `inode/`/`x-scheme-handler/`/`x-content/`**
  （fs:open 规则应用有 `stats.isFile()` 守卫，这些条目写用户规则永不
  生效，展示会误导；目录默认程序由「默认文件管理器」行独立管理）；
  标题 = 文件类型 + 后缀、副标题 = 应用名（exec 为绝对路径时
  `名称 · 路径`），悬停复制图标，点击把配置按 DefaultOpenRule 格式写
  入用户配置目录（`setOpenRuleMime`：exec + desktopFile + name）
- **覆盖与不可复制**：被用户配置覆盖的条目显示 X（title「已被用户
  配置覆盖」）且点击无效；**无法解析 Exec 的条目**（桌面文件缺失——
  多为 mimeapps.list 残留已卸载应用/未找到 snap 目录，或无 Exec 行）
  无图标 + `openrule-item--inactive` 点击无效（title「无法复制」
  解释）——桌面文件搜索目录补充 snap（`/var/lib/snapd/desktop/
  applications`；真机 355 条默认中 85 可复制、242 条全部为残留已卸载
  应用条目，展示层已如实标注原因）
- **恢复默认设置不清空用户规则**（规则是数据非设置，与固定项/最近
  文件同类——批量清空走对话框内「清除全部」）
- **后端**：`openRules.ts` 加 `listOpenRules`/`clearAllOpenRules`
  （逐文件解析/删除，损坏跳过）；system.ts 加
  `system:list-open-rules`（附 extensions）/`list-system-defaults`/
  `set-open-rule-mime`/`delete-open-rule-mime`/`clear-all-open-rules`/
  `get-recommended-apps-mime`（mime 入参校验合法形态防 shell 注入，
  推荐程序按 MIME 直查从 `get-recommended-apps` 抽出共享
  `getRecommendedAppsForMime`）

#### B 组：对话框 Tab 遍历滚动闪烁修复（Dialog 焦点滚动校正）

- **现象**：打开方式/打开方式管理对话框 Tab 遍历时闪烁——先滚动、
  选框飞到别处、再回到正确位置
- **根因（插桩确认）**：① 旧实现 fixed 20ms 只校正一次，校正发生在
  首帧绘制之后 → 居中位置先绘制一帧再跳回；② 浏览器焦点滚动会
  **迟到回放**——首段滚动后立即校正 scrollTop，约 50–100ms 后浏览器
  按原居中量再次滚动把校正顶掉（软件渲染下更明显）
- **新实现**（`Dialog.tsx`）：focusin 登记待校正目标 → 每个 scroll
  事件 0ms 尾部 debounce 校正（滚动落定后、渲染前校正，只绘制终态）
  → 校正后保持武装 400ms（`CORRECTION_HOLD_MS`，回放滚动再次触发
  校正，幂等）→ 滚轮/触摸/键盘（keydown 挂 **window 捕获**而非
  host）立即放弃待校正（不与用户主动滚动争夺位置；彩蛋对话框
  PgDn/PgUp 翻页不受干扰——e2e 49 曾因此翻页失效；Tab 引发的
  keydown 后紧跟 focusin 会重新登记）
- **回归**：27c 最小滚动校正、49 彩蛋翻页、04/44 设置、62 打开规则、
  64 全部通过；全套件中 21d/22b/55 等失败经 `git stash -u` 基线对照
  确认为 HEAD 既有失败，与本会话改动无关

## 二、i18n

- 新增 4 键 × 12 语言文件：`open_with.set_default`（以此应用作为默认
  打开方式）、`open_with.restore_default`（还原默认打开方式）、
  `open_with.restored_notice`（已还原为默认打开方式）、
  `open_with.restored_title`（已还原）
- 新增 17 键 × 12 语言文件：`settings.open_rule_manager` /
  `settings.open_rule_manager_desc` / `settings.open_rule_manager_enter`
  与 `openrule.title` / `user_section` / `system_section` / `user_empty`
  / `system_empty` / `user_subtitle` / `edit_title` / `exec_label` /
  `clear` / `quick_import` / `clear_all` / `clear_all_confirm` /
  `overridden` / `copy_unavailable` / `hint`

## 三、当前状态

- 版本：**0.11.47**；e2e 64 套（新增 61 无默认处理程序跳转、62 手动
  默认打开方式规则、63 地址栏路径语法、64 打开方式配置管理；55 环境
  无关化修复；27a Tab 序更新；27c/49 焦点滚动校正回归；62/04/44 回归；
  全套件基线对照确认 21d/22b/55 为 HEAD 既有失败）
- 文档：docs/进度.md、docs/latest-updates.md、docs/9月8日.md（本文件）、
  AGENTS.md 同步更新
- 已知限制：portal 多文件保存（SaveFiles）返回 NotSupported；收藏夹/
  格式化有意不做；GNOME 需桌面图标扩展才能看到桌面快捷方式；UI 字体
  Google Sans（中文回落 Noto Sans SC）；DefaultOpenRule 按 MIME 键
  （同类型文件共享一条规则，非按单文件）
