# 9 月 1 日工作记录

> 本文件汇总 2026-09-01 会话的全部变更与关键信息（版本 0.11.17 → 0.11.24）。
> 提交：`e97829f` → 本次会话末（见 git log）。

---

## 一、功能变更总览

### v0.11.17 — 文件预览面板
- 设置 → 行为「文件预览」开关（默认关）；面板常驻：未选中显示当前目录只读属性、单选目录显示该目录属性、单选文件预览、多选「多个项目无法预览」占位
- 类型：图片/音频/视频（含 mkv）/PDF（pdf.js 前 5 页 + 超出说明）/归档列表（unzip/tar/bsdtar/7z）/Markdown（marked + DOMPurify）/文本（512 KiB 上限）
- 分隔条拖动 20–60% 持久化；`preview://` 协议（standard + corsEnabled 注册、`preview://localhost` 形态、Range 206 流式）
- 目录属性与右键属性对话框共用 `PropertiesGrid`（预览侧只读）
- 新依赖：pdfjs-dist / marked / dompurify（全部动态 import 代码分割）

### v0.11.18 — 批量重命名 + e2e 固化
- 多选右键只显示「批量重命名...」；对话框顺序跟随显示顺序（sortedFiles 过滤选中集）
- `preview.multiple` 文案：文件 → 项目
- **e2e 套件固化**：`scripts/e2e/` 公共 harness + 12 套用例（真实构建产物驱动，无测试框架）、`npm run e2e`、AGENTS 记录坑点

### v0.11.19 — 选择器通信层 + portal 后端
- `picker:open` 扩展 `filters`/`initialPath`/`defaultFilterId`（白名单校验 + resolvedMime）；底部类型下拉常驻（路径右侧，56px 高，撑满至按钮前）
- 并发独立（按 webContents.id 隔离）；mime 文案扩充（Office 描述名）
- **xdg-desktop-portal FileChooser 后端**（impl 接口，dbus-next）：`--portal` 服务模式、packaging 配置、docs/portal-filechooser.md

### v0.11.20 — 自定义 M3 标题栏
- frameless（`frame: false` + 菜单置空屏蔽 Alt + F12 补回）；v 菜单 + 最小化/最大化还原/关闭
- 三态开关（跟随系统/开/关）：`system:detect-window-manager`（平铺 WM 隐藏/常规 DE 显示/fallback 显示）；**确定时生效**（本地预览）
- 标题规则：仪表盘「Hoshineko Nya~」/回收站/目录名（完整路径开关）；`document.title` 同步 Electron 窗口标题
- 应用名全面修正 **HoshinekoFM**（productName/setDesktopName/index.html/服务文件）

### v0.11.21 — 默认文件管理器 + FileManager1
- 设置「设为默认/恢复为系统默认」：用户级桌面入口（Exec 按环境自适应）+ `xdg-mime default HoshinekoFM.desktop inode/directory`
- **org.freedesktop.FileManager1**：OpenFolders/ShowFolders/ShowItems（定位选中）/ShowItemProperties；`app:get-startup-request` 启动定位机制；`--filemanager1` 服务模式；名称被占静默降级
- 顺带修复 FileList 滚动定位竞态（useListCallbackRef + 挂载后重试）

### v0.11.22 — 系统集成一键安装
- `scripts/system-integration/install.sh`（幂等）：root 级（portal 配置 + D-Bus 激活文件，pkexec 重入）+ 用户级（portals.conf preferred、xdg-mime、portal 服务重启）
- 设置「系统集成」行：说明做不到的部分（Firefox about:config 手动）+ 安装按钮 + 状态检测

### v0.11.23/24 — portal 兼容真实 Firefox
- v0.11.23：Firefox 过滤是**带字符类的 glob**（`*.[jJ][pP][gG]`）——转后缀匹配大小写不敏感正则；名称直接作 id/label；匹配全部 `*` 跳过；请求对象导出在 portal 传入的 handle 路径本身
- v0.11.24：本机 xdg-desktop-portal **1.22 为新协议**——OpenFile 直接返回 `(u, a{sv})`（无 Request 往返）；过滤激活时列表**只显示**匹配文件 + 全部目录

---

## 二、关键技术信息（坑点速查）

### D-Bus / portal
- impl 接口签名：OpenFile `osssa{sv}` → 新协议返回 `ua{sv}`、旧协议（<1.19）返回 `o` + Response 信号
- dbus-next：`a{sv}` 服务端值须 **unwrap Variant**；signals 必须声明在 `configureMembers` 里客户端才能订阅；`bus.requestName` PrimaryOwner = 1；方法返回多值用数组
- portal 前端在 handle 路径监听（旧协议）；FileManager1 名是单实例——**Nautilus 运行时持有则我方静默降级**
- 验证命令：`gio mime inode/directory`、`gdbus call …OpenFile`、`journalctl --user -u xdg-desktop-portal.service -f`

### Electron
- 缩放因子**会话级共享**；同窗口 `localStorage.setItem` 不触发 storage 事件（同步必须另一窗口写）
- `picker:resolvePicker` 立即关窗 → 自身 IPC 响应丢失（fire-and-forget）
- frameless：`Menu.setApplicationMenu(null)` 屏蔽 Alt；F12 经 `before-input-event` 补回
- `app.setName` 会改变 userData 默认路径（先读旧值再写回）

### React / UI
- 同窗口多个 `useLocalStorage` 同键实例互不同步（状态须独占持有——useTitleBar 教训）
- react-window：内联 ref 回调导致 React #185 无限循环 → `useListCallbackRef`（稳定 setState 作 listRef）
- md-select 的 `select()` 静默不派发事件（补 `input` 事件）；选项是宿主轻 DOM 子节点
- 跑马灯把文本复制多份渲染（断言读 title 属性）；Dialog 250ms 串行化延迟（断言 `md-dialog.open`）
- 同键 useLocalStorage、FileList 滚动定位「列表未挂载即消费防重标志」的竞态

---

## 三、当前状态

- 版本：**0.11.24**；e2e 18 套全绿（`npm run e2e`）
- 外部调用链路三条齐备：MIME 关联（打开文件夹）/ portal FileChooser（文件对话框，OpenFile 已通）/ FileManager1（显示定位）
- 未实现（有意不做）：收藏夹、格式化；SaveFile 未实现（portal v1 限制）
- Firefox 验证：`widget.use-xdg-desktop-portal.file-picker=1` + portals.conf preferred=hoshineko + 应用运行中 → 网站已能收到所选文件
