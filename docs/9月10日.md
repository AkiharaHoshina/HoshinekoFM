# 9 月 10 日工作记录

> 本文件汇总 2026-09-10 会话的全部变更与关键信息（版本 0.11.47 →
> 0.11.48）。
> 提交：本会话末（见 git log）。

---

## 一、变更总览

### 版本升版 0.11.48

- 9 月 9 日开发的三个右键菜单功能（e2e 65 / e2e 66 / e2e 67，
  此前以「v0.11.47 补充」暂记）正式纳入 **v0.11.48**：
  - **已固定文件夹右键菜单（e2e 65）**：侧边栏固定项右键——第一组
    复用文件区文件夹菜单（三裁剪）+ 第二组上移/下移/取消固定；
    重命名/删除联动
  - **Places 位置区右键菜单（e2e 66）**：位置区条目右键（仪表盘 =
    仅「打开」、回收站 = 打开 + 属性、其余位置 = 目录菜单裁剪版）；
    后端 get-directory-size trash 映射
  - **回收站背景右键菜单「属性」（e2e 67）**：回收站空白处右键补
    「属性」第三项；App 属性补全链路统一（`openPropertiesWithStat`）
- `package.json` / `package-lock.json` 版本 0.11.47 → 0.11.48

### 地址栏按钮自动收缩（e2e 68，设计文档 docs/设计功能 地址栏按钮自动收缩设置.txt）

- **设置 → 外观**新增「地址栏按钮自动收缩」行（默认关闭，**确定时
  生效**——与全部设置项统一 pending 草稿机制）——`settings.
  sortControlsAutoCollapse`（PickerViewPrefs 扩展 + 恢复默认设置重置
  false；i18n 新键 `settings.sort_auto_collapse` × 12 语言）
- **关（默认）= 现状**：收起把手/「展开控件」项都在、窄窗口换行
  （e2e 55 行为逐字节不变）；**开 = 隐藏手动切换入口**（SortControls
  新 prop `autoCollapse`），生效折叠态 = `auto ? autoDerived : 手动值`
- **自动推导**（`useAutoSortCollapse` hooks/ 单向状态机防振荡——
  「wrapped ⇄ 折叠」往返会振荡，振荡带 ~250px）：收缩 = 展开态下
  `wrapped === true`（与换行条件严格同源）；展开 = 折叠态下改测容器
  宽度（up/展开态控件宽/padding 全部实测 + 4px 回差，两条件互斥）；
  地址栏 min-width 按生效态（折叠时 0 永不换行）、自动模式抑制换行
  装饰线、SortControls 菜单残留 menuOpen 渲染期归零
- **选择器/保存器**：经 viewPrefs 快照注入继承（主窗口为权威，
  无会话覆盖）；自动模式开时手动折叠值被忽略、关闭恢复
- **e2e 55 环境修复**：宽度注入由 maxWidth 改为显式 `width`
  （maxWidth 在窗口被 WM 固定窄于目标宽时完全失效——本会话环境窗口
  被 tiler 固定为 817px，e2e 55 在干净代码上同样失败；显式宽度使
  顶栏容器独立于窗口宽度，两用例均已环境无关）
- e2e 68 通过；回归 04/06/13/25/26/33/41/42/43/44/50/51/55/59/65/66/67
  通过（54/57 为环境性失败，干净代码同失败）

### 全部设置项确定时生效

- SettingsDialog **全部**设置项统一 pending 草稿机制：对话框内更改
  （含此前立即生效的显示隐藏文件/视图模式/图标大小/实心图标/主页
  存储占用/计算目录大小/地址栏按钮自动收缩，及二级对话框「新建
  标签页目录」——二级确认只写草稿 `pendingNewTabPath`）只更新本地
  预览，底部三按钮（与主题颜色对话框同款）**取消**（丢弃草稿直接
  关闭）/ **应用**（保存不关闭）/ **确定**（保存并关闭）才写
  localStorage + storage 事件 + 快照广播同步其余窗口；**Escape/遮罩
  关闭 = 取消**（不再「退出 = 确定」）；打开对话框重置全部草稿、
  应用值外部变化时重置草稿（props 同步 effect）
- 四个 toggle 型 props 改值型 setter（`onShowHiddenFilesChange` 等，
  App 传 setter——应用/确定时写入草稿值而非取反）；**顶栏直接控件**
  （视图模式切换按钮/Ctrl+滚轮缩放/控件组把手/排序分组）无确定
  步骤，保持立即生效与立即同步
- e2e：04 补「确定前不写持久化键」断言 + Escape = 取消回归 + 点击
  前 sleep 等滚动落定；57 新建标签页目录补草稿语义（`~` 展开断言
  移到确定后）；68 同款草稿断言；15/27 的 `.settings-row` 绝对下标
  改文本精确匹配（外观区新增行移动下标）

### 打开方式对话框确认即关窗（修复）

- `OpenWithDialog.handleConfirm` 原为 `await onSelect(...)`（等待启动
  IPC 完成）后才 `onClose()`——启动挂起/被拒时文件已打开但对话框
  残留不关。改为**先 `onClose()` 再 `await onSelect`**（启动与关闭
  解耦）：确认即关闭（与 GNOME 等文件管理器同款语义），启动失败只
  经 toast 呈现；快速导入（OpenRuleManagerDialog）同链路不受影响
- 验证：e2e 62/64 通过；伪造「永不 resolve 的 open-with handler」
  探针确认对话框仍立即关闭

### 外观设置预览（e2e 69，设计文档 docs/设计功能 外观设置预览.txt）

- 设置对话框外观区顶部 sticky 不可滚动预览区（`.settings-preview-fixed`）
  ——文件区样例（隐藏 `.example.txt` / 长名 png（缩略图 = src/icon.svg）
  / 文件夹 folder）随外观**草稿**即时变化（隐藏文件/视图样式/图标
  大小/实心图标/滚动文本 + 分组应用值，分组开显示语义分组头）；
  scroller 离开顶部时预览区底部分界线着色（onScrollerReady +
  `--scrolled`，sticky 复用主题颜色对话框同款机制）；预览区高度随
  内容自适应；对话框高度 560→680
- 「滚动文本」行自行为区移入外观区（实心图标之后，pending 不变）
- **实现后精修（用户反馈四项）**：① 预览背景卡——圆角 16px +
  `color-mix(surface-container-high 85%, black)`（明暗模式都深一点，
  M3 色阶暗色反转纯 token 不可通用）+ overflow hidden；② 全面复用
  文件区类——`.file-list-container`/`.file-group-header`（Row.tsx 同款
  内联样式）/`.grid-row-container`/`.file-grid-item`，列表行补
  `height:auto`（类默认 52px 会裁大图标）；③ 界面缩放滑杆穿透预览
  区——md-slider 内部 `.onTop z-index:1` 且宿主无 stacking context，
  固定区 z-index 1→2；④ 对话框 560→640 加宽（长语言按钮 ~230px 被
  挤压截断；`max-width !important` + min-width 640 + 行内按钮
  `flex-shrink:0`；e2e 04 宽度断言 640）；⑤ 预览高度 `45vh` 封顶
  （e2e 44 回归暴露：uiScale 150% 时自适应预览 462px 高过 scroller
  445px，吸顶预览遮住全部设置行——真实 UX 缺陷）
- **harness scrollIntoView 升级**：原生 scrollIntoView 不滚动 shadow
  内 scroller——改手动居中 + 避开吸顶 sticky 区 + 几何验证重滚 +
  先等 500ms 让 Dialog 焦点滚动校正解除武装（与其互搏产生滚动风暴
  曾致 WATCHDOG 挂起）；低于折叠线的开关改 js click（真实指针在本
  环境间歇命中 md-dialog 宿主，e2e 04/15 已改）；e2e 44 补
  waitDialogAnim（打开动画收尾前焦点校正未解除武装）
- e2e 69 通过；04/15/16/44/56/57/64/68 回归通过（05/27/42 等在本
  会话末随用户 dev 实例启动后出现环境性失败——测试窗口与 dev 应用
  窗口争夺显示器/焦点，非功能回归）

### 外观预览再精修（高度/收起开关/二级滚动）

- 对话框高度 740px（710 +30px）
- sticky 固定区顶部整行「展开/收起预览」开关（文案 + 三角指向切换
  目标，i18n × 12 新键 `settings.preview_collapse/preview_expand`）——
  收起后固定区只剩开关细条（展开入口始终可达，仍吸顶带分界线）；
  **状态持久化** `settings.previewCollapsed`（默认 false = 展开，
  与其他设置同款跨窗口 storage 同步，恢复默认设置重置展开，无
  pending 直接生效）
- 预览卡固定 `max-height: 200px` + `overflow-y: auto` 卡内二级滚动
  （替换 45vh 封顶；滚动条为 Dialog 注入的 `::slotted(*)` M3 样式；
  固定上限顺带规避 uiScale 放大时吸顶预览高过整个 scroller 视口的
  缺陷）；e2e 69 补滚动/收起/展开断言

### 五项再精修（200px / 取消·应用·确定按钮 / Escape=取消）

- 预览卡最大高度 210 → 200px（含「完成」改「确定」前的 240 → 210）
- 设置对话框底部改**三按钮**（与主题颜色对话框同款）：**取消**
  （text，丢弃草稿直接关闭）/ **应用**（tonal，`applyPending` 保存
  不关闭）/ **确定**（filled，应用并关闭，原「完成」改名）——
  「确定」文案 = `settings.done` × 12 语言（完成 → 确定/OK 等，
  OpenRuleManagerDialog 底部按钮同键同文案）+ 新键 `settings.apply`
  × 12 语言
- **Escape/遮罩关闭 = 取消**（丢弃草稿不保存，「退出 = 确定」旧
  语义废弃）——Dialog `onClose` 接 `handleCancel`；App 的
  pickerSettings 上报注释同步（取消时应用值未变、重复上报幂等）
- e2e：harness 新增 `clickSettingsConfirm`（js 点击 open 且含
  `.settings-content` 的 md-dialog 的 `[slot="actions"] md-filled-button`
  ——限定 actions 槽：内容区有视图模式等 filled 按钮）；04 补
  Escape=取消断言；15/42/44/56/57/68/69 的「Escape = 确定」改点
  确定按钮

### 四项再精修（240px / 隐藏文件行移位 / Ctrl+PgUp / dev 无效化）

- 预览卡最大高度 260 → 240px
- 「显示隐藏文件」设置行移入外观区、置于预览区下方（语言区下方
  独立行移除；e2e 04 下标 1 仍为实心图标行不受影响）
- 设置界面打开期间 **Ctrl+PgUp** 打开 portal 运行信息——现拉
  `getPortalRuntimeInfo` 后打开 PortalVersionDialog 开发详情视图
  （标题「Portal 运行时状态」，正文含 `appVersion:` 诊断字段不受
  locale 影响，失败也打开、详情区空值；与 Ctrl+PgDn 同款监听，
  portal 版本弹窗打开期间不生效）；e2e 49 补断言
- **开发模式（未打包）下 .desktop 自动操作无效化**：
  `electron/launcherEntry.ts` 的 `launcherOpsDisabled()`（
  `!app.isPackaged && !HOSHINEKO_E2E_LAUNCHER_EXEC`）——ensure/
  remove 直接返回 no-op 成功（不创建/删除 .desktop、不写 marker，
  设置开关值照常持久化）；e2e 56 经该 env 显式启用覆盖链路 + 末尾补
  「删 env 重载不得创建/marker」断言

### 预览区 open 守卫（e2e 42 回归暴露）

- 预览样例复用真实文件区类（.file-list-item/.file-group-header 等），
  而关闭的设置对话框常驻 DOM（display:none）——关闭后仍渲染会污染
  全局 .file-list-item 查询（e2e 全套「文件区已加载」信号首命中预览
  样例：窗口首帧即匹配、真实顶栏尚未挂载时点击落空，e2e 42 实测
  复现）
- SettingsDialog 新增 `previewMounted` 状态守卫：打开立即挂载、
  关闭延迟 300ms 卸载（覆盖 md-dialog 关闭动画的内容可见收尾期，
  预览区不在动画中提前消失导致内容高度突变）——预览复用任何全局
  选择器常客类时必须考虑关闭态常驻 DOM 的污染，不能仅按 open 守卫
- e2e 64 底部按钮断言 完成|Done → 确定|OK（settings.done 改名波及
  OpenRuleManagerDialog 同键按钮文案）
- 全套受影响的 e2e 回归通过（04/05/15/16/27/41/42/43/44/49/55/56/
  57/62/64/68/69）

## 二、文档同步

- **docs/latest-updates.md**：新增顶层 `## v0.11.48` 章节，三个
  「v0.11.47 补充」条目移入并改标「v0.11.48」；v0.11.47 章节保留
  原有内容（打开方式配置管理 + Tab 滚动闪烁修复 + 自定义新标签页
  目录 `~` 支持）；v0.11.48 章节追加地址栏按钮自动收缩、外观设置
  预览、全部设置项确定时生效与三按钮/Escape=取消
- **docs/进度.md**：头部版本与最后更新日期、三个功能条目版本标记
  （v0.11.47 补充 → v0.11.48）、默认配置表/设置项枚举/功能条目
  追加地址栏按钮自动收缩、e2e 55 宽度注入描述更新、预览卡
  max-height 200px 与三按钮/Escape=取消
- **AGENTS.md**：三个新坑点条目版本标记更新（e2e 65 补标
  v0.11.48 起、e2e 66/67 由 v0.11.47 起改为 v0.11.48 起）+ 新增
  地址栏按钮自动收缩坑点条目 + e2e 55 宽度注入坑点更新 + 全部设置
  项应用/确定时生效条目改写（取消/应用/确定三按钮 + Escape=取消 +
  clickSettingsConfirm 坑点）+ 预览卡 max-height 200px
- **docs/9月9日.md**：头部与「当前状态」补 9 月 10 日升版说明
- **docs/设计功能 外观设置预览.txt / docs/设计功能 地址栏按钮自动收缩设置.txt**：
  预览卡 200px、应用/确定时生效与取消语义同步
- 本文件（docs/9月10日.md）新建并随本功能补充

## 三、当前状态

- 版本：**0.11.48**；e2e 65、e2e 66、e2e 67、e2e 68、e2e 69 通过
- 文档：docs/latest-updates.md、docs/进度.md、docs/9月9日.md、
  docs/9月10日.md（本文件）、docs/设计功能 地址栏按钮自动收缩设置.txt、
  docs/设计功能 外观设置预览.txt、AGENTS.md 与版本号同步
