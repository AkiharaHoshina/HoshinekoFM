# Hoshineko File Manager — 项目进度

> 本文档基于代码库现状（`main` 分支，版本 0.11.48）整理，记录本项目已实现与未实现的功能。
> 最后更新：2026-09-10

## 项目概况

Hoshineko File Manager（构建产物名 "HoshinekoFM"）是基于
[material-3-file-explorer](https://github.com/bhimio1/material-3-file-explorer) 重构的现代文件管理器：
Electron + React 19 + Material 3（`@material/web`），后端使用 Linux 系统命令与 D-Bus 服务，
目标平台为 Linux（AppImage 分发）。

### 默认配置（首次使用 / 恢复默认设置）

| 设置项 | 默认值 |
| --- | --- |
| 语言（`settings.locale`） | 跟随系统（`auto`） |
| 显示隐藏文件（`settings.showHiddenFiles`） | 开 |
| 显示模式（`settings.viewMode`） | 列表（`list`） |
| 图标大小（`settings.iconSize`） | 48px |
| UI 大小（`settings.uiScale`） | 100% |
| 实心图标（`settings.filledIcons`） | 关 |
| 主题颜色（`settings.theme`） | 跟随系统（null，matugen 传统加载） |
| 明暗主题（`settings.darkMode`） | 跟随系统（null） |
| 标题栏（`settings.titleBar`） | 跟随系统（null） |
| 标题显示完整路径（`settings.showFullPathTitle`） | 关 |
| 滚动文本（`settings.marqueeEnabled`） | 关 |
| 搜索分类（`settings.searchGroupByDir`） | 开 |
| home 存储占用（`settings.showHomeStorageUsage`） | 关 |
| 文件预览（`settings.filePreview`） | 关 |
| 计算目录大小（`settings.calculateDirSize`） | 开 |
| 自动创建桌面图标（`settings.autoCreateDesktopEntry`） | 开 |
| 自动创建应用程序菜单条目（`settings.autoCreateAppMenuEntry`） | 开 |
| 新建标签页目录（`settings.newTabPath`） | `/` |

恢复入口：设置底部「默认配置」区域「恢复默认设置」按钮（带遮罩确认
对话框）——实现见 App.tsx `handleRestoreDefaults`（与各 useLocalStorage
默认参数一致）。

---

## 一、已实现

### 1. 窗口与应用架构

- 多窗口单后端：所有窗口共享一个主进程，窗口实例集合管理
- 单实例锁：二次启动打开新窗口并聚焦（已修复 niri 工作区回跳问题）
- 启动路径按窗口解析（命令行参数传入文件/目录）
- **自动创建启动器条目（v0.11.44）**：两个独立开关（设置 → 行为，默认开启，
  **确定时生效**——SettingsDialog pending 机制）——桌面快捷方式
  （`settings.autoCreateDesktopEntry`）与应用程序菜单条目
  （`settings.autoCreateAppMenuEntry`）；语义：**打开并确定 → 创建**（首次挂载
  按当前开关值创建，默认开 → 首启即建；主进程 marker（userData
  `launcher-entries.json`）保证「创建一次，删掉不补」——手动删文件不重建、
  已存在（系统集成装过/用户自建）不覆盖），**关闭并确定 → 删除条目并清
  marker**（重新打开并确定 = 新创建意图，可再建）；恢复默认设置补一次创建
  意图。核心逻辑抽成 `electron/launcherEntry.ts` 共享模块（main.ts 与 e2e
  harness 同一代码路径）：Exec 优先 `APPIMAGE` 回落进程路径（桌面条目规范
  转义 + `%U`）、图标复制到 `~/.local/share/icons/hicolor/scalable/` 稳定落点
  （extraResources 打包）、`chmod 755` + `gio metadata::trusted`（GNOME 双击
  信任 best-effort）；marker 读-改-写竞态经进程内串行链修复（两路调用并行会
  丢标志）
- 防白屏（`ready-to-show`）、Wayland 窗口装饰标志
- **frameless 自定义 M3 标题栏**：原生标题栏彻底隐藏（`frame: false` + 菜单置空屏蔽 Alt，F12 devtools 经 before-input-event 补回）；标题栏可选（跟随系统/开/关三态，跟随系统按 `system:detect-window-manager` 检测平铺 WM 隐藏、常规 DE 显示，fallback 显示）；**三态开关语义（v0.11.38）**——跟随系统按钮→null、手动点开关→取反、跟随模式点开关→退出跟随+新值=生效值的反（与明暗开关同构：md-switch 纯展示化 + 外层 role=switch 容器接管 + 函数式更新，见 `docs/跟随系统设置项目逻辑.md`）；v 菜单（**新建窗口（v0.11.45）**/最大化/最小化/退出）+ 最小化/最大化还原/关闭三按钮；**平铺 WM 下隐藏最小化入口**（按钮 + v 菜单项，`TitleBar hideMinimize`）+ **主进程 `window:minimize` no-op 兜底**（平铺 WM 不实现 iconify，最小化后无从恢复的卡死根治）；标题规则（仪表盘「Hoshineko Nya~」/回收站/目录名/完整路径开关），经 document.title 实时同步 Electron 窗口标题

### 2. 文件浏览与导航

- 虚拟化文件列表（react-window + AutoSizer），网格/列表双视图（**右上角单个切换按钮即时切换**，与设置同键 `settings.viewMode`），图标 16–128px 可调（设置滑条或 **Ctrl+滚轮缩放**——文件区上按 Ctrl+滚轮按 16–128±8 同范围/步进缩放，主窗口/选择器/保存器三处同款手势，见 v0.11.41）
- 语义分组（文件夹/媒体/文档/代码/压缩包/可执行文件/其他，可开关）
- 排序：名称/大小/日期，点击已选中键切换升降序
- 100+ 种 MIME 类型图标映射；图片经 `media://` 协议显示真实缩略图（懒加载、失败回退）
- 特殊条目样式：软链接（损坏标红）、块设备/挂载点徽标、隐藏文件开关
- 面包屑导航：家目录胶囊（多用户检测）、特殊挂载点胶囊、回收站胶囊、软链接斜体+展平、可编辑模式
- 地址栏（Omnibar）：路径导航与搜索二合一（`find -iname`，限 100 条）；**搜索结果右键「定位到所在文件夹」**——跳转父目录并滚动定位/选中目标条目（搜索结果与目录同名条目的竞态已处理）；**搜索高级过滤**（类型 文件/文件夹、最小/最大大小，maxSize 已处理 find -size 取整陷阱）；**路径语法（v0.11.46）**——`~`/`~/…` 展开家目录、`.`/`..`/`./x`/`../x` 相对当前显示路径 POSIX 词法折叠、绝对路径折叠 `.`/`..`/重复斜杠（`src/utils/addressPath.ts`，主窗口与选择器/保存器共用 Omnibar 同款生效；软链接按词法父级；trash:// 视为根级 `..` 越根回落 `/`；仪表盘不渲染地址栏）；**波浪号开头的文件名**（`~file.txt`）不算 `~` 语法按搜索处理
- 侧边栏：常用位置（Places）+ 设备区（外接磁盘树、挂载/卸载/弹出）
- 仪表盘：问候语、存储用量卡片、固定项（文件/目录，**支持拖拽排序**）、最近访问
- 状态栏：条目数、选中数、选择模式提示、悬停文件信息
- **全键盘导航体系**（键盘分区框架 `src/utils/focusZones.ts`）：
  - **Tab 分区循环**：Tab 在「功能栏 → places（侧边栏）→ 标签页 → 返回上级键 → 地址栏内 → 分类开关/视图切换/排序方式 → 文件区」间循环停靠（Shift+Tab 反向；仪表盘为 功能栏-places-标签页-存储-固定项-最近访问 三子区；未注册分区自动跳过）；输入框/终端/对话框保持浏览器默认 Tab 行为；文件条目 `tabIndex=-1` 不进 Tab 序（修复 Tab 聚焦条目引发的键盘崩溃场景）
  - **分区内导航**：方向键移动（roving tabindex 或容器级）、Enter/Space 显式激活（注入键盘事件的 Enter 不合成原生按钮点击；分区级 stopPropagation + 变体切换丢焦点的 rAF 恢复）
  - **文件区键盘选择**：方向键 = 移动选择而非滚动——列表视图沿显示序（Up/Left 上一个、Down/Right 下一个，跳过分组头）；网格视图二维移动（左右同行、上下跨行且列位钳制到目标行长度，行首行尾不环绕）；无选中时 Down/Right 取首项、Up/Left 取末项；Home/End 首末项、PageUp/PageDown 翻页估算、Ctrl+方向键网格跳行首行尾/首末行、Space 切换选中、type-ahead 键入定位（1.5s 前缀累积）；Enter 打开游标条目、空选择回上级（虚拟目录除外）；**Tab 落到文件区 = 用文件区选择机制选中视口内第一个可见文件**（不直接聚焦条目，避免崩溃场景）
  - **Shift 范围选择**：锚点（lastSelectedPath）与游标（cursorPath）分离——Shift 连按方向键游标前进、锚点固定，范围随之扩展/收缩；列表 = 扁平序 anchor↔cursor 连续区间；网格 = (row, col) 对角线的矩形，分组头跳过（**跨分类连续**），列区间以**锚点行（基准行）**为准——游标在锚点行内时完全控制（行内收缩/扩展），游标在其他行时锚点行选区不收缩、游标只能在基准两侧扩展，短行按列区间钳制取全部（第一行 5 个、第二行 3 个时两行分别选 5/3）；鼠标 Shift+点击与键盘共用同一函数（修饰键点击不再污染双击检测）；主窗口与选择器窗口语义统一
  - **滚动控制**：越界目标经 `scrollToRow(align auto)` 最小滚动（完整可见不滚、否则只滚到恰好完整显示）；对话框焦点滚动校正（Chromium 居中滚动的修正，覆盖对话框 shadow scroller 与嵌套滚动容器）
  - **对话框键盘语义**：保持原生 Tab 序（如「打开方式」= 搜索框 → 程序列表第一项 → 取消 → 打开 → 循环，程序列表 role="listbox" + ↑/↓ 细选/Home/End/Enter）；无原生控件的行（设置主题颜色入口）补 role="button" + Tab 停靠 + Enter/Space 激活；标签页快捷键 Ctrl+Tab/Ctrl+Shift+Tab 切换、Ctrl+W 关闭、Ctrl+T 新建；布局计算与渲染完全同源（FileList 渲染期回传 `layoutRef`，`computeArrowTarget`/`computeShiftRange` 基于同一份 flattenItems 产物）；框选（覆盖模式）/取消选中/换目录后锚点与游标同步更新
- **文件预览面板**（设置 → 行为「文件预览」开关，默认关闭）：单选文件时文件区右侧「挤压」出预览区（与文件区一起随内置终端挤压，顶端与文件区平齐）；**面板常驻**——未选中条目时显示当前浏览目录的只读属性，**单选目录时显示该选中目录的属性**（回收站目录经 trashOriginalPath 显示原位置）；与右键属性对话框**共用 `PropertiesGrid` 共享网格**，预览侧 `canEditPermissions=false` 只读；`fs:get-dir-info` 轻量 IPC 只取 stat+属主（不含 du），大小单独走与对话框同一条 `getDirectorySize`（**全局单飞 + 10s 超时**——目录切换杀掉旧 du、超时/被杀显示「无法获取」，受设置「计算目录大小」开关控制，关闭显示「已禁用」），仅大小行「计算中」、其余字段秒回；trash:// 映射真实回收站目录）；分隔条拖动调整宽度（20%–60%，`settings.previewWidth` 持久化跨窗口同步）；**图片**（png/jpg/gif/webp/bmp/avif/svg/ico，`preview://` 原图）；**音频**（mp3/wav/ogg/flac/m4a/aac/opus，Chromium 原生解码）；**视频**（mp4/webm/ogg/quicktime/mkv，Range 206 分段支持 seek，编码不支持时提示失败）；**PDF**（pdfjs-dist 惰性加载独立 chunk，逐页 canvas 渲染**前 5 页**、超出时尾部显示「全文共 N 页」说明；页面宽度随预览区宽度自适应缩放，ResizeObserver 测宽 + dpr ≤ 2；**重渲染/卸载时 cancel 进行中的 render 任务**——修复画布重置后旧任务继续绘制导致的第一页黑屏/上下翻转）；**归档内容列表**（zip/jar/apk 走 `unzip -Z1`、tar 系走 `tar -tf`、7z 走 bsdtar/7z，上限 5000 条截断提示）；**Markdown 渲染**（marked + DOMPurify 消毒，**本地相对图片按文档所在目录解析为 `preview://` 绝对路径**）；**文本/代码**（512 KiB 上限，超限 TOO_LARGE 提示）；**内容变化自动刷新**——外部编辑保存后（fs:dir-changed → 重列目录带来新 mtime）文本/Markdown/PDF/归档自动重载，媒体与 PDF URL 带 `?v=mtime` 缓存戳强制重新加载；多选显示「多个文件无法预览」占位；不可预览文件/取消选择/目录/仪表盘视图隐藏；回收站（path 为 Trash/files 真实文件）与搜索结果均可预览

### 3. 文件操作

- 新建文件/文件夹（带冲突安全名）、重命名（对话框 + 行内）、**批量重命名**（查找替换/前缀/后缀/序号四种模式，实时预览 + 冲突检测；**多选右键菜单只显示「批量重命名...」（去掉单项重命名）；对话框文件顺序跟随当前排序/分组模式的视觉顺序**——列表从上到下、网格行主序）
- 复制/剪切/粘贴：跨窗口共享剪贴板（主进程持有 + 持久化到 `clipboard.json`，重启后可粘贴）
- 删除进回收站；永久删除走批量任务管线（确认框含条目数与总大小）
- 就地解压（zip/tar/gz/xz，`unzip`/`tar`）
- **压缩为 zip / tar.gz**（右键「压缩...」→ CompressDialog：归档名 + 格式选择，`zip -r`/`tar -czf` argv 安全；已存在归档返回 EXISTS 绝不覆盖；zip 缺失返回 NO_TOOL 结构化提示）
- 系统默认打开、打开方式（`.desktop` 推荐应用 + 全部应用 + 搜索过滤，`gio launch` 保环境变量）；**可执行文件直接执行（v0.11.38 扩展）**——无扩展名 + X_OK 位，或带扩展名的可执行映像（X_OK + MIME ∈ x-elf/x-executable/x-pie-executable/x-sharedlib，如 `.AppImage`）直接 spawn 执行（cwd = 文件所在目录），避免 xdg-open 无注册 handler 时按 octet-stream 交给浏览器弹「是否保存」；ENOEXEC 回退 xdg-open
- **无默认处理程序打开 → 「打开方式」对话框（v0.11.46）**：MIME 未注册系统默认应用时，双击/回车/右键「打开」不再经 xdg-open 交给浏览器弹「是否保存」——`fs:open` 经 `detectMime` + `xdg-mime query default`（3s 超时，工具缺失/检测失败 fail-open）确无默认时返回哨兵 `OPEN_NO_HANDLER`，渲染层四处打开入口（App 右键菜单 / ExplorerTab 双击与 Enter / 搜索态菜单 / 仪表盘最近访问）收到后直接弹「打开方式」对话框
- **手动默认打开方式规则（v0.11.46）**：打开方式对话框底部固定操作区（与取消/打开同排）新增「以此应用作为默认打开方式」勾选行——勾选并打开成功后按 **MIME 键**写入 `~/.config/HoshinekoFM/DefaultOpenRule/<mime转义>.json`（`electron/openRules.ts` 原子写 + 读取校验）；`fs:open` 打开文件时**规则优先于系统默认**（也优先于无 handler 检测），与「打开方式」共用 `electron/openLaunch.ts` 的 `launchWithApp` 启动链路（gio launch 优先 + Exec 字段码回退，system:open-with 同源）；所选程序已是手动默认时勾选行换「还原默认打开方式」链接——点击删除规则文件、关闭对话框并弹带遮罩提示（标题「已还原」/正文「已还原为默认打开方式」）；IPC：system:get/set/delete-open-rule（按文件路径参数，主进程检测 MIME）
- **打开方式配置管理（v0.11.47）**：设置 → 行为「打开方式配置管理」行 →「进入」打开二级对话框 `OpenRuleManagerDialog`（与设置同宽 512px/固定高 480px/带遮罩；底部提示在 actions 行与「完成」按钮同行常驻不滚动）——用户配置段（先）：标题 = 文件类型 + 常见文件后缀（`getExtensionsForMime` 由 EXT_TO_MIME 反向映射，最多 6 个加 …）、副标题 = `打开方式：exec`，悬停编辑图标、点击进内联编辑（清除配置 = 草稿清空 / 快速导入 = 打开方式对话框导入模式按 MIME 查推荐程序 / 输入为空确认 = 删除回归系统默认；`~` 前缀展开、`~file` 波浪号开头文件名保持字面量）+ 段头「清除全部用户配置」（遮罩确认）；系统配置段（后，只读）：各层 mimeapps.list `[Default Applications]` 合并（过滤 `inode/`/`x-scheme-handler/`/`x-content/`——写用户规则永不生效；目录默认程序由「默认文件管理器」行管理），悬停复制图标、点击按 DefaultOpenRule 格式写入用户配置目录，被覆盖显示 X 点击无效、无法解析 Exec（桌面文件缺失/无 Exec 行）点击无效并悬停解释——桌面文件搜索目录含 snap。**恢复默认设置不清空用户规则**（规则是数据非设置，批量清空走对话框内「清除全部」）
- **对话框 Tab 遍历滚动闪烁修复（v0.11.47）**：Chromium 对移出视口的焦点目标默认居中滚动，旧实现 fixed 20ms 只校正一次（居中位置先绘制一帧再跳回 = 闪烁）且浏览器焦点滚动会迟到回放（首段滚动后立即校正，约 50–100ms 后按原居中量再次滚动把校正顶掉）——`Dialog.tsx` 新实现：focusin 登记待校正目标 → 每个 scroll 事件 0ms 尾部 debounce 校正（渲染前完成，只绘制终态）→ 校正后保持武装 400ms（回放滚动再次触发校正，幂等）→ 滚轮/触摸/键盘（keydown 挂 window 捕获）立即放弃待校正不与用户主动滚动争夺位置（彩蛋对话框 PgDn/PgUp 翻页不受干扰；Tab 引发的 keydown 后紧跟 focusin 会重新登记）
- **已固定文件夹右键菜单（v0.11.48，e2e 65）**：右键侧边栏已固定文件夹按钮弹右键菜单，两组间分界线隔开——第一组 = 文件区文件夹右键菜单原样复用（去掉「固定到侧边栏」「解压到当前文件夹」「压缩」三项，其余条目与分界线位置不变；App 抽取 `buildItemContextMenu(item, selectedFiles, opts)`，固定项传三裁剪 + `onDeleted`）；第二组 = 上移/下移/取消固定（复用 `reorderPinnedDir`/`unpinSidebarDir`，新 i18n `sidebar.pin_move_up/down`）。联动：重命名成功（`useRenameDialog` 新增 `onRenamed` 回调）→ 固定项名称/路径同步；删除/永久删除成功（`onDeleted` 回调，失败不误毁）→ 按钮销毁
- **Places 位置区右键菜单（v0.11.48，e2e 66）**：右键侧边栏位置区条目（仪表盘/主页/下载/…/回收站）——仪表盘 = 仅「打开」；回收站 = 手写「打开 + 属性」（其余目录菜单条目对 `trash://` 虚拟路径无语义）；其余位置 = `buildItemContextMenu` 最小 IFile 复用，裁剪 `hideCopy/hideCut/hideDelete/hideDeletePermanent/hideRename/hideExtract/hideCompress`（新增裁剪项，不传 opts 行为不变），删空中段后连续 divider 由 `collapseMenuDividers` 折叠；「属性」经新增 `onProperties` 覆盖动作先 `fs:stat` 补真实 mtime/size（trash:// 改经 `fs:get-dir-info`）。后端 `fs.ts` 抽 `trashVirtualToReal` 共享给 `fs:get-dir-info` 与 `system:get-directory-size`——回收站属性大小行正常计算（此前「无法获取」）。Sidebar 新可选 prop `onPlaceContextMenu`（仅 default 变体，picker 无菜单）。无新 i18n 键
- **回收站背景右键菜单「属性」（v0.11.48，e2e 67）**：文件区回收站（trash://）空白处右键菜单由「清空回收站/刷新」扩为「清空回收站/刷新/属性」（两组分界线，与普通目录背景菜单同款语义——回收站自身属性）。App 属性补全链路统一：`handlePropertiesFile` 与位置菜单 `openPlaceProperties` 合并为共享 `openPropertiesWithStat(file)`——先 `fs:stat` 补全真实 mtime/size（背景菜单/搜索菜单构造的最小 IFile 会显示 1970），trash:// 改经 `fs:get-dir-info`，都拿不到保留传入值；大小行仍由 PropertiesGrid 经 get-directory-size（含 trash 映射）异步计算。副作用改进：普通目录背景菜单「属性」由 `mtime: new Date()` 假值改为真实 stat mtime
- 批量任务系统（copy/move/trash/delete）：进度 toast、可取消、部分失败提示
- 错误码本地化（EEXIST/ENOENT/EACCES/ENOSPC/EROFS/EISDIR/ENOTDIR/EXDEV/EBUSY 等）
- 跨设备移动（EXDEV）自动回退复制+删除；目标父目录不存在自动创建
- 外部文件拖入导入

### 4. 回收站（freedesktop Trash 规范）

- `trash://` 虚拟目录：解析 `.trashinfo`（percent 解码、删除时间），按时间倒序
- 还原（按原始目录分组处理冲突）、永久删除、清空回收站
- 监听真实 Trash 目录，外部改动自动刷新；空状态提示
- 拖入 = 移入回收站；拖出 = 还原（带确认）

### 5. 拖拽系统

- 原生 OS 拖拽（`webContents.startDrag`），外部应用（LocalSend 等）可收到真实文件
- 三种来源：窗口内部、跨窗口（主进程 claim 仲裁）、外部应用
- 落点：文件条目/列表背景/面包屑胶囊/标签页/**地址栏背景（= 复制/移动到当前目录）**；Shift 强制复制
- 冲突对话框：跳过/自动重命名/手动重命名（逐项编辑+实时校验）；「取消」选项；串行化弹窗
- Wayland 兜底：光标静止合成 drop、幻影 drop-back 抑制、单次消费仲裁

### 6. 标签页

- 多标签（新建/关闭/切换），支持虚拟路径（`app://dashboard`、`trash://`）
- 每标签独立路径/排序/分组状态；非活动标签隐藏
- 拖文件到标签页 = 移动/复制到该标签目录
- 切换回标签自动刷新（watcher 摘除期间变更不残留）

### 7. 终端

- 内置终端（xterm.js + node-pty）：任意目录打开、自适应尺寸、多窗口会话隔离、窗口关闭自动 kill
- 在系统默认终端中打开目录 / 运行可执行文件（右键菜单，含可执行位判断）
- **自定义终端**：读取 `~/.config/HoshinekoFM/terminal.conf`（`command = <终端>` 或裸命令）覆盖默认终端，优先级高于 $TERMINAL/xdg-terminal-exec 等全部检测链；参数风格按 basename 查规格表，未知终端回退通用 `-e`
- 默认终端检测链 7 级：`$TERMINAL` → `xdg-terminal-exec` → `gsettings` → `exo-open` → KDE 配置 → 常见终端扫描 → `x-terminal-emulator`；含各终端参数风格表；ghostty 单实例 cwd 问题已修复

### 8. 设备管理

- 完整设备树（`lsblk`）、挂载/卸载/弹出（`udisksctl`）
- UDISKS2 D-Bus 热插拔监听（`dbus-next`），不可用时降级轮询；事件广播所有窗口
- 弹出失败结构化错误码（`PARTITIONS_MOUNTED`）、未格式化设备提示
- `/dev` 目录块设备类型图标（isMountable/可挂载标记）

### 9. 对话框与交互

- 属性对话框：位置/大小（目录 `du` 异步计算）/修改时间/权限位（`drwxr-xr-x`）/属主（用户名:组名，回退 UID/GID）/类型；**权限编辑（chmod）**——3 位八进制输入 + 实时校验，成功后更新显示并刷新目录
- 冲突、重命名、新建名称、拖拽动作（移动/复制/取消）、打开方式、M3 确认对话框
- 右键菜单：按条目类型动态生成（文件/目录/设备/回收站/背景），批量操作作用于完整选中集，触屏长按适配
- 选择：Ctrl/Shift 多选、Ctrl+A、橡皮筋框选（replace/union/intersection/difference 四模式+边缘自动滚动）
- 快捷键：Ctrl+A / F5 / Delete / Shift+Delete / Ctrl+C / Ctrl+X / Ctrl+V / **Ctrl+滚轮（文件区图标缩放，16–128±8，与设置滑条同源）**

### 10. 主题与设置

- Matugen 主题：启动读取 `~/.config/matugen/theme.css` 注入
- **主题颜色系统**：12 预设色盘 / 自定义调色盘 / 壁纸取色（matugen + nativeImage 兜底）/ 导入 matugen / **系统主题（DMS 继承，读取 dms-colors.json 全套角色）** / **黑暗主题开关**（默认跟随系统——DMS(门户)→GNOME→KDE 检测链（niri 不负责明暗主题，不参与），fallback 暗色；开=强制暗色、关=强制亮色 M3，经 nativeTheme.themeSource 全局即时同步所有窗口；**明暗草稿即时预览**——切换/复位时仅预览卡即时切换明暗（解析注入 CSS 的明暗变量表内联覆盖预览卡），全局明暗仍在确定/应用时才切换）；**颜色草稿即时预览（仅预览卡）**——选择预设/壁纸/自定义/DMS/matugen 只经 `resolveThemeVars` 解析变量表内联覆盖预览卡，应用其余部分与所有窗口在确定/应用后才经 storage 同步全局应用（theme:preview 预览广播 IPC 已移除）；**固定预览区**——预览卡 + 明暗开关为 `.theme-color-fixed` sticky 区钉在对话框顶部，其余设置下方滚动，预览始终可见；**跟随系统经检测链显式落值（v0.11.31）**——不再用 Chromium 的 `nativeTheme.themeSource='system'`（Linux 上不读外观门户 color-scheme，DMS 暗色环境误判为亮色、与对话框预览不一致），主进程另起**系统明暗实时监听**（`gsettings monitor color-scheme` 即时 + 30s 定时重检兜底，变化广播 `theme:system-scheme-changed`，跟随模式下全窗口实时同步）
- 设置项：语言（12 种 + 跟随系统，确定时应用，跨窗口同步）、隐藏文件、视图模式、图标大小、实心图标、跑马灯开关、**界面缩放（整页缩放 50%–200%，含文件图标；确定/关闭设置后一次性生效，跨窗口实时同步、选择器同步）**、**主题设置（一级入口，二级含黑暗主题开关，见主题颜色系统）**、**文件预览开关**、**计算目录大小开关（`settings.calculateDirSize`，默认开——关闭后属性网格与删除确认不再发起 du 遍历，频繁遍历大目录对磁盘不好）**、**自动创建启动器条目开关（`settings.autoCreateDesktopEntry` 桌面 / `settings.autoCreateAppMenuEntry` 应用程序菜单，默认开，确定时生效——开并确定创建（创建一次删掉不补）、关并确定删除）**、**新建标签页目录（`settings.newTabPath`，默认 `/`——「自定义」按钮打开二级对话框输入绝对路径 / `~` 或 `~/…`（确认时展开为家目录下绝对路径，IME 全角 `～` 归一）/ app://dashboard（旧别名 dashboard:// 仍可输入）/ trash:// 或 trash://文件夹名，确认即生效）**、主页存储占用开关、默认文件管理器、系统集成、打开方式配置管理、关于（版本号 + GitHub 链接）

### 11. 国际化（i18n）

- 12 种语言：zh-CN / zh-HK / zh-CT / zh-TW / zh-AC / en-US / ja-JP / ko-KR / ko-KP / ko-CN / ru-UA / uk-UA
- 跨窗口语言同步（storage 事件）、新旧存储格式兼容迁移

### 12. 后端基础设施

- 目录列表六阶段流水线：16 并发分类 → 批量 MIME（魔数 4 级策略 + 30s TTL 缓存）→ 挂载信息富化 → passwd/group 富化
- 目录监听（inotify）按窗口注册、全局去重、300ms 防抖
- 任务系统（job:start/cancel/progress/complete，AbortController 取消）
- MIME 检测：魔数 → 扩展名表（约 300 个）→ 文件名表 → `file` 命令兜底；容器/冲突仲裁表
- 缩略图生成（ImageMagick 优先 + nativeImage 回退，磁盘缓存）：**全局队列化**——未命中缓存的生成走并发上限（默认 6，`HOSHINEKO_THUMB_CONCURRENCY` 可覆盖）+ in-flight 去重 + **世代优先 + 世代内 FIFO**（渲染侧排序/目录变化时 URL 携带 ?v=世代，新世代整体压过旧视口陈旧任务，同世代内到达序 = DOM 序 = 上→下，首屏顶部先生成）+ 队列满淘汰最旧（回退原文件服务），冷缓存大图片目录首开不再 fork 风暴/OOM；**提速**——JPEG 源 `-define jpeg:size` 按目标分辨率解码（DCT 降采样，**必须放在输入文件之前**——实测 24MP：40ms/20MB vs 160ms/297MB，4 倍速度、15 倍内存）+ 输出 q80 .jpg（缓存体积约 10 倍小），非 JPEG 源 PNG 编码级别降 1（透明无黑底）；**尺寸随图标大小动态调整**——`media://` 携带 `&s=<尺寸>`（图标 × 2 HiDPI 预留，分桶 [64, 96, 128, 192, 256]，默认 64px → 128px 替代固定 256px），各尺寸独立缓存，桶化避免滑条每格重生成；**崩溃防护**——`-limit memory/map 128MiB` 硬内存护栏（超限巨图改走磁盘缓存）、GIF 只解第一帧、convert 30s 超时、nativeImage 兜底限 25MB（宁慢不崩）
- 拖拽图标渲染缓存、`media://` 协议（缩略图）、`preview://` 协议（预览面板原图/视频/音频/PDF，`preview://localhost` 形态 + standard/corsEnabled 注册，Range 206 流式分段）、挂载表 30s 缓存
- 权限失败回退到最近可访问父目录
- **e2e 测试套件**（`scripts/e2e/`，63 套，无测试框架——Electron 驱动真实构建产物）：文件列表导航、行内重命名（React 受控输入）、右键菜单、设置对话框、主题跨窗口同步、文件选择器、界面缩放、剪贴板跨窗口、media/preview 协议、归档列表、批量重命名、终端 PTY、选择器类型过滤器与并发独立、portal FileChooser 后端（D-Bus 全链路）、自定义标题栏、默认文件管理器、FileManager1 D-Bus 接口、系统集成一键安装、搜索权限容错、方向键选择导航（网格/列表/锚点/Shift 矩形/跨分类/锚点行基准/滚动量）、目录大小计算（成功/切换杀死/超时/失败/禁用）、Markdown 预览本地图片、文件预览内容变化自动刷新、键盘分区与 Tab 循环（含仪表盘三子区/顶栏三站/崩溃压测）、选择器窗口键盘语义、对话框键盘导航（打开方式 Tab 顺序/设置主题入口/焦点滚动量）、ESC 层级关闭与键盘 Enter 语义、无后缀可执行文件直接执行、缩略图缓存统计/清除、**缩略图队列化**（e2e 34：冷缓存批量图片请求全局队列排空 + 二次缓存命中）、仪表盘最近访问、滚动条拖动与框选隔离、**选择器/保存器固定目录注入**（e2e 32：picker:open 与 portal SaveFile 两条链路 pinnedDirs 快照注入 + 侧边栏渲染/点击导航）、**选择器/保存器显示偏好注入**（e2e 33：picker:open 与 portal SaveFile 两条链路 viewPrefs 快照注入 + 列表/网格 DOM 渲染断言）、**后端总线名冲突探测**（e2e 36：同版本/旧版本/无版本/无主四态假后端探测断言）、**会话总线重启 IPC**（e2e 37：PATH 前置假 systemctl 测成败契约，绝不重启真实总线）、**保存器重名冲突**（e2e 38：portal SaveFile 重名链路——自动重命名含预览断言/覆盖/冲突弹窗取消）、**portal 版本不一致检测与一键重装**（e2e 39：未安装不弹 / 版本号文件缺失不弹 / 不一致弹开发详情 + 取消关闭 / reinstall IPC 回归 source 依赖复制 bug / reinstall.sh --user-only 幂等与假 pkexec EXIT trap 回归）、**选择器同步规则**（e2e 41：立即组/确认时组注入与实时广播 + 搜索分类强制分组按钮）、**搜索分类确定时生效与强制分组按钮**（e2e 42：改名定位 + 对话框内不生效 + 确定生效 + 强制高亮/点击无效/退出恢复）、**GUI 模式选择器设置回落**（e2e 43：关闭注入后打开即同步完整路径标题 + storage 实时跟随标题回落与搜索分类关闭）、**默认配置与恢复默认设置**（e2e 44：取消不变/确认全量默认值断言/旧预览不盖回 + 滚动文本确认生效 + 选择器语言注入与广播实时切换）、**回收站目录属性预览**（e2e 45：trash:// 映射真实回收站目录——面板标题虚拟名 + 属性网格出现 + 无「无法预览」占位）、**可执行映像打开修复**（e2e 03d：.AppImage 等带扩展名可执行文件直接执行，双击 + 右键两条链路）、**固定目录嵌套高亮**（e2e 46：A 与 A/B 同时固定打开 B 时仅 B 实心——图标实心从 startsWith 前缀判定改为精确匹配，与高亮 pill 一致）、**固定目录顺序实时同步**（e2e 47：跨窗口 localStorage 写入等价主窗口排序——主窗口实时变序 + 打开中的选择器实时跟随两次连续排序 + 主窗口条目 draggable=true / 选择器 draggable=false 只读断言）、**系统集成 kill 路径**（e2e 48：PATH 影子化假 busctl/systemctl/pkill/pgrep/xdg-mime + 沙箱 HOME/USER_BIN，不设 SKIP_SERVICE_KILL 测真实 kill 路径——总线名无主（busctl 非零退出）重装不炸回归 + 假常驻击杀验证与僵尸判定）、**彩蛋对话框**（e2e 49：设置打开期间 Ctrl+PgDn 触发——未开设置/普通 PgDn 不弹、同宽与图片固定尺寸、底部链接按钮 openExternal 地址精确断言（测试内替换 handler 录制）、打开时 0×0 焦点锚点无高亮 + 滚动置顶、Tab 选中首按钮、矮窗口 PgDn/PgUp 翻页回归）、**Ctrl+滚轮图标缩放**（e2e 50：主窗口缩放持久化键与图标宽度断言 + 打开中的选择器经 viewPrefs 广播实时跟随 + 选择器内缩放会话覆盖 + 主窗口变化清除覆盖 + GUI 模式（关闭注入）storage 双向同步——注入滚轮 deltaY 符号与页面收到的相反）、**视图模式切换按钮**（e2e 51：主窗口/选择器切换按钮翻转网格/列表断言 + 注入继承与实时跟随 + 会话覆盖与权威恢复 + GUI 模式双向同步——按钮按图标 ligature 文本定位，locale 无关）、**固定项排序拖拽不误高亮**（e2e 52：合成 DragEvent——拖拽期间对标签页/面包屑胶囊/文件夹条目派发 dragover/dragenter 断言无 `.drag-over` 类 + dragstart→dragover→drop 完整换序回归）、**选择器/保存器拒绝拖放**（e2e 53：选择器内胶囊/文件夹条目无高亮且 defaultPrevented=false、地址栏背景不被接受 + 主窗口同款合成事件仍高亮/接受回归）、**控件组折叠**（e2e 54：收起把手 → 仅「更多」按钮 + 溢出菜单执行分组/排序/展开恢复 + 折叠持久化重载保持 + 选择器/保存器继承与广播跟随 + 会话覆盖权威恢复 + GUI 模式双向同步——按钮/菜单项按图标 ligature 定位 locale 无关）、**顶栏控件组自动换行**（e2e 55：内容区变窄换行/变宽回单行实时往返 + 折叠态不换行（地址栏可压缩低于 240px）+ 回收站视图 + 选择器/保存器同款——宽度驱动全走顶栏容器 maxWidth 注入（宽 1200/窄 500，窗口宽度无关））、**自动创建启动器条目**（e2e 56：默认开启挂载创建桌面+菜单两份 .desktop（Exec 转义/图标复制同大小/chmod 755/内容断言）+ marker 幂等与手动删除后重载不重建 + 确定时生效开关往返（关闭并确定 → 实际删除文件并清 marker、重新打开并确定 → 再次创建）+ APPIMAGE 优先 + 非法 kind 拒绝（ensure/remove 两通道）——经 electron/launcherEntry.ts 共享模块同源接线，沙箱目录 + env 覆盖 exec 路径）、**自定义新标签页目录与回收站子目录虚拟路径地址栏**（e2e 57：绝对路径/dashboard 归一/trash 根/trash 子目录四类新标签页初值 + 设置对话框 UI 链路（非法输入错误态 + 确认禁用、dashboard:// 归一化为 app://dashboard）+ 回收站子目录面包屑「回收站胶囊 + 文件夹名」/编辑框 trash://虚拟路径/返回上级归一——多标签页下隐藏标签页 DOM 常驻，地址栏/文件区选择器必须限定活动标签页 wrapper）、**保存器背景右键新建文件夹**（e2e 58：保存模式背景右键菜单只含「新建文件夹」一项 + 点击 → 复用 NameInputDialog → 确认 mkdir（磁盘断言）+ 列表刷新 + 同名冲突错误态取消不创建 + 选择模式背景右键无菜单回归——菜单项是 `md-list-item` 不是 `.context-menu-item`）、**回收站子目录预览虚拟路径与终端虚拟目录回落家目录**（e2e 59：预览面板悬停标题/属性网格位置行显示 trash://… 虚拟路径且全文无 .local/share/Trash + 记录型 terminal:spawn handler 断言真实目录/仪表盘/回收站三种 cwd——虚拟目录传空串由后端回落 ~）、**缩略图缓存失效与重新缓存**（e2e 60：就地覆盖后同路径世代变化请求重新生成（缓存字节变化 + sidecar 戳更新）/重命名新 key 生成/旧路径重建重新生成/删除回退不崩溃）、**无默认处理程序打开跳转打开方式**（e2e 61：PATH 假 xdg-mime/xdg-open——有默认仍走 xdg-open（fail-open 回归）/无默认双击与右键「打开」弹打开方式对话框且不调 xdg-open）、**手动默认打开方式规则**（e2e 62：勾选「设为默认」→ 规则落盘 DefaultOpenRule + 双击走规则应用覆盖系统默认 / 所选已是默认显示「还原默认打开方式」链接 → 点击删除规则文件、关闭对话框、弹带遮罩「已还原为默认打开方式」提示、双击回落系统默认 / 规则优先于无系统默认（不弹对话框）——system:get-apps 换假应用，规则目录在沙箱 userData）、**地址栏路径语法**（e2e 63：真实目录 `..`/`.`/`./x`/`../x`/折叠解析、`~` 与 `~/.` 家目录、trash:// 上 `..` 回落 `/`、无斜杠输入与带 `~` 文件名走搜索回归——Enter 后 loadPath 异步须轮询读地址栏）；公共 `harness.cjs`（handler/协议注册与 main.ts 同步维护、zoom 感知坐标、shiftClickEl、120s 看门狗、**默认沙箱化 `HOSHINEKO_PORTALS_DIR`**——开发者机真实装有 portal 时启动版本弹窗会干扰用例），`npm run e2e` 一键运行
- **侧边栏固定项快照注入（v0.11.31）**：GUI 渲染进程经 `app:set-pinned-dirs` 上报固定项，主进程原子落盘快照 `sidebar-pinned.json`（写 GUI userData，不触碰服务模式隔离的 Local Storage LevelDB）；服务模式（`--portal`/`--filemanager1` 常驻）创建选择器/保存器窗口时从快照注入 `pinnedDirs`（每次现读，GUI 改动立即可见）；`picker:open` 调用方传入该字段被白名单忽略（不可伪造）；GUI 模式选择器读共享 session 的 localStorage 不受影响
- **侧边栏固定文件夹拖拽排序（v0.11.39）**：固定区条目 HTML5 DnD 仅排序语义——拖起后源条目隐藏本体（只显示跟随鼠标的拖拽图像，隐藏延迟到 rAF 保证浏览器先截取拖拽图像；源条目仍留 DOM 末尾 display:none 保证 ESC 取消时 dragend 正常派发清理），拖到目标条目按其**上/下半区**（±4px 死区防中线抖动）在对应位置生成**空占位按钮（虚线边界）**——上半区插前、下半区插后，悬停最后一项下半区即放到末尾，松手确认；静态列表 + 占位槽模型（列表不随拖拽移位，反馈仅占位一个元素，无实时重排的反馈回路振荡），drop 在死区内提交当前显示的占位间隙（所见即所得）；位置/设备/添加按钮/文件列表等其他区域不接收（落下回弹）；顺序经 `sidebar.pinned` 持久化并复用同步通道（主窗口间 storage 事件实时同步；快照广播使打开中的选择器/保存器实时跟随，含连续排序）；**选择器/保存器固定区只读**（draggable 关闭、无移除按钮，不允许改变顺序）。e2e 47 覆盖
- **选择器显示偏好快照注入（v0.11.31）**：与固定项同一机制——GUI 经 `app:set-picker-view-prefs` 上报只读显示偏好（视图模式网格/列表、图标大小、隐藏文件、实心图标、跑马灯）落盘 `picker-prefs.json`，服务模式选择器/保存器注入 `viewPrefs` 跟随主窗口视图模式（不注入则永远停在默认网格）；排序/分组不在快照内（选择器内可调、常驻进程自行持久）；调用方传入一律丢弃
- **选择器同步规则（v0.11.33，见 同步规则.md）**：`picker-prefs.json` 扩展**分类/排序**（sortBy/sortOrder/groupingEnabled）——立即同步组，主窗口一变即广播（App.tsx 立即上报；FilePicker 注入值优先、主窗口为权威）；新增 `picker-settings.json` + `app:set-picker-settings`（**确认时同步组**：搜索分类 searchGroupByDir、**标题栏完整路径 showFullPathTitle**——设置按下确定/退出时上报）+ `pickerConfig.settings` 注入 + `picker:settings-changed` 广播；FilePicker 搜索态 `searchActive` + `groupByDir` 支持搜索分类；**选择器搜索对齐主窗口**（结果计数 + 清除 + 类型/最小/最大大小筛选器，同一条 system:search）；**选择器标题栏完整路径**（「选择文件夹：完整目录」等）。e2e 41 覆盖两条链路；e2e 26/33 改全字段上报（真实 App 挂载即上报默认 viewPrefs）
- **搜索分类确定时生效 + 搜索态强制分组按钮（v0.11.34）**：设置项改名「搜索结果按所在目录分类」（全部语言同步）；搜索分类开关与标题栏完整路径同款 pending 机制——设置对话框内切换只改预览、按下确定/退出才生效（此前开关即改），生效经 `app:set-picker-settings` 同链路同步到服务模式选择器/保存器；搜索分类开启且搜索页面打开时右上角分类按钮**强制高亮（filled）且点击无效**（SortControls 新增 `groupingForced`，onClick 置空而非 `disabled`——禁用态会置灰，与强制高亮矛盾），退出搜索后恢复可点；主窗口与选择器/保存器一致。e2e 42 覆盖（改名定位 + 对话框内不生效 + Escape 确定生效 + 强制高亮/点击无效/退出恢复），e2e 41 扩展选择器侧断言
- **选择器/保存器 GUI 模式设置回落（v0.11.34）**：真实 createWindow 只在服务模式注入快照，GUI 模式选择器此前对 searchGroupByDir/showFullPathTitle 无 localStorage 回落——标题栏完整路径在 GUI 模式选择器/保存器完全不显示（标题栏同步失效根因）；FilePicker 新增 `localSearchGroupByDir`/`localShowFullPathTitle`（useLocalStorage 同键回落，注入/广播值优先）——**新窗口创建时打开即同步当前设置** + storage 事件实时跟随；harness 注入加开关 `setPickerSnapshotInjection(false)` 模拟 GUI 模式、`pickerConfigByWindow.set` 移出注入块无条件登记（与 main.ts 一致）。e2e 43 覆盖
- **默认配置 + 恢复默认设置 + 选择器本地化（v0.11.34）**：**首次使用默认值**——语言跟随系统（auto）、显示隐藏文件开、列表模式、图标 48px、UI 100%、实心图标关、主题颜色与明暗跟随系统、标题栏跟随系统、完整路径关、滚动文本关、搜索分类开、home 占用关、文件预览关、计算目录大小开（App.tsx 与 FilePicker 回落默认同步：viewMode 'list' / iconSize 48 / marqueeEnabled false）；**恢复默认设置**——设置底部「默认配置」区域（行为底部分界线 + 关于分界线上方）按钮 → 带遮罩 ConfirmDialog（确认/取消）→ App `handleRestoreDefaults` 全部重置（setter 触发跨窗口同步 + 快照自动上报），对话框内旧预览经 props 同步 effect 重置不被盖回；**滚动文本/文件预览开关确认时生效**（pending 机制）；**选择器本地化**——标题渲染期派生（语言切换实时跟随）、pickerSettings 新增 locale（确认时同步组：sanitize 'auto'/xx-XX 可选字段 + 快照注入 + 广播，服务模式选择器应用 GUI 语言），12 个语言文件补齐新键并核对 308 个源码用键无缺失。e2e 44 覆盖；e2e 15/20/29 适配跑马灯新默认
- **主题快照注入（v0.11.33）**：确认主题后颜色同步到服务模式选择器/保存器——GUI 经 `app:set-theme-snapshot` 上报 `settings.theme` + `settings.darkMode`（sanitize：kind 白名单 + 字段类型校验，非法整体丢弃）落盘 `theme-snapshot.json`，服务模式创建选择器/保存器窗口时注入 `pickerConfig.theme`（现读，GUI 改动下次弹窗即生效）；FilePicker 优先取注入值、GUI 模式回落共享 session 的 localStorage（storage 事件同步）；明暗经注入 darkMode 调 `theme:set-source`（nativeTheme 进程级）；实时继承——常驻进程快照监听扩展第三文件，变化广播 `picker:theme-changed`，打开中的选择器/保存器不重开窗口即切换；调用方传入 theme 字段被白名单忽略。e2e 40 覆盖注入/实时广播/非法丢弃
- **快照实时监听（v0.11.31）**：服务模式常驻进程 `fs.watch`（inotify）监听 GUI userData 目录（目录级——GUI 是 tmp+rename 原子写，文件级 watch 在 rename 后失联；300ms 防抖 + 内容对比过滤 LevelDB 噪声），快照变化即广播 `picker:view-prefs-changed` / `picker:pinned-dirs-changed` 到所有打开中的选择器/保存器窗口——打开中实时切换视图模式、固定目录即时增删；独立生命周期（不受 window-all-closed 的 stopAllWatching 影响，退出时清理，watch 出错 5s 重试）
- **系统明暗实时监听（v0.11.31）**：`gsettings monitor color-scheme`（DMS/GNOME 即时，300ms 去抖）+ 30s 定时重检兜底（KDE 等），变化广播 `theme:system-scheme-changed`；仅 GUI 模式启动、退出时清理子进程与定时器
- **内置文件选择器通信层**（`docs/picker-api.md`）：`picker:open` 支持 mode + `filters`（类型过滤器：extensions/mimes、主进程白名单校验 + resolvedMime 解析）/ `initialPath` / `defaultFilterId`；底部类型下拉**常驻**（无声明仅「所有文件」；位于路径提示右侧、宽度自适应），与设置语言选择同款 OutlinedSelect；缺省 label 由 mime 描述体系生成 i18n 名；过滤只约束文件可选中性、目录不受约束、切换清除失效选中；**并发独立**（按窗口隔离，多选择器互不影响）；**保存模式重名冲突（v0.11.33）**——确认时 `existsBatch` 检测目标是否存在，重名弹与复制/移动同款冲突对话框（覆盖/自动重命名/手动重命名 + 自动重命名「原名 → 新名」预览），resolvePicker 只回传最终路径
- **xdg-desktop-portal FileChooser 后端**（外部程序标准入口，`docs/portal-filechooser.md`）：实现 `org.freedesktop.impl.portal.FileChooser`（总线名 `org.freedesktop.impl.portal.desktop.hoshineko`），portal 请求（directory/multiple/filters/current_filter）翻译为同一 PickerConfig 复用同一窗口工厂；Request.Close 取消、Response 信号回传 file:// URI；**SaveFile 保存对话框已支持**（current_name/current_file/current_folder/accept_label → 保存模式选择器，**v0.11.33 起重名时先弹覆盖/自动重命名/手动重命名冲突对话框**）；多文件 SaveFiles 返回 NotSupported；`--portal` 参数仅服务不建主窗口；packaging 提供 .portal 与 D-Bus .service 安装文件
- **后端总线名冲突诊断（v0.11.33，方案 B）**：两个 D-Bus 后端对象暴露只读 `Version` 属性（`electron/handlers/backendInfo.ts` 定义常量与探测函数）；注册名失败时 main.ts 经 `startBackendConflictQuery` 探测占名者版本——`outdated`（旧版常驻，建议卸载重装）/ `noVersion`（更旧构建）/ `unresponsive`（僵尸占名，进程已死但连接未释放）/ `sameVersion`（同版本另一实例，正常）；渲染进程经 `system:get-backend-conflicts` 取报告在设置页「系统集成」行常驻展示 + **带遮罩警告弹窗**（AlertDialog，每次会话一次；**版本不一致时抑制**——只弹版本弹窗，重装一并解决冲突）
- **会话总线重启（v0.11.33）**：设置页**常驻**「重启会话总线」按钮——`system:restart-session-bus` 依次尝试 `systemctl --user restart dbus-broker.service` / `dbus.service`（30s 超时），成功后延迟 2.5s 重新注册后端并作废冲突缓存；portal/fm1 后端挂 error 监听防总线断开时主进程崩溃；**移除 usocket**（Electron 43 下 use-after-free/退出挂起，僵尸占名根因之一，dbus-next 回退 net.Socket）；集成脚本 systemctl restart 改 `--no-block` + timeout 双保险防挂起
- **默认文件管理器（MIME 关联，设置 → 行为）**：`xdg-mime default HoshinekoFM.desktop inode/directory`（写用户级 mimeapps.list，用户级桌面入口自动安装、Exec 按 AppImage/开发环境自适应）；「设为默认 / 恢复为系统默认」+ 状态副标题；只关联目录不抢占文件类型；xdg-open/gio open 传目录即开新窗口导航（复用单实例锁 + 启动路径）
- **org.freedesktop.FileManager1 D-Bus 接口**：第三方程序（DMS/IDE/下载管理器「在文件管理器中显示」等）标准入口——OpenFolders/ShowFolders（开窗口）、ShowItems（打开目录 + 定位选中，**大目录目标在首屏之外时文件区滚动到该条目**——FileList scrollToRow 冷缓存竞态修复，见 v0.11.45.14）、ShowItemProperties（+ 属性对话框）；名称被占时静默降级；`--filemanager1` 服务模式 + D-Bus 激活文件；URI 白名单
- **系统集成一键安装**（设置 → 行为「系统集成」）：`scripts/system-integration/install.sh`（幂等）——root 级（portal 配置 + D-Bus 激活文件 + **版本号文件 `hoshineko.version`**（值 = `app.getVersion()`，经 `runIntegrationScript` 注入 `HOSHINEKO_VERSION` 并显式透传 pkexec），pkexec 授权）+ 用户级（portals.conf preferred 项、xdg-mime 关联、portal 服务重启）；设置副标题说明覆盖范围与手动步骤（Firefox about:config）；安装状态检测；`HOSHINEKO_PORTALS_DIR` 覆盖 portal 目录（沙箱/e2e 用，三脚本与 system.ts 读取一致）
- **portal 版本不一致启动检查与一键重装（v0.11.33）**：`system:get-portal-runtime-info` 聚合版本文件对比、集成状态、后端注册结果（backends.ts `getLastBackendRegistration`）与冲突报告——portal 已安装且版本号文件 ≠ 当前版本 → 弹 `PortalVersionDialog`（每次会话一次，取消不记忆每次启动弹；**版本号文件缺失不弹**，视为旧流程残留/不完整安装；未安装不弹）；打包版双按钮（取消 / 一键重装，正文含重启生效提示），开发版仅取消 + 运行时诊断详情（打包版弹窗内按 PgDn 切换开发详情）；一键重装执行 `scripts/system-integration/reinstall.sh`——source install.sh/uninstall.sh 复用函数（两脚本主入口 `BASH_SOURCE` 守卫可被 source），卸载 + 安装合并为**单次 pkexec 授权**，root 级先卸载后安装原子完成；`runIntegrationScript` 跑 reinstall.sh 时把两个依赖脚本一并复制到临时目录
- **portal 相关操作结果弹窗化（v0.11.33）**：系统集成安装/卸载、一键重装、会话总线重启的成功与失败从 toast 改为**带遮罩 AlertDialog**（`portalNotice`，错误细节截尾 400 字符进正文；会话总线重启成功正文为空）——portal 故障属需用户明确知晓级别，toast 易被忽略；默认文件管理器/缩略图缓存等非 portal 项仍走 toast
- **平铺 WM 最小化修复 + 回收站目录预览 + 标题栏/侧边栏细节（v0.11.38）**：平铺 WM 下**隐藏最小化入口**（`TitleBar hideMinimize`：右侧按钮 + v 菜单项，主窗口/选择器/保存器一致，检测失败 fallback 显示）+ **主进程 `window:minimize` no-op 兜底**（`detectWindowManager` 导出复用，绕过 UI 的直接 IPC 调用也不会把窗口送进无法恢复的状态）；**回收站目录属性预览**（`fs:get-dir-info` 校验放行 trash://——此前 `startsWith('/')` 校验先于映射执行使映射成死代码，切回收站显示「无法预览」；现映射真实回收站 files 目录后 stat，面板展示真实属性、大小经真实路径 du，e2e 45）；**v 菜单最大化项随状态切换**（最大化时显示「还原」+ 还原图标，与右侧按钮语义一致）；**标题栏开关三态逻辑修正**（md-switch 纯展示化 + 外层 `role=switch` 容器接管 + 函数式更新，跟随模式点开关一次即退出跟随并切换——此前内部 checkbox 状态机与受控赋值竞争「点两次才生效」，语义写入 `docs/跟随系统设置项目逻辑.md`）；**可执行映像打开修复**（`fs:open`：带扩展名的可执行文件（.AppImage 等，X_OK + MIME 可执行类型）与无扩展名可执行文件同语义直接执行——此前 xdg-open 无 handler 按 octet-stream 交给浏览器弹「是否保存」，e2e 03d）；**侧边栏区块间隔**（非首区块标题上方 16px，首区块贴顶与添加固定按钮 4px 间距不受影响）；**固定目录嵌套高亮修复**（固定项图标实心从 startsWith 前缀判定改为精确匹配——嵌套固定时只有当前打开项实心，与高亮 pill 一致，e2e 46）。e2e 15 改造（平铺两按钮/v 菜单两条目/minimize no-op + GNOME 环境覆盖堆叠分支三按钮全链路 + 跟随模式真实指针点击回归）；e2e 03/24/27/32/44/45/46 回归通过
- **侧边栏固定文件夹拖拽排序（v0.11.39）**：固定区条目 HTML5 DnD 仅排序语义——**源条目隐藏本体**（只显示跟随鼠标的拖拽图像；隐藏延迟到 rAF 保证浏览器先截取拖拽图像否则拖影空白，`pinReorderActiveRef` 守卫起拖瞬间松手竞态；源条目仍留 DOM 末尾 display:none 保证 ESC 取消时 dragend 正常派发清理），拖到目标条目按**上/下半区**（分界线 ±4px 死区防中线抖动翻转，drop 在死区内提交当前显示的占位间隙所见即所得）在对应位置生成**空占位按钮（虚线边界 + 淡入动画）**——上半区插前、下半区插后，悬停最后一项下半区放到末尾；**静态列表 + 占位槽模型**（列表不随拖拽移位、反馈仅占位一个元素，取代早期「实时重排」版本——幽灵条目占槽 + 硬阈值半区判定 + 重排反馈回路导致频繁闪动与振荡）；从 × 移除按钮起拖被排除；位置/设备/添加按钮/文件列表等其他区域不接收（落下回弹）；**顺序持久化与同步**（App `reorderPinnedDir` splice 移动 → `sidebar.pinned` localStorage → 主窗口间 storage 事件实时同步 + `app:set-pinned-dirs` 快照上报 → watcher 广播使打开中的选择器/保存器实时跟随）；**选择器/保存器固定区只读**（draggable 关闭、无移除按钮）。e2e 47（跨窗口 localStorage 写入等价排序——主窗口实时变序 + 打开中选择器连续两次排序实时跟随 + draggable true/false 断言）；e2e 25/32/46 回归通过
- **一键重装失败修复（v0.11.39）**：`bus_owner_pid` 里 `timeout 5 busctl --user status | awk` 在 `set -o pipefail` 下，busctl 对**无主总线名**以非零退出（dbus-broker：Failed to get credentials: No such device or address）——管线失败经命令替换 `pid="$(bus_owner_pid …)"` 在 `set -e` 下炸掉整个安装流程（实测：版本更新后首次真实重装——第一次清理击杀旧常驻成功、第二次清理时名字已无主当场死，重试永远停在「[user] 清理 portal/FileManager1 常驻进程」，root 级文件已装但被报安装失败）。修复：`bus_owner_pid` 末行 `|| true` 吞掉无主退出码（无主是正常状态，输出空 → continue）；`kill_pid_escalate` 加固——新增 `proc_dead`（kill -0 失败**或** /proc/pid/status State=Z 僵尸）判死（僵尸 SIGKILL 无效、父进程未收割时曾误报「仍存活」致失败），TERM 送达失败时区分 ESRCH 竞态（已自行退出视为清理）与 EPERM（真失败）。e2e 48（PATH 影子化假 busctl/systemctl/pkill/pgrep/xdg-mime + 沙箱 HOME/USER_BIN 测真实 kill 路径：无主名字回归 + 假常驻 `exec -a 'HoshinekoFM --portal' sleep 300` 击杀验证）；e2e 39/18/37 回归通过
- **加入彩蛋（v0.11.40）**：设置对话框打开期间按 **Ctrl+PgDn** 弹出「Hoshineko Nya~」对话框（M3 样式 + 叠层遮罩，与设置同宽 560px）——标题固定不随语言；内容为 HoshinekoAkihara.png 与 Transgender Pride 旗纵向排列（固定 293px 宽锁定渲染尺寸）；底部分割线 + 贡献声明 + 「一些链接」outlined 按钮横向排列（HoshinekoFM讨论群/星奈的频道/Project Trans 经 `shell:open-external` 用系统浏览器打开，文案为专有名词不随语言）；确定按钮关闭。交互细节：内容溢出时 PgDn/PgUp 翻页滚动（焦点在 scroller 外时拦截手动滚动 shadow scroller）；**打开时无高亮且滚动置顶**（autofocus 目标 = 内容顶部 0×0 焦点锚点——原生 showModal 强制聚焦首按钮会产生 :focus-visible 高亮环，锚点承接初始焦点、不进 Tab 序，Tab 后才选中首按钮；高内容容器的聚焦滚动校正不再滚到底部）；普通 PgDn 不触发、portal 版本弹窗打开期间不生效。i18n 新增 `dialog.button.ok`/`nya.contribution`/`nya.links`（12 语言文件）。e2e 49 覆盖；e2e 04/27/39 回归通过
- **Ctrl+滚轮图标缩放 + 右上角视图模式切换按钮（v0.11.41）**：**Ctrl+滚轮缩放**——光标在文件区（`.file-list-container`）上按 Ctrl+滚轮按设置滑条同范围/步进（16–128px±8）调整图标大小，范围/步进常量抽到 `src/utils/iconZoom.ts`（SettingsDialog 滑条共用）；主窗口走 App.tsx 窗口级 wheel handler（并入既有菜单/对话框拦截 effect，`{ passive: false }` 阻止默认滚动——React 受控 onWheel 是 passive 的无效），写 `settings.iconSize`（立即同步组）：GUI 模式选择器经共享 session 的 storage 事件实时跟随、服务模式选择器/保存器经 viewPrefs 快照广播实时跟随；选择器/保存器内缩放同样生效——GUI 模式写共享 localStorage **双向**同步，服务模式（userData 隔离、注入值优先）经 `iconSizeOverride` **会话覆盖**立即生效，收到 `picker:view-prefs-changed` 广播即清除覆盖（主窗口为权威）；对话框/右键菜单打开时不响应。**视图模式切换按钮**——SortControls（主窗口/选择器/保存器三处共用）分类开关右侧新增**单个切换按钮**（图标/tooltip 显示切换目标：列表模式显示网格图标），点击翻转网格/列表，与设置同键 `settings.viewMode`（立即同步组，跨窗口/快照广播自动生效）；选择器内切换走 `viewModeOverride` 会话覆盖（与图标缩放同款语义：GUI 模式只写共享 localStorage 双向同步，服务模式注入优先时写覆盖、收到广播即清除）；键盘分区 TOP_BAR_BTN_SELECTOR 是通用选择器，新增按钮自动进方向键循环。i18n 新增 `sort.switch_to_grid`/`sort.switch_to_list`（12 语言文件）。e2e 50（主窗口缩放持久化键与图标宽度断言 + 打开中的选择器实时跟随 + 选择器内缩放会话覆盖 + 主窗口变化覆盖 + GUI 模式双向同步）、e2e 51（主窗口/选择器切换按钮翻转断言 + 注入继承与实时跟随 + 会话覆盖与权威恢复 + GUI 模式双向同步，按钮按图标 ligature 文本定位 locale 无关）；全套 51 套通过
- **固定项排序拖拽误导高亮修复 + 选择器/保存器拒绝拖放（v0.11.42）**：**主窗口误高亮修复**——固定项排序拖拽是仅排序语义的纯 HTML5 会话（text/plain 源索引，不设 DragContext、不 startDrag），文件落点处理器此前不区分拖拽类型，拖动固定项经过文件区文件夹条目/地址栏胶囊出现「可放置」误导高亮、地址栏背景/文件区背景/终端给光标提示、标签页残留陈旧 dragState 时误高亮。修复：`src/utils/pinReorderDrag.ts` 全局标志（Sidebar dragstart 置 true、dragend 置 false——规范保证 dragend 必派发），FileList 文件夹 dragover/drop、Breadcrumbs 胶囊 dragenter、addressBarDrop、ExplorerTab 文件区容器、TabBar dragover/drop、TerminalPane 六处守卫早退（不 preventDefault → 无高亮无光标提示），排序链路本身不受影响。**选择器/保存器拒绝拖放**——拖动任何可拖动控件（固定项/文件条目/外部文件）经过选择器窗口时文件区文件夹条目与地址栏胶囊同样误导高亮：FileList 文件夹落点按 `onDropOnFolder` 是否存在门控（选择器不传）；Omnibar/Breadcrumbs 的 `onDropFiles`/`onDropExternalFiles` 改为**可选**（选择器不传 → addressBarDrop/dropHandler 为 null → dragover/dragenter/drop 全早退），主窗口两处照传不受影响。e2e 52（合成 DragEvent 可触发 React 拖拽处理器——拖拽期间对标签页/面包屑/文件夹条目断言无 `.drag-over` + 完整换序回归）、e2e 53（选择器内无高亮且 defaultPrevented=false + 主窗口同款事件仍接受回归）；全套 53 套通过
- **右上角控件组折叠（v0.11.43）**：SortControls（主窗口/选择器/保存器三处共用）支持收起/展开——展开态五按钮右端「收起」把手（chevron_right 指向折叠方向，与排序键间分隔线）；折叠态仅「更多」按钮（tune，**仅搜索强制分组时 filled**——分组开关自身状态不改变按钮高亮，避免「更多」被误读为激活态）+ `md-menu` 溢出菜单（popover 锚定按钮，6 项：分组（勾选标记，搜索强制分组时禁用）/视图切换/三项排序带勾选+升降箭头/展开控件）——折叠后无需展开即可完成全部操作。折叠状态持久化 `settings.sortControlsCollapsed`（立即同步组，恢复默认设置重置）：`PickerViewPrefs` 扩展 `sortControlsCollapsed`（sanitize 可选向后兼容旧快照缺省 false；main.ts / harness 手工副本 / electron/handlers/picker.ts 同源类型三处同步），选择器/保存器经快照注入继承 + `picker:view-prefs-changed` 广播跟随，选择器内切换走 `sortControlsCollapsedOverride` 会话覆盖（与 viewModeOverride 同款语义：GUI 模式只写共享 localStorage 双向同步、服务模式收到广播即清除，主窗口为权威）。`md/index.ts` 的 Menu 包装补 events（onOpened/onClosed——lit-react 不自动映射事件）；**菜单项键盘激活**——md-menu-item 键盘 Enter/Space 只派发 close-menu 不合成 click（MenuItemController.onKeydown），动作经 `onCloseMenu`（MenuItem 包装显式声明事件）按 reason.kind='keydown'+reason.key 过滤执行（Escape 忽略）、鼠标点击仍走 onClick；键盘分区 TOP_BAR_BTN_SELECTOR 通用选择器自动纳入把手/更多按钮。i18n 新增 `sort.collapse`/`sort.more`/`sort.expand`/`sort.grouping`（12 语言文件）。e2e 54 覆盖（主窗口折叠/菜单操作/展开/重载持久化 + 选择器继承与广播跟随 + 会话覆盖权威恢复 + GUI 模式双向同步 + 保存器同款 + 分组切换不改「更多」按钮高亮、菜单分组项勾选跟随 + **键盘激活**（Enter/Down+Enter/同字段翻转/Escape 不执行动作——打开动画后焦点才落首项、关闭动画未收尾重开不落焦点两坑）；e2e 25/26/41/42/51 回归通过。**顶栏控件组自动换行（v0.11.43）**：地址栏压缩过度（低于 `OMNIBAR_MIN_WIDTH_EXPANDED`=240px）且展开态时右上角控件组自动换到第二行（margin-left auto 右对齐）、地址栏独占第一行，折叠态地址栏 min-width 0 永不换行——纯 flex（顶栏容器 flex-wrap + 地址栏分区条件 min-width），窗口/内容区宽度变化实时往返无 JS 状态；主窗口顶栏与选择器/保存器 picker-topbar 同款。**换行底部分界线**：`useTopBarWrap`（hooks/）ResizeObserver 实时检测换行（仅驱动装饰样式，常驻 1px 占位、换行着色/未换行透明防跳动，换行时分界线下移 8px；选择器 config 异步到达经 `active` 依赖重挂观察器）。溢出菜单条目紧凑化（40px 与右键菜单同款）、「更多」按钮仅搜索强制分组时 filled、菜单分组项文案「分组」（sort.grouping）。e2e 55 覆盖（内容区宽窄实时往返 + 折叠态压缩不换行 + 回收站 + 选择器/保存器 + 分界线随换行着色/回单行消失；平铺 WM 下 win.setSize 无效，宽度驱动经侧边栏隐藏与顶栏 maxWidth 注入）

- **自动创建启动器条目（v0.11.44）**：两个独立开关（设置 → 行为，默认开启，**确定时生效**——SettingsDialog pending 机制，对话框内切换只改预览）——`settings.autoCreateDesktopEntry`（桌面快捷方式）/`settings.autoCreateAppMenuEntry`（应用程序菜单条目）。语义：**打开并确定 → 创建**（首次挂载按当前开关值创建，默认开 → 首启即建；主进程 marker（userData `launcher-entries.json`）保证「创建一次，删掉不补」——手动删文件不重建、已存在（系统集成装过/用户自建）绝不覆盖只补 marker）；**关闭并确定 → 删除条目并清 marker**（`app:remove-launcher-entry`；重新打开并确定 = 新创建意图，可再建）；恢复默认设置补一次创建意图；删除只发生在确定操作的窗口（其他窗口经 storage 同步后 effect 只走 ensure 幂等路径）。核心逻辑抽成 `electron/launcherEntry.ts` 共享模块（main.ts 与 e2e harness 同一代码路径无手工副本，`ensureLauncherEntry`/`removeLauncherEntry` 两入口共用串行链）：.desktop 内容生成（Exec 优先 `APPIMAGE` 回落进程路径，桌面条目规范转义 + `%U`；`StartupWMClass=HoshinekoFM` 与 productName 对齐）、图标复制到 `~/.local/share/icons/hicolor/scalable/hoshineko-fm.svg` 稳定落点（SVG 矢量源 `src/icon.svg` 经 extraResources 打包；AppImage 挂载路径每次随机，Icon= 不能指向挂载内文件）、`chmod 755` + `gio metadata::trusted`（GNOME 双击信任 best-effort）；marker 读-改-写竞态经进程内 promise 串行链修复（desktop/appmenu 两路调用并行时后写者整体替换丢另一 kind 标志，e2e 56 首跑即复现）；桌面目录走 `app.getPath('desktop')`（XDG 解析，中文桌面目录可用，异常/空回落 `~/Desktop`）。i18n 新增 4 键（12 语言文件）。e2e 56 覆盖（默认开启挂载创建两份条目——Exec 转义/图标复制/chmod/内容断言 + marker 幂等与手动删除后重载不重建 + 确定时生效开关往返——关闭并确定实际删除文件并清 marker、重新打开并确定再次创建 + APPIMAGE 优先 + 非法 kind 拒绝两通道）；e2e 04 补 waitDialogAnim（Wayland 软件渲染下对话框开启动画未完成时点击落在错位坐标的间歇失败，8/8 稳定）；回归 04/15/33/43/44/55 通过

- **标题栏 v 菜单「新建窗口」（v0.11.45）**：v 菜单顶部新增「新建窗口」项（`open_in_new` 图标）——点击后菜单由 ContextMenu 统一关闭，主进程创建新窗口（与 second-instance「再次启动开新窗口」共用同一 `createWindow` 工厂，同进程共享后端；新窗口无启动路径，打开于仪表盘）。菜单结构：新建窗口 /（分隔线）/ 最大化（最大化时切「还原」）/ 最小化（平铺 WM 隐藏）/（分隔线）/ 退出——平铺三条目（图标字号 18/18/22）、堆叠四条目（18/18/22/22），与右侧按钮字号同源。IPC 接线：`registerWindowHandlers` 新增第二个可选参数 `createNewWindow` 工厂（`createWindow` 定义在 main.ts，无法在 window.ts 直接引用）——main.ts 注入 `() => void createWindow()`，工厂未注入时不注册 `window:new`（e2e harness 注入 `createTestWindow` 同款接线）；preload 暴露 `newWindow()`、`electron.d.ts` 同步声明。i18n 新增 `window.new`（12 语言文件）。e2e 15 按新结构更新（平铺三条目 18/18/22、堆叠四条目，「还原」断言改按第二项）并新增「新建窗口」点击回归——点击后菜单关闭 + 第二窗口打开于仪表盘标题

- **自定义新标签页目录 + 回收站子目录虚拟路径地址栏（v0.11.45.14）**：
  **自定义新标签页目录**——设置 → 行为新增「新建标签页目录」行
  （副标题常驻展示当前值）→「自定义」按钮打开二级对话框
  （`NewTabPathDialog`，样式与重命名对话框一致：OutlinedTextField +
  提示行，**带背景遮罩**——Dialog `backdrop` prop 叠在设置对话框之上）
  ——输入绝对路径（`/` 开头）、`~` / `~/…`（确认时经 `getHomePath`
  展开为家目录下绝对路径存储；IME 全角 `～` 前缀归一为 `~`）、
  `app://dashboard`（打开仪表盘，提示与展示形态；旧别名
  `dashboard://` 仍可输入，归一化为内部形态 `app://dashboard`）或
  `trash://`（回收站本身，合法）/ `trash://文件夹名`（打开回收站/
  回收站中的目录）；校验规则在 `src/utils/newTabPath.ts`（绝对 /
  `~` 前缀 / dashboard 别名 / trash 前缀 + 相对段
  拒绝 `.`/`..`/空段防止逃逸），非法输入显示错误态 + 确认禁用，
  不校验目录存在性（不存在由新标签页 loadPath 报错，与地址栏输入
  同语义）；确认即生效（对话框本身即草稿机制，无 pending 开关），
  持久化 `settings.newTabPath`（默认 `/`，恢复默认设置重置）。
  新建标签页三入口（TabBar + 按钮 / Ctrl+T / 空状态按钮）统一从该键
  取初值。
  **回收站子目录虚拟路径地址栏（混合路径模型）**——打开回收站中的
  文件夹后地址栏不再显示真实 `Trash/files` 绝对路径：面包屑渲染
  「回收站胶囊 + 条目相对段」（`trash://文件夹名` 形态），点击编辑
  按钮时输入框显示 `trash://文件夹名` 虚拟路径。实现：currentPath 仍
  保持真实路径（文件操作/搜索/监听全部照常），仅显示换算虚拟
  ——`src/utils/trashPath.ts` 提供虚拟↔真实互转；ExplorerTab 的
  `displayPath` 换算喂给 Omnibar/Breadcrumbs，`loadPath` 解析
  `trash://…` 虚拟路径（trashRoot 异步兜底重拉）、把回收站 files 根
  归一化为 trash:// 视图（从子目录返回上级时清空/还原等回收站语义
  不丢）、支持 `dashboard://` 别名；Breadcrumbs 回收站分支渲染
  胶囊 + 相对段（拖放落点经 ExplorerTab `resolveDropTarget` 换算真实
  路径）；Omnibar 跳过虚拟路径的软链接检测（渲染期复位检测结果）。
  i18n 新增 `settings.new_tab_path`/`settings.new_tab_path_edit`/
   `newtab.title`/`newtab.hint`/`newtab.invalid`（12 语言文件）。e2e 57
   覆盖（绝对路径/`~` 与 `~/xxx` 家目录展开（含全角 `～` 合法态）/
   dashboard 归一/trash 根/trash 子目录五类新标签页
   初值 + 设置对话框 UI 链路（背景遮罩注入 + 非法输入错误态与确认
   禁用 + trash:// 本身合法 + dashboard 归一）+ 回收站子目录面包屑/
   编辑框/返回上级虚拟语义——**坑**：多标签页时隐藏标签页 DOM 常驻，
   地址栏/文件区选择器必须限定在活动标签页 wrapper（`.content-area`
   下 `display!=='none'` 的子 div）内）；e2e 44 补 `settings.newTabPath`
   恢复默认断言；回归 04/06/15/20/26/31/41/42/43/44/45/52/53/55 通过。
  **保存器背景右键「新建文件夹」**——仅保存模式（`config.mode ===
  'save'`）文件区空白处右键弹背景菜单，**只含「新建文件夹」一项**
  （无新建文件/粘贴/属性——文件名走底部输入框，其余无语义）；
  选择模式背景右键无任何菜单（handler 内 `isSave` 守卫早退，与
  「选择器不接收拖放」同源）。点击菜单项 → 复用主窗口的
  `NameInputDialog`（`isDir` 确认补尾斜杠、同名冲突校验/安全名同源）；
  确认后 `createDirectory` mkdir + 重载当前目录（列表出现新文件夹）。
  FileList 的 `onBackgroundContextMenu` 是可选 prop——选择器此前不传
  （无背景菜单），现在保存模式传入该 handler。e2e 58 覆盖（菜单只含
  一项且文案「新建文件夹」+ 点击 → 对话框 → 确认创建磁盘断言 + 列表
  刷新 + 同名冲突错误态取消不创建 + 选择模式无菜单回归——**坑**：
  菜单项是 `md-list-item` 不是 `.context-menu-item`）；回归
  06/38/41/43 通过。**回收站子目录预览虚拟路径 + 终端虚拟目录回落
  家目录**——预览面板预览回收站内目录时不再暴露真实 Trash/files
  路径：ExplorerTab `previewState` 对 files 目录下的路径经
  `realToTrashVirtual` 换算为 trash://…（与地址栏同款，trashRoot
  依赖），后端 `fs:get-dir-info` 接受 `trash://…` 虚拟路径（含子目录）
  映射到真实 files 子目录（相对段剔除 `.`/`..`/空段防逃逸，与前端
  trashPath 同语义）——面板名称仍为文件夹名，悬停标题/属性网格位置
  行为虚拟路径；终端回落——App 派生 `isVirtualTerminalDir`
  （app://dashboard / dashboard:// / trash://），左侧功能栏打开终端时
  cwd 传 undefined（主进程 `cwd || os.homedir()` 回落 ~），此前虚拟
  路径当 cwd 传 node-pty spawn 失败、shell 立即退出（「在此打开
  终端」优先不受影响）。e2e 59 覆盖（回收站子目录预览标题/位置行
  虚拟断言 + 面板全文无 .local/share/Trash + 记录型 terminal:spawn
  handler 断言三次 cwd：真实目录 = 该目录 / 仪表盘 = 空串 / 回收站根
  = 空串）；回归 12/23/24/45/57 通过。**应用图标改用 SVG（.desktop
  等位置）**——图标源从 `assets/icon.png`（实为 JPEG 内容）改为
  `src/icon.svg`（SVG 矢量）：启动器条目复制落点随之改为
  `hicolor/scalable/hoshineko-fm.svg`（freedesktop 规范 SVG 归
  scalable 目录；launcherEntry.ts / main.ts / e2e harness 环境同源
  三处），`src/icon.svg` 经 extraResources 进打包产物（dev 回落仓库
  文件）；系统集成 .desktop 开发分支（system.ts buildDesktopEntry）
  Icon= 同步改指 `<appPath>/src/icon.svg`；AppImage 打包图标
  （electron-builder linux.icon 仅支持 PNG）仍用 assets/icon.png；
  e2e 56 更新（scalable 落点 + `<svg` 内容断言）。
  **缩略图缓存失效与重新缓存**——图片被改动（重命名/复制/移动/
  就地覆盖替换/删除后重建）后不再显示陈旧缩略图：`electron/fsUtils.ts`
  生成成功时记录源内容戳（mtimeMs+size）到点号前缀 sidecar
  （`.{hash}.stamp`，e2e 计数与缓存统计均排除）；命中校验——内存 LRU
  条目带 `{cachePath, stamp, epoch}`，**同世代请求零 stat 快路径直返**
  （滚动风暴性能不回归），世代变化（FileList 在文件操作/目录刷新后
  bump thumbEpoch 经 media:// URL `?v=` 传入）时异步 stat 源文件比对
  戳——一致刷新 LRU 世代直返、不一致作废旧缓存+戳并重新生成、源已
  删除返回旧缓存、无戳（旧版缓存）按当前源补录；内存命中但磁盘缓存
  消失（外部删除/清缓存竞争）作废条目走生成。e2e 60 覆盖（覆盖后字节
  变化+戳更新/重命名新 key 生成+旧孤儿保留/旧路径重建重新生成/删除
  回退不崩溃）；e2e 30/34/35 回归通过（35 滚动风暴为环境性 flake，
  stash 对照与改动无关）。
  **FileList scrollToRow 冷缓存竞态修复**——外部应用「在文件管理器中
  显示」（FileManager1 ShowItems）打开新窗口时文件区定位到目标条目：
  本仓库的 react-window 是定制版（rowHeight 可传函数），`scrollToRow`
  按「最后已测量行」外推总高——挂载早期边界缓存只有已渲染行（容器
  未测量时仅 48px 分组头），外推总高严重偏小（48×301 ≈ 真实 60%），
  落点错且被过矮的 spacer 钳制、目标行不在视口内。修复：第一次
  scrollToRow 同步扩展边界缓存到目标行，下一次渲染按扩展后缓存重建
  spacer，双 rAF 后再滚一次落位（双同步调用无效——第二次仍被旧
  spacer 钳制）。e2e 17 补大目录 ShowItems 断言（300 文件目标第 250
  个 → 选中 + 条目在文件区视口内）。
  **UI 字体改用 Google Sans**——`src/index.css` 的 `@font-face`
  （`'Google Sans VF'` 可变字体 wght 400–700 + `font-display: swap`）+
  `:root font-family` 与 `--md-ref-typeface-plain/brand` 首位；中文/日文/
  韩文自动回落 Noto Sans SC（与以往 Roboto 行为一致）；Roboto 20 个
  @fontsource import 移除（bundle 减小）；字体文件
  `src/assets/fonts/GoogleSans-Var.woff2` 由仓库根
  `fonts/GoogleSans-Var.ttf` 经 fonttools（flavor='woff2'，需 brotli）
  转换（4.8MB → 1.5MB）

---

## 二、未实现 / 待办

### 1. 收藏夹/书签（**有意不做**）

- 与仪表盘固定项、侧边栏固定目录功能重合，用户决策不实施

### 2. 格式化无文件系统设备（**有意不做**）

- 高危操作应交给专业工具（磁盘管理软件/终端），用户决策不实施；未格式化设备保留「无法挂载」提示

### 3. 跨平台支持

- 完全 Linux 专有：inotify、`/proc/mounts`、`/etc/passwd`、`/sys/class/block`、`lsblk`、`udisksctl`、`df`、`du -sb`、`find`、`unzip/tar`、`xdg-mime`、`gio`、dbus-next + UDisks2
- macOS / Windows 不可用（PTY 模块有 win32 分支，但整体系统集成不跨平台）

### 4. 自动更新

- `latest-linux.yml` 存在但**无更新检查逻辑**（无 electron-updater 类依赖、无更新 handler）

---

## 三、已解决的已知问题（docs/bugs.md 核对）

| 问题 | 状态 |
| --- | --- |
| 启动时打开两个仪表板标签页 | 已修复 |
| Trap Pages（双标签目录同步、关一关二） | 已修复 |
| 多选文件无法拖给 LocalSend | 已修复（原生 startDrag） |
| 打包类文件（Office/压缩包）分类错误 | 已修复（ZIP 容器扩展名仲裁） |
| 「打开方式」启动丢失环境变量 | 已修复（优先 `gio launch`） |
| 拖文件到标签页复制/移动 | 已实现（v0.11.2） |
| 新建文件（夹）对话框 | 已实现（NameInputDialog） |
| 在系统默认终端中打开 | 已实现（v0.11.3） |
| Dashboard 固定项支持文件/文件夹 | 已实现（v0.11.2） |
| 在自定义终端中打开（程序外配置） | 已实现（v0.11.16，`~/.config/HoshinekoFM/terminal.conf`） |
| 拖文件到地址栏（复制/移动到目标目录） | 已实现（v0.11.16，地址栏背景落点 = 当前目录） |
| 内建调色板 / 继承 DMS 配色 | 已实现（v0.11.9 起主题颜色系统，v0.11.16 启用 DMS 系统主题卡） |

---

## 四、剩余项目工作逻辑（计划）

> 本节为剩余未实现项目的**技术实施逻辑**说明，
> 供后续开发按此推进，避免重新调研。
> （收藏夹/书签、格式化、e2e 固化三项已按用户决策移除：前两项有意
> 不做，e2e 已实施。）

### 1. 自动更新

- 依赖 `electron-updater`；主进程新增 `electron/updater.ts`：
  `autoUpdater.checkForUpdatesAndNotify()`（app ready 后 10s 延迟触发，
  避免影响启动），`update-downloaded` 时提示重启（复用 ConfirmDialog
  样式的主进程 dialog）。
- AppImage 部署需 `publish` 配置（GitHub Releases / generic URL）与
  `electron-builder` 的 `latest-linux.yml` 一致；`latest-linux.yml`
  已存在于构建产物说明发布链路曾配置过，补上依赖与 handler 即可。
- 无网络/失败时静默（仅 console.warn），不打扰用户。

### 2. 跨平台支持（长期工程）

- 分层替换：`electron/` 下按平台拆实现（`fsLinux.ts`/`fsWin.ts`），
  共享接口（列目录/挂载/设备/MIME/终端检测），`electron/main.ts`
  按 `process.platform` 注册。
- 优先 Windows 支持（用户基数大）：替换 `lsblk`→`wmic/powershell
  Get-Disk`、`du`→walk、`find`→递归遍历、inotify→`fs.watch`、
  回收站→`shell.trashItem` 已跨平台；macOS 用 `diskutil`/`df`。
- 每完成一个平台再启用对应 electron-builder target；PTY 已有 win32
  分支（`electron/pty.ts`）。
