# AGENTS.md — Hoshineko File Manager

## Commands

| Command                  | What it does                                                                                  |
| ------------------------ | --------------------------------------------------------------------------------------------- |
| `npm run dev`            | Start Vite dev server (port 5173)                                                             |
| `npm run electron:dev`   | Start Vite + wait-on port 5173 + build electron CJS + launch Electron with devtools           |
| `npm run build`          | `tsc -b && vite build && tsc -p electron/tsconfig.json`                                       |
| `npm run electron:build` | `npm run build` → `scripts/set-cjs.cjs` → `electron-builder` (outputs AppImage to `release/`) |
| `npm run lint`           | `eslint .` (flat config, ESLint v9)                                                           |
| `npm run e2e`            | `npm run build` → run every `scripts/e2e/*.test.cjs` via `npx electron` (needs a display)     |

No `typecheck` script — run `npx tsc -b` or `npx tsc --noEmit` for type-checking.
No unit-test framework — e2e tests live in `scripts/e2e/` (Electron main-process driven, no deps; see below).

## e2e 测试（scripts/e2e/）

- 运行：`npm run e2e`（先 build 保证测最新产物）；单跑：`npx electron scripts/e2e/<name>.test.cjs`。
- 需要图形会话（本机 DISPLAY=:0 可跑；无头 CI 用 `xvfb-run -a npm run e2e`）。
- 架构：`harness.cjs` 加载真实 `dist/index.html` + 真实 `dist-electron/preload.js`，用 `sendInputEvent` 模拟输入、`executeJavaScript` 断言；**不 import `electron/main.ts`**——harness 里的 scheme 注册、preview 协议、`app:get-startup-path`/`picker:get-config`/`fs:watch-dir` 等是 main.ts 顶层注册的**手工副本**，改 main.ts 时必须同步更新 harness（注释已互相指向）。**例外**：D-Bus 服务后端（portal FileChooser / FileManager1）已抽成 `electron/backends.ts` 共享模块（`registerServiceBackends`）、启动器条目创建抽成 `electron/launcherEntry.ts` 共享模块——main.ts 与 harness 走同一条接线，无手工副本，改坏注册接线 e2e 直接失败（harness 侧仅环境路径不同：沙箱目录 + `HOSHINEKO_E2E_DESKTOP_DIR`/`HOSHINEKO_E2E_APPMENU_DIR`/`HOSHINEKO_E2E_LAUNCHER_EXEC`/`HOSHINEKO_E2E_LAUNCHER_APPIMAGE` 环境变量覆盖）。
- 已知坑点（写新用例必读）：
  - **React 受控输入**：直接 `el.value =` 不触发 onChange，须用 prototype value setter + `input` 事件（`setReactInput`；md-* 文本域穿透 shadow root 找 input）。
  - **双击 = 两次独立 click**（应用用 lastClickRef 手动检测，`doubleClickEl` 发两对 down/up，间隔 60ms）；间隔 > 500ms 的慢速双击进入行内重命名。
  - **Dialog 有 250ms 串行化延迟**：内容常驻 DOM，断言对话框必须查 `md-dialog` 的 `open === true`，不是查内容是否存在；操作间 `waitDialogAnim()`。**开启动画未完成时点击会落在错位坐标**（e2e 04 实测：Wayland 软件渲染下动画更慢，`open === true` 时 rect 仍带亚像素小数、点击点最终落在无 class 的底层 DIV 上，开关不切换）——打开对话框后、任何真实输入前必须 `waitDialogAnim()`。
  - **缩放因子会话级共享**：任一窗口 setZoomFactor 后其他窗口 getZoomFactor 读到同值；同窗口直接 `localStorage.setItem` 不触发 storage 事件（跨窗口同步必须由另一窗口写入）。
  - **picker `resolvePicker` 会立即关窗**：其自身 IPC 响应可能丢失，测试里必须 fire-and-forget（不能 await）。
  - **md-select 程序化选择**：`select(value)` 是静默的（不派发事件），须补 `dispatchEvent(new Event('input'))` 才触发 React onInput（harness 已封装 `selectOption`）；选项是宿主**轻 DOM** 子节点（非 shadow）。
  - **标题栏标题断言**：跑马灯（MarqueeText）会把文本复制多份渲染，textContent 不可靠——读 `.title-bar-title .marquee-container` 的 title 属性；重载后启动路径异步解析（初始为默认仪表盘标签），须 waitFor 目标标题。
  - **侧边栏布局异步移位**：重载后位置列表（主页等）异步到达会把固定区/设备区整体下移——对侧边栏条目做真实输入点击前须等待布局稳定（固定项出现后再 sleep 数百 ms），否则点击落在位移前的旧坐标误点其他项（e2e 46 踩过：误点主页列表项）。
  - **平铺 WM 下无最小化入口**：检测到 tiling（i3/sway/hyprland/niri 等）时右侧最小化按钮与 v 菜单「最小化」项隐藏（`TitleBar hideMinimize`），且主进程 `window:minimize` no-op 兜底——断言控制区按钮数/下标、最小化行为前先确认当前检测环境（`detectWindowManager` 在 invoke 时读 `process.env.XDG_CURRENT_DESKTOP`，e2e 测试进程即主进程，可直接改环境变量切换分支，e2e 15 用 GNOME 覆盖堆叠分支）。**v 菜单结构（v0.11.45 起）**：新建窗口（`window:new`，经 `registerWindowHandlers` 第二个可选参数注入 createWindow 工厂——harness 同款接线注入 createTestWindow；点击后菜单由 ContextMenu 统一关闭）/ 最大化（最大化时切「还原」）/ 最小化 / 退出——平铺三条目（18/18/22 图标字号）、堆叠四条目，e2e 15 已按此断言并回归「新建窗口」点击（第二窗口打开于仪表盘标题）。
  - **三态开关（跟随系统/开/关）模式**：标题栏（SettingsDialog）与明暗主题（ThemeColorDialog）同款——md-switch 必须纯展示化（pointer-events:none + 内部 input 移出 Tab 序），交互由外层 `role=switch` 容器接管，草稿只经函数式更新；直接给受控 md-switch 挂 onClick 会有内部 checkbox 状态机与 React 受控赋值的时序竞争（跟随模式「点两次才生效」）。语义：跟随系统按钮→null；手动点开关→取反；跟随模式点开关→退出跟随+新值=生效值的反。**新增同款开关必须遵循 `docs/跟随系统设置项目逻辑.md`**。
  - **portal 后端总线名**：e2e 用**进程级随机名** `…hoshineko.e2e.p<pid>.r<随机>`（`h.E2E_PORTAL_BUS_NAME` / `h.E2E_FM1_BUS_NAME`，harness 经 backends.js 注册），避免与运行中的应用实例/残留 e2e 进程抢名；后端就绪断言 `await h.getBackendRegistration()` 返回值（本进程注册状态），**不要用 GetNameOwner 轮询**（只证明名字有主、不证明主是本进程）。
  - **不要重新引入 `usocket` 依赖**（曾为直接依赖，已移除）：dbus-next 把它当 optional 依赖，仅用于 FD 传递/abstract 套接字；其原生 addon（uwrap.node，2016 年代）在 Electron 43 下有 libuv 句柄 use-after-free——`bus.disconnect()` 时偶发 SIGSEGV（uv__finish_close 写已释放的关闭队列指针）或退出时主线程死循环挂起，且泄漏总线连接 FD（僵尸占名根因之一）。移除后 dbus-next 自动回退 `net.Socket`（`unix:path=` 总线地址不受影响；abstract 地址的旧 X11 会话会注册失败，属已知取舍）。应用从未启用 negotiateUnixFd。
  - **会话总线重启按钮**（设置页**常驻**）：`system:restart-session-bus` 依次尝试 `systemctl --user restart dbus-broker.service` / `dbus.service`（30s 超时），成功后经 `registerSystemHandlers(onSessionBusRestarted)` 回调延迟 2.5s 重新注册后端并作废冲突缓存（`resetBackendConflictCache`）；e2e 37 用 PATH 前置假 systemctl 测 IPC 契约，**绝不重启真实总线**。
  - **portal 冲突警告弹窗**（每次会话一次，带 backdrop 遮罩的 AlertDialog）：portal 后端冲突（outdated/noVersion/unresponsive）不再用 toast——启动查询与设置打开刷新都经 `maybeAlertPortalConflict`（ref 守卫防重复），详情仍常驻设置页「系统集成」行副标题。
  - **保存器重名冲突**：FilePicker 保存模式确认（按钮/文件名输入 Enter/双击文件）都先 `checkSaveConflict`（existsBatch 以真实文件系统为准）——目标存在则弹与复制/移动同款 `ConflictDialog`（`operation="save"`：skip 模式改标「覆盖」）。覆盖 = 原名回传；自动/手动重命名 = 安全名/编辑名回传；手动留空不解析（弹窗保持）。e2e 38 覆盖此链路。
  - **保存器背景右键「新建文件夹」（v0.11.45.14 起，e2e 58）**：仅保存模式（`config.mode === 'save'`）文件区空白处右键弹背景菜单，**只含「新建文件夹」一项**（无新建文件/粘贴/属性——文件名走底部输入框，其余无语义）；选择模式背景右键无任何菜单（handler 内 `isSave` 守卫早退，与「选择器不接收拖放」同源）。点击菜单项 → 复用主窗口的 `NameInputDialog`（`isDir` 确认补尾斜杠、同名冲突校验/安全名同源）；确认后 `createDirectory` mkdir + 重载当前目录（列表出现新文件夹）。FileList 的 `onBackgroundContextMenu` 是可选 prop——选择器此前不传（无背景菜单），现在保存模式传入该 handler。e2e 58 坑：**菜单项是 `md-list-item` 不是 `.context-menu-item`**（ContextMenu 用 ListItem 渲染，断言用 `.context-menu md-list-item`）；背景右键点容器中心——单条目列表顶部留白命中空白（条目多时须点空白坐标）。

  - **portal 版本不一致启动弹窗**：`system:get-portal-runtime-info` 检查 `hoshineko.portal` 存在且 `<portal目录>/hoshineko.version`（install.sh 写入，值 = `app.getVersion()`，经 `runIntegrationScript` 注入 `HOSHINEKO_VERSION` 并显式透传 pkexec）≠ 当前版本 → 弹 `PortalVersionDialog`：打包版双按钮（取消 / 一键重装 → `system:reinstall-system-integration` 跑 reinstall.sh）；开发版仅取消 + 运行时诊断详情（`appVersion:` 字段开头，e2e 按此定位不受 locale 影响）。打包版弹窗内按 PgDn 切开发详情（App.tsx window keydown，仅在弹窗 open 时挂监听）。**取消不记忆，每次启动弹**；portal 未安装不弹；**版本号文件缺失也不弹**（视为旧流程残留/不完整安装——曾有用户部分安装后误弹）；每次会话只弹一次（ref 守卫）。**版本不一致时抑制冲突弹窗**（`portalVersionMismatchRef`，重装一并解决冲突，两个遮罩弹窗不叠层）。e2e 39 覆盖此链路。
  - **portal 相关操作结果已从 toast 改为带遮罩 AlertDialog**（`portalNotice`）：系统集成安装/卸载、一键重装、会话总线重启的成功与失败都弹对话框（错误细节截尾 400 字符进正文）；会话总线重启成功正文为空（仅标题）。e2e 断言这些操作结果须查 `md-dialog`，不是 snackbar。默认文件管理器/缩略图缓存等非 portal 项仍走 toast。
  - **系统集成脚本可被 source**：install.sh/uninstall.sh 主入口 case 包裹 `[ "${BASH_SOURCE[0]}" = "$0" ]` 守卫；reinstall.sh（版本弹窗「一键重装」执行体）source 两者复用函数，卸载 + 安装合并为**单次 pkexec 授权**（分别跑两脚本会弹两次密码框）；root 级先卸载后安装原子完成。**runIntegrationScript 跑 reinstall.sh 时必须把 install.sh/uninstall.sh 一并复制到临时目录**（source 依赖，缺了直接「没有那个文件或目录」）。reinstall.sh 与两脚本同样遵守 `HOSHINEKO_SKIP_SERVICE_KILL` / `HOSHINEKO_SYSTEM_BIN` / `HOSHINEKO_USER_BIN` / `HOSHINEKO_PORTALS_DIR`（portal 目录覆盖，install/uninstall/reinstall 三脚本与 system.ts 读取一致）。
  - **e2e harness 沙箱 portal 目录**：harness 在 setupApp 里默认把 `HOSHINEKO_PORTALS_DIR` 指向不存在的沙箱目录（开发者机真实装有 hoshineko.portal 时启动版本弹窗会干扰用例；system.js 在 registerIpc 时读取该变量）——需测版本弹窗的用例必须在 setupApp 前自行设置 `HOSHINEKO_PORTALS_DIR`（e2e 39）。
  - **主题颜色预览只作用于预览卡**：ThemeColorDialog 调整颜色（预设/壁纸/自定义/DMS/matugen）只经 `resolveThemeVars` 解析变量表内联覆盖 `.theme-color-preview`（不注入 `#app-theme`、不广播）；「应用」/「确定」才保存并经 storage 跨窗口同步全局应用；取消无需回滚（调整期间无全局改动）。`theme:preview`/`theme:preview-end` 预览广播 IPC 已移除（main.ts 与 harness 手工副本同步删除）。e2e 05 断言：选择预设后 #app-theme 不变、预览卡 style 出现变量；确定后本窗口更新且双窗口一致。**固定预览区**：标题 + 预览卡 + 明暗开关包在 `.theme-color-fixed`（sticky，钉在 md-dialog shadow `.scroller` 顶部），其余设置下方滚动——e2e 里 `.theme-color-preview` 仍在光 DOM 内，选择器不受影响。**sticky 坑**：md-dialog shadow 会给内容槽包装层（Dialog 的 `div[slot=content]`）加 padding（scrollable+headline 时顶 8px）——它是 sticky 的直接父级 padding，会让固定区先位移再吸附；已用 `md-dialog:has(.theme-color-fixed) > div[slot="content"] { padding: 0 !important }` 归零（固定区 `margin: 0 -24px` 横向铺满）。  对话框用 `noHeadline`（Dialog prop）取消标题槽：否则滚动时 md-dialog 在标题下画内置分隔线（shadow 内部不可定位），标题移入固定区、分隔线由固定区底部自绘（透明 1px 常驻占位，滚动后着色——滚动状态经 Dialog 的 `onScrollerReady` 回调拿**当前打开周期**的 scroller 挂监听写入 `--scrolled` 类；md-dialog 每次打开重挂载全新元素，自行 closest/shadowRoot 定位会拿到已替换的旧元素）。**内置底部分隔线透明化**（`--md-divider-color: transparent`）：其 isAtScrollBottom 判定在内容高度带亚像素（圆形色盘 aspect-ratio 产生 0.125px 小数）时失效、滚到底底线恒显示——改自绘 `md-dialog:has(.theme-color-fixed--show-bottom) > div[slot="actions"] { border-top }`，非底部显示/滚到底隐藏（组件按 scrollTop 判定，1px 容差）。**主题快照注入（服务模式选择器/保存器）**：GUI 经 `app:set-theme-snapshot` 上报 settings.theme+darkMode 落盘 `theme-snapshot.json`（main.ts sanitize：kind 白名单 + 字段校验，非法整体丢弃；harness 有手工副本），服务模式创建选择器/保存器时注入 `pickerConfig.theme`、FilePicker 优先取注入值（GUI 模式回落 localStorage storage 同步）；实时继承经 startSnapshotWatcher 广播 `picker:theme-changed`（harness 在 IPC 内直接广播）；调用方传入 theme 字段被白名单忽略。e2e 40 覆盖（明暗通道不在 harness 断言——主窗口 App 跟随系统检测与选择器竞争 themeSource，服务模式无 GUI 窗口无此竞争）。**选择器同步规则（立即组/确认时组，见 同步规则.md）**：`picker-prefs.json`（viewPrefs）现含分类/排序（sortBy/sortOrder/groupingEnabled）与控制组折叠（sortControlsCollapsed）——立即同步组，主窗口一变即广播；`picker-settings.json`（新增 settings 快照）为确认时同步组（searchGroupByDir/showFullPathTitle——**主窗口内部也确认时生效**：SettingsDialog pending 机制，对话框内切换只改预览，确定/退出才落 localStorage），主窗口设置按下确定/退出时经 `app:set-picker-settings` 上报。FilePicker 注入值优先于本地（主窗口为权威，选择器内调整会被下一次主窗口变化覆盖）；选择器搜索经 `searchActive` + `groupByDir` 支持搜索分类。**pickerSettings 含 locale（确认时同步组，可选字段）**：服务模式选择器 userData 隔离、语言默认跟随系统——经快照注入 + `picker:settings-changed` 广播应用 GUI 语言（sanitize：'auto' 或 xx-XX，可选向后兼容旧快照）；选择器标题文本渲染期派生（语言切换实时跟随，勿存 state）。**首次使用默认值（v0.11.34 起）**：语言 auto / 隐藏文件开 / **列表** / 图标 **48** / UI 100 / 实心图标关 / 主题与明暗 null / 标题栏 null / 完整路径关 / **跑马灯关** / 搜索分类开 / home 占用关 / 预览关 / 目录大小开 / **自动创建桌面图标与应用程序菜单条目开** / **新建标签页目录 /**（App.tsx 与 FilePicker 回落默认一致，e2e 15/20/29 跑马灯断言须显式开启）。**恢复默认设置**：设置底部「默认配置」区域按钮 → ConfirmDialog（带遮罩，确认/取消）→ App `handleRestoreDefaults` 全量重置（setter 触发跨窗口同步 + 快照自动上报）；SettingsDialog 有 props 同步 effect（应用值变化时重置 pending 预览，防止恢复后「确定」把旧预览盖回）。**滚动文本/文件预览开关确认时生效**（pendingMarquee/pendingFilePreview，与语言/完整路径同款 pending 机制）。**GUI 模式回落**：真实 main.ts 只在服务模式注入快照（`createWindow` 的 `SERVICE_ONLY_MODE` 分支），GUI 模式选择器靠 FilePicker 的 `useLocalStorage` 读同一组键（localSearchGroupByDir/localShowFullPathTitle）——打开时同步 + storage 事件实时跟随；harness 默认注入（服务模式语义），`h.setPickerSnapshotInjection(false)` 模拟 GUI 模式（e2e 43），且 `pickerConfigByWindow.set` 必须在注入块外无条件登记（与 main.ts 一致，否则选择器 get-config 为 null 白屏）。**搜索态强制分组按钮**：searchGroupByDir 开启且搜索中时 SortControls `groupingForced` 强制右上角分组按钮 filled 高亮 + onClick 置空（点击无效，不用 disabled——禁用态置灰与高亮矛盾），退出搜索恢复；主窗口/选择器/保存器一致。**主窗口经派生状态 `searchGroupActive`（搜索中 + 开启 + 非回收站）三处同源**（锁定/`sortFilesByDir` 聚簇/`groupByDir` 分组）：回收站搜索是按名称过滤虚拟目录，不锁定不按目录分组（回落语义分组）；搜索失败/后端不可用复位搜索态并重载当前目录（不残留锁定态）。**选择器/保存器搜索聚簇**：`sortedFiles` 在 `searchActive && searchGroupByDir` 时同样走 `sortFilesByDir`——否则同目录条目被全局排序打散、组头重复出现。**注意**：真实 App 挂载即上报 viewPrefs（含默认值）——e2e 里想用 localStorage 预置排序/分组会失效，必须改经 `setPickerViewPrefs` 全字段上报（e2e 26/33/41 已如此）。
  - **Ctrl+滚轮图标缩放（主窗口/选择器/保存器同款手势）**：作用域 = 光标下的 `.file-list-container`（文件区），范围/步进与设置滑条同源（`src/utils/iconZoom.ts` 常量，SettingsDialog 滑条共用）：16–128±8。主窗口走 App.tsx 窗口级 wheel handler（并入既有菜单/对话框拦截 effect，`{ passive: false }` 阻止默认滚动）；对话框/右键菜单打开时不缩放。选择器/保存器走 FilePicker 窗口级 handler：写本地 `settings.iconSize`（GUI 模式与主窗口共享 session，storage 事件**双向**同步；服务模式 userData 隔离、注入 viewPrefs 优先，写本地不生效 → 另有 `iconSizeOverride` 会话覆盖立即生效），收到 `picker:view-prefs-changed` 广播即清除覆盖（主窗口为权威，「选择器内调整被下一次主窗口变化覆盖」语义）；GUI 模式不写覆盖（viewPrefs 为 undefined 时只写本地）。React 受控 onWheel 是 passive 的、preventDefault 无效，必须 addEventListener。**e2e 50 注入滚轮坑**：`sendInputEvent({type:'mouseWheel', deltaY})` 注入的 deltaY 符号与页面收到的 wheel 事件**相反**（注入 -120 → 页面 +120），放大传 +120、缩小传 -120（e2e 50 已按此写）。
  - **右上角视图模式切换按钮（主窗口/选择器/保存器共用 SortControls）**：位于分组开关右侧的单按钮（图标/tooltip 显示切换**目标**——列表模式显示 grid_view），与设置对话框同键 `settings.viewMode`（立即同步组，跨窗口/快照广播自动生效）；选择器内切换走 `viewModeOverride` 会话覆盖（与 Ctrl+滚轮缩放同款语义：GUI 模式只写共享 localStorage 双向同步，服务模式注入优先时写覆盖、收到广播即清除，主窗口为权威）；键盘分区 TOP_BAR_BTN_SELECTOR 是通用选择器，新增按钮自动进方向键循环，无下标断言（e2e 41/42 点击分组按钮取 DOM 序首个 filled 变体，不受影响）。e2e 51 定位切换按钮按图标 ligature 文本（grid_view/view_list，locale 无关），勿用 title 文案。
  - **固定项排序拖拽与文件落点目标互斥（v0.11.41 修复误导高亮）**：排序拖拽是仅排序语义的纯 HTML5 会话（dataTransfer 只有 text/plain 源索引，不设 DragContext、不 `startDrag`），文件落点处理器此前不区分拖拽类型——文件区文件夹条目（FileList `handleFolderDragOver`）与地址栏胶囊（Breadcrumbs `handleDragEnter`）直接高亮，地址栏背景/文件区背景/终端给出 copy/move 光标提示，标签页有 dragState 守卫但残留陈旧 dragState 时仍会误高亮。修复：`src/utils/pinReorderDrag.ts` 全局标志（Sidebar 固定项 dragstart 置 true、dragend 置 false——HTML5 规范保证 dragend 必派发，含 ESC/落点不被接收），六个文件落点处理器（FileList dragover/drop、Breadcrumbs dragenter、addressBarDrop、ExplorerTab 文件区容器、TabBar dragover/drop、TerminalPane）守卫早退（不 preventDefault → 无高亮无光标提示）。**新增文件落点目标时必须加同款守卫**。合成 DragEvent 可触发 React 拖拽处理器（dragstart 实测有效，React 18 批处理需 sleep 后再断言类名）——e2e 52 用合成事件覆盖：拖拽期间对标签页/面包屑/文件夹条目派发 dragover/dragenter 断言无 `.drag-over` + dragstart→dragover→drop 完整换序回归（tracker 在 drop 捕获阶段补发合成 dragend 会先清 pinDrag，drop 处理器回落按 clientY 重算 gap，属既有正常路径）。**选择器/保存器不接收任何拖放（v0.11.41）**：拖动任何可拖动控件（固定项/文件条目/外部文件）经过选择器窗口，文件区文件夹条目与地址栏胶囊不得高亮、地址栏背景不得接受——FileList 文件夹落点按 `onDropOnFolder` 是否存在门控（选择器不传）；Omnibar/Breadcrumbs 的 `onDropFiles`/`onDropExternalFiles` 改为**可选**（选择器不传 → addressBarDrop/dropHandler 为 null → dragover/dragenter/drop 全早退，不 preventDefault 不高亮）；主窗口两处照传不受影响。e2e 53 断言：选择器内胶囊/文件夹条目无高亮且 defaultPrevented=false、地址栏背景不被接受 + 主窗口同款合成事件仍高亮/接受（回归）。**e2e 断言 defaultPrevented 的坑**：合成拖拽事件后读 `dataTransfer.dropEffect` 不可靠（Chromium 派发后复位 none）；处理器内 stopPropagation 会截断冒泡到 window——监听器须挂在 root 容器冒泡阶段（React 处理器注册在先先执行，stopPropagation 后监听器仍在 root 上可读 `e.defaultPrevented`）。
  - **注入键盘事件的 Enter 不合成原生按钮点击**：sendInputEvent/CDP 注入的 keydown Enter 不触发 `<button>` 的 Enter→click 默认行为（Space keyup 反而可以）——键盘导航的「Enter 激活」在应用内显式 `click()`（NavigationRail/Sidebar 均如此），测试不要依赖原生合成。
  - **导航栏活动项是 `md-filled-icon-button` 变体**：标准 `md-icon-button` 选择器不含活动项——e2e 15 按标准按钮计下标（0..3 = 仪表盘/回收站/终端/设置，活动项 Files 不计入）；激活导致变体切换替换元素丢焦点，应用内键盘激活后经 rAF 恢复同下标焦点（测试勿依赖激活后的焦点落点，必要时显式 focus）。
  - **滚动条拖动（sendInputEvent）**：Chromium 原生滚动条接管拖动，期间**不向页面派发 mousemove**（mousedown/mouseup 照常、click 被吞）——框选副作用由 scroll 事件驱动；e2e 28 断言「滚动条拖动不得进入框选模式」用状态栏框选提示（onSelectionModeChange 副作用）作确定性信号（选框本身依赖几何，合成输入下不一定出现）。
  - 菜单/按钮文案按中英文双匹配（`/取消|Cancel/`），规避系统语言差异。
  - **侧边栏固定区拖拽排序是 HTML5 DnD（仅排序语义，不经过文件拖拽系统）**：源条目拖起后 display:none（隐藏延迟到 rAF——先让浏览器截取拖拽图像，`pinReorderActiveRef` 守卫起拖瞬间松手竞态；源条目留 DOM 末尾保证 ESC 取消时 dragend 派发清理）；插入位置 = 上/下半区（±4px 死区防抖动）+ 空占位按钮（`.sidebar-pin-gap`）；间隙语义基于「去掉源条目后的剩余列表」（`computePinReorderGap` 的 `- (from < hoverIndex ? 1 : 0)` 补偿，与 App 侧 `splice(from,1)` 一致）；其他区域不接收（无 drop 处理器落下即回弹）。**harness 无 HTML5 DnD 模拟能力**——e2e 47 测同步链路本身（跨窗口 localStorage 写入等价于另一主窗口完成排序；同窗口写不派发 storage 事件），不要试图合成 dragstart/drop；选择器/保存器固定区只读（draggable 关闭）。
  - **彩蛋对话框（设置 + Ctrl+PgDn，e2e 49）**：原生 `dialog.showModal()` 强制聚焦首个可聚焦子元素（键盘触发下 :focus-visible 高亮环）——用 autofocus 承接点接管初始焦点，**属性必须在 ref 回调写入**（Dialog 以 key={cycle} 每次打开重挂载 md-dialog、内容节点重建，挂载期 effect 写到旧节点；TS 不允许 div 的 autofocus 属性）；承接点做成内容顶部 **0×0 焦点锚点**（tabindex=-1、不进 Tab 序）——聚焦高内容容器会被 Dialog 的焦点滚动校正滚到底部（r.bottom > s.bottom 分支），锚点在顶部则不滚动、打开置顶；无焦点环断言查 `outlineStyle === 'none'`（`outline:none` 后 outlineWidth 仍报 3px = medium 初始值）；内容无焦点元素时 PgDn/PgUp 翻页失效（焦点在 scroller 外的确定按钮）——经 `onScrollerReady` 拿当前周期 scroller 手动滚动；图片固定 max-width 像素值（勿用 vh 上限，会随窗口高度漂移）。
  - 对话框内容超出视口时点击前 `scrollIntoView`。
  - **右上角控件组折叠（v0.11.43 起，e2e 54）**：SortControls（主窗口/选择器/保存器共用）折叠态只渲染「更多」按钮（tune，**仅搜索强制分组时 filled**——分组开关自身状态不改变按钮高亮，避免「更多」被误读为激活态）+ `md-menu` 溢出菜单（`positioning="popover"` 锚定按钮，6 项：分组勾选/视图切换/三项排序勾选+升降箭头/展开控件，菜单条目高度 40px 与右键菜单同款——`SortControls.css` 的 `--md-menu-item-*` 紧凑 token）；展开态五按钮右端加「收起」把手（chevron_right 指向折叠方向，与排序键间分隔线；菜单「展开控件」项用 chevron_left 指向展开方向）。`Menu` 是 lit-react 包装——`onClosed` 需在 `md/index.ts` 的 events 里显式声明（`'closed' as EventName<Event>`），否则 TS 直接拒绝；open 受控时**菜单项 onClick 内先 `closeMenu()`**（md-menu 内部关闭经动画异步派发 closed，不同步会残留 open=true）。折叠状态持久化 `settings.sortControlsCollapsed`（立即同步组）：`PickerViewPrefs` 加 `sortControlsCollapsed`（sanitize 可选向后兼容旧快照，缺省 false——main.ts / harness 手工副本 / electron/handlers/picker.ts 三处同源类型都要改）；选择器/保存器经快照注入继承 + 广播跟随，本地切换走 `sortControlsCollapsedOverride` 会话覆盖（收广播即清除，主窗口为权威）；GUI 模式写共享 localStorage 双向同步。**菜单项键盘激活（v0.11.43 修复）**：md-menu-item 键盘 Enter/Space **只派发 close-menu、不合成 click**（@material/web MenuItemController.onKeydown，仅 href 锚点特例 click）——动作不能只挂 onClick，须在 MenuItem 挂 `onCloseMenu`（md/index.ts events 显式声明 `'close-menu'`）按 `detail.reason.kind === 'keydown'` + `reason.key` 过滤执行（**Escape 也派发 close-menu，必须忽略**）；鼠标点击同时走 onClick（click 也派发 close-menu reason='click-selection'，不在键盘处理器执行避免双触发）。e2e 54 坑：**上一轮关闭动画未结束时重开菜单，md-menu 不落焦点**（实测 600ms 内重开 activeElement 留 body）——键盘段落重开前须 sleep 等关闭动画收尾；**打开后焦点经打开动画延迟落首项**（defaultFocus FIRST_ITEM）——发 Enter/Down 前须 waitFor activeElement 为 md-menu-item。e2e 54 其余坑：**选择器写 localStorage 会经共享 session 同步到主窗口**（harness 服务模式模拟与生产不同）——「主窗口为权威」断言前须 waitFor 主窗口已折叠再经菜单展开；折叠后重载保持断言前须确认键已落盘。恢复默认设置重置该键（`handleRestoreDefaults`）。
  - **顶栏控件组自动换行（v0.11.43 起，e2e 55）**：地址栏压缩过度（低于 `OMNIBAR_MIN_WIDTH_EXPANDED`=240px）且展开态时右上角控件组自动换到第二行、地址栏独占第一行——纯 flex 实现：顶栏容器 `flex-wrap: wrap` + 地址栏分区展开态 `min-width: 240`（折叠态 0）+ 排序分区 `margin-left: auto`（第二行右对齐）；窗口/内容区宽度变化实时往返，无 JS 状态、无振荡风险。主窗口顶栏与选择器/保存器 `picker-topbar` 同款。**换行底部分界线**：`useTopBarWrap`（hooks/）ResizeObserver 检测「排序分区顶边 ≥ 地址栏分区底边」仅驱动装饰样式（常驻 1px border，换行着色/未换行透明防跳动；换行时 padding-bottom 8px 让分界线下移 8px）——不参与布局无反馈回路；**必须传 `active`（顶栏已渲染）依赖**：选择器 config 异步到达、顶栏晚于 effect 挂载，ref 恒 null 时不重跑观察器则检测永不生效（仪表盘无顶栏同理）。e2e 55 坑：**平铺 WM 下 `win.setSize` 无效**（本机实测窗口被 tiler 固定 817px）——宽度驱动改经侧边栏 `display:none`（宽态）与顶栏容器 `style.maxWidth`（窄态，注意 topbar 是 content-box、maxWidth 不含 padding）注入；**前一用例遗留的折叠态会经快照注入污染后续窗口**（选择器内折叠写共享 localStorage → 主窗口折叠 → 保存器继承折叠 → min-width 0 永不换行），开新窗口前须把主窗口状态复位。
  - **自动创建启动器条目（v0.11.44 起，e2e 56）**：两个独立开关（设置 → 行为，默认开，**确定时生效**——SettingsDialog pending 机制，与搜索分类/滚动文本同款）`settings.autoCreateDesktopEntry`（桌面）/`settings.autoCreateAppMenuEntry`（应用程序菜单）——核心逻辑在 `electron/launcherEntry.ts` 共享模块（main.ts 与 harness 同一条接线，**无手工副本**——harness 仅环境路径不同：沙箱 userData 下目录 + `HOSHINEKO_E2E_DESKTOP_DIR`/`HOSHINEKO_E2E_APPMENU_DIR`/`HOSHINEKO_E2E_LAUNCHER_EXEC`/`HOSHINEKO_E2E_LAUNCHER_APPIMAGE` 覆盖）。语义：**打开并确定 → 创建**（`app:ensure-launcher-entry`；userData `launcher-entries.json` marker 保证「创建一次，删掉不补」——手动删文件不重建，已存在不覆盖）；**关闭并确定 → 删除条目并清 marker**（`app:remove-launcher-entry`；重新打开并确定 = 新的创建意图，可再建）；首次挂载按当前开关值创建（默认开 → 首启即建）；恢复默认设置补一次创建意图。删除只发生在确定操作的窗口（其他窗口经 storage 同步后 effect 只走 ensure 幂等路径）。**marker 读-改-写竞态（e2e 56 首跑即复现）**：desktop 与 appmenu 两路调用并行时后写者用旧快照整体替换 marker、丢另一 kind 标志——全部 ensure/remove 调用经模块内 promise 串行链排队修复（新写同款读-改-写逻辑必须串行化）。细节：Exec 优先 `APPIMAGE` 回落进程路径（桌面条目规范转义反斜杠/引号 + `%U`）；Icon 复制到 `~/.local/share/icons/hicolor/512x512/hoshineko-fm.png`（AppImage 挂载路径随机，Icon= 不能引用挂载内文件；`assets/icon.png` 经 extraResources 打包，实为 JPEG 内容，gdk-pixbuf 内容嗅探可正常显示）；`chmod 755` + `gio set metadata::trusted true`（GNOME 双击信任 best-effort，e2e 传 null 跳过）；桌面目录走 `app.getPath('desktop')`（XDG 解析，中文桌面目录可用，异常/空回落 `~/Desktop`）。已知取舍：GNOME 默认不显示桌面图标（需扩展）、AppImage 移动后桌面 Exec 失效、关闭开关会删除应用菜单条目（若系统集成 install.sh 已装且设为默认文件管理器，删除后 xdg-mime 回落系统默认）。
  - **自定义新标签页目录 + 回收站子目录虚拟路径地址栏（v0.11.45.14 起，e2e 57）**：`settings.newTabPath`（默认 `/`，恢复默认设置重置）——设置 → 行为「新建标签页目录」行（副标题常驻展示当前值）→「自定义」按钮打开二级对话框 `NewTabPathDialog`（重命名样式，**带背景遮罩**——Dialog `backdrop` prop，叠在设置对话框之上；确认即生效**无 pending 开关**）：输入绝对路径 / `dashboard://`（归一化为内部形态 `app://dashboard`，`src/utils/newTabPath.ts` 校验：相对段拒绝 `.`/`..`/空段防逃逸）或 `trash://`（回收站本身合法）/ `trash://文件夹名`；不校验目录存在性（loadPath 报错，与地址栏输入同语义）。新建标签页三入口（TabBar + / Ctrl+T / 空状态按钮）统一取该键。**混合路径模型**：浏览回收站子目录时 `currentPath` 仍保持**真实路径**（文件操作/搜索/监听/预览照常），仅显示层换算虚拟——`src/utils/trashPath.ts` 虚拟↔真实互转（非法段静默剔除防逃逸）；ExplorerTab `displayPath` 换算喂给 Omnibar/Breadcrumbs（`trash://` 根与 `trash://…` 原样、files 目录下真实路径换算）；`loadPath` 解析 `trash://…`（trashRoot 未到达时兜底重拉 `getTrashDir`）、**回收站 files 根归一化为 trash:// 视图**（从子目录返回上级/手输真实路径时清空/还原等回收站语义不丢——否则 `currentPath === 'trash://'` 的菜单/删除/搜索守卫全部失效）、支持 `dashboard://` 别名（地址栏手输同支持）；Breadcrumbs 回收站分支渲染「回收站胶囊 + 相对段」（拖放落点经 ExplorerTab `resolveDropTarget` 换算真实路径）；Omnibar 跳过虚拟路径软链接检测 + 渲染期复位检测结果（官方 adjusting-state-during-render 模式，effect 内同步 setState 会被 `react-hooks/set-state-in-effect` 报错）；**watch/mount-map 两个 effect 必须守卫 `trash://…`/`dashboard://` 挂载初值**（`useState(initialPath)` 在 loadPath 换算前就有虚拟值，直接 watchDirectory 会 ENOENT 噪音）。e2e 57 坑：**多标签页时隐藏标签页 DOM 常驻（display:none）**——地址栏/文件区选择器必须限定在活动标签页 wrapper（`.content-area` 下 `display !== 'none'` 的子 div，querySelector 会命中隐藏标签页同名元素导致误判）；e2e 44 已补 `settings.newTabPath` 恢复默认断言。
- harness 有 120s 全局看门狗，任何挂起会强制退出并报 WATCHDOG TIMEOUT。

## Architecture

- **Frontend**: `src/main.tsx` → `App.tsx`, React 19 + Vite, ESM
- **UI framework**: `@material/web` (Lit-based Material 3 web components) — all web components registered in `src/material-web.ts` (imported before App)
- **Backend**: `electron/main.ts`, `electron/preload.ts`, `electron/pty.ts`, `electron/handlers/{fs,system,window}.ts` — compiled to `dist-electron/` as **CommonJS** (`electron/tsconfig.json` sets `"module": "commonjs"`)
- **后端总线名冲突诊断**: 两个 D-Bus 后端对象暴露只读 `Version` 属性（`electron/handlers/backendInfo.ts` 定义常量与探测函数）；注册名失败时 main.ts 经 `startBackendConflictQuery` 探测占名者版本（旧版常驻/僵尸占名/无版本），渲染进程经 `system:get-backend-conflicts` 取报告在设置页与启动弹窗（带遮罩 AlertDialog）提示。
- **portal 版本检查**: `system:get-portal-runtime-info`（system.ts）聚合版本文件对比、集成状态、后端注册结果（backends.ts `getLastBackendRegistration`）与冲突报告——启动版本弹窗与开发详情共用（见「已知坑点」）。
- **IPC bridge**: `preload.ts` exposes `window.electron` via `contextBridge`; types in `src/types/electron.d.ts`
- **Services**: `FileSystemService`, `ThemeService` live in `src/services/`
- **Terminal**: `node-pty` spawned via `electron/pty.ts`; frontend communicates via `window.electron.ptySpawn/ptyWrite/ptyOnData/ptyOnExit`
- **No routing**: Tab/explorer state managed in `App.tsx` via `useState`; no React Router

## Commands order for production build

`npm run build` runs: `tsc -b` (frontend, project references) → `vite build` → `tsc -p electron/tsconfig.json` (electron CJS).
`electron:build` additionally runs `scripts/set-cjs.cjs` (writes `dist-electron/package.json` with `{"type":"commonjs"}`) then `electron-builder`.

## Key quirks

- **Electron tsconfig is separate**: `electron/tsconfig.json` uses CommonJS, outputs to `dist-electron/`. Do not use `verbatimModuleSyntax` there.
- **`scripts/set-cjs.cjs` is required**: Without it, Electron errors on `import` statements because the compiled electron JS uses `import` syntax but Electron's main process needs CJS.
- **Custom `media://` protocol**: Registered in `electron/main.ts` for serving file thumbnails (including generated thumbnails) via `net.fetch`. Paths are served as `media://<absolute-path>`.
- **`vite.config.ts` uses `base: './'`**: Required for Electron `file://` loading of production builds (otherwise asset paths break).
- **`vite.config.ts` excludes `react-window` and `react-virtualized-auto-sizer`** from dependency optimization.
- **File operations use Linux system commands**: `du -sb`, `find`, `unzip`, `tar`, `lsblk`, `df`, `xdg-mime`, `grep`. Not portable to macOS/Windows.
- **UDISKS2 device monitoring**: `setupUdisks2Monitor` in `electron/handlers/system.ts` listens for device add/remove via D-Bus; only works on Linux.
- **Dynamic theming**: reads `~/.config/matugen/theme.css` at startup via `theme:get-css` IPC.
- **Product name**: "HoshinekoFM" (`productName` in `package.json`); the npm package `name` is `hoshineko-fm`, overridden at runtime via `app.setName('HoshinekoFM')` (userData old path is written back first, see `electron/main.ts`).
- **Single-instance lock**: GUI mode holds it (second launch opens a new window). Service mode (`--portal` / `--filemanager1`) skips it — arbitration is purely D-Bus name based; registration failure exits non-zero (so dbus activation reports failure instead of hijacking via an old resident).
- **Service-mode userData isolation**: `--portal`/`--filemanager1` residents redirect `userData` to `<userData>-service` (see `electron/main.ts`). Rationale: if a resident shares the GUI profile, its picker windows open the GUI's Local Storage LevelDB and hold the lock — the GUI's storage commits then fail silently and the resident's stale snapshot can overwrite the DB on exit, wiping settings/pins/recent files after a restart. Therefore picker windows in service mode use default settings (no shared prefs/clipboard). Never remove the isolation.
- **System integration scripts** (`scripts/system-integration/install.sh` / `uninstall.sh`) kill stale service-mode residents: preferred path resolves the session-bus name owner PID via `busctl --user status` (works for renamed AppImages / non-standard paths) and kills TERM → 5s wait → KILL with exit verification; owner that is NOT service-form (no `--portal`/`--filemanager1` in `/proc/<pid>/cmdline`, e.g. a running GUI) is never killed — only a warning is printed; cmdline `pkill -f 'HoshinekoFM.*--portal'` pattern remains as fallback (covers other-session strays / no-busctl envs) with the same escalation. **busctl 对无主名字以非零退出**（dbus-broker 报 Failed to get credentials: No such device or address）——`bus_owner_pid` 末行必须 `|| true` 吞掉（set -o pipefail + 命令替换 `pid="$(bus_owner_pid …)"` + set -e 会把整个安装流程炸掉：曾致「版本更新后一键重装失败、重试永远停在 [user] 清理 portal/FileManager1 常驻进程」）；`kill_pid_escalate` 用 `proc_dead`（kill -0 失败**或** /proc/pid/status State=Z 僵尸）判死——僵尸 SIGKILL 无效，父进程未收割时会误报「仍存活」导致安装失败。e2e/sandbox runs must set `HOSHINEKO_SKIP_SERVICE_KILL=1` or they would kill real session services；**例外**：e2e 48 不设跳过，改为 PATH 影子化假 busctl/systemctl/pkill/pgrep/xdg-mime + 沙箱 HOME/USER_BIN 测真实 kill 路径（无主名字回归 + 假常驻 `bash -c "exec -a 'HoshinekoFM --portal' sleep 300"` 击杀验证——spawnSync 阻塞期父进程不收割、被杀进程成僵尸，断言按 State=Z 判死）。
- **Integration change re-registration**: `registerSystemHandlers` takes a second optional callback `onIntegrationChanged` — main.ts passes `reRegisterBackends` (same as session-bus-restart path: reset conflict cache + re-register) so after install/uninstall/reinstall success the running window immediately takes over the D-Bus names (no restart needed). The re-register path relies on `setupPortalFileChooser`/`setupFileManager1` treating "name owned by this process's other connection" (EXISTS reply + GetConnectionUnixProcessID == process.pid) as success, avoiding spurious conflict reports. e2e harness calls `registerSystemHandlers()` without callbacks — unaffected.
- **CSS only**: no CSS-in-JS or CSS modules — plain `.css` files in same directory as component, imported in component file.
- **Monorepo workspace**: `pnpm-workspace.yaml` exists but only for `allowBuilds` hints (no actual packages). Both `package-lock.json` and `pnpm-lock.yaml` are checked in.

## Style conventions

- **Indentation**: ESLint enforces 2-space indent (`indent: ['error', 2]`)
- **Imports**: React imports use `import { ... } from 'react'` pattern.
- **TypeScript frontend**: `verbatimModuleSyntax: true` in `tsconfig.app.json` — use `import type` for type-only imports.
- **`noUnusedLocals` / `noUnusedParameters`**: both enabled in frontend tsconfig — unused vars/params cause compile errors.
- **CSS**: flat `.css` files in same directory as component, imported in component file.
- **Web Components**: Material 3 UI uses `@material/web` custom elements (e.g., `<md-filled-button>`, `<md-dialog>`). Import any new component in `src/material-web.ts`.

## 注意事项

一定要写jsdoc！写字段说明！！！写i18n！！

先检查会不会破坏别的代码逻辑和功能。会的话跟我说！！
